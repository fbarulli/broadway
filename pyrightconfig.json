{
  // Pyright ADVISORY config — compare-vs-mypy period (opened 2026-09-19).
  // mypy remains the ENFORCING checker (run_local_ci.sh `mypy` gate).
  // This config exists so the advisory gate and editors share one scope:
  // src/broadway only, basic mode, zero suppressions — raw findings feed
  // the keep-mypy-vs-switch comparison. Promote to enforcing only by ruling.
  // Pinned runner: npx pyright@1.1.414 (see scripts/pyright_advisory.sh).
  "venvPath": ".",
  "venv": ".venv",
  "include": ["src/broadway"],
  "typeCheckingMode": "basic"
}
