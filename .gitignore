.env
.venv/
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
.ruff_cache/
mlruns/
notebooks/
*.egg-info/
dist/
build/
data/raw/
data/processed/
data/samples/
# STATE-20260921-004 custody: artifacts/tracking/ is the ignored machine truth
# (data_manifest.json + baseline_stats.json written by 001/002 writers, owned
# by the etl/train steps). The published surface is tracked
# reports/tracking/ (data_card.md + rendered summaries via the report/audit
# renderer pattern) plus MLflow run artifacts on the train run (mlruns/ stays
# local-only). Do NOT add negations for artifacts/tracking/ here.
artifacts/
/results/
project/experiments/results/**
!project/experiments/results/**/
!project/experiments/results/**/*.csv
# Pinned sample + evidence (single source of truth — committed on purpose)
!project/experiments/results/univariate/sample_evidence/sample_evidence.parquet
!project/experiments/results/univariate/sample_evidence/sample_evidence.json
*.png
!reports/figures/*.png
graphify-out/
.mplconfig/
diagnostics_experiment/
legend_experiment/
.coverage
data/optuna-backup/
deepseek-harness/
.mlflow*.db

.uv-cache/

# SECRET-1: local-only resolved optuna DB credentials — never commit.
k8s/optuna/secret.local.yaml
k8s/optuna/.secret.local.*
