data_source:
  loader: joined
  schema_contract: joined
features:
  include:
    - pickup_location_id
    - dropoff_location_id
    - trip_distance
    - passenger_count
  exclude: []
  derived:
    - name: pickup_hour
      func: datetime_hour
      source: pickup_datetime
    - name: pickup_day_of_week
      func: datetime_dayofweek
      source: pickup_datetime
    - name: pickup_month
      func: datetime_month
      source: pickup_datetime
    - name: is_weekend
      func: is_weekend
      source: pickup_datetime
    - name: rush_hour
      func: in_hour_window
      source: pickup_datetime
    - name: is_night
      func: is_night
      source: pickup_datetime
    - name: same_borough
      func: same_group
      source: pickup_location_id
  encodings:
    - type: target
      columns:
        - pickup_location_id
      smoothing: 20
model:
  type: linear
  params: {}
split:
  type: time
  validation_size: 0.2
random_state: 42
target_metric: rmse
