# DB credentials — mounted as FILES (/etc/broadway/secret/<KEY>), never env.
# Values are for the local kind cluster; rotate for any real deployment.
apiVersion: v1
kind: Secret
metadata:
  name: optuna-db
type: Opaque
stringData:
  DB_USER: optuna
  DB_PASSWORD: TozGU1qsvK4mHl4_yqlhBhmF
  DB_NAME: optuna
