apiVersion: apps/v1
kind: Deployment
metadata:
  name: broad-way-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: broad-way-api
  template:
    metadata:
      labels:
        app: broad-way-api
    spec:
      containers:
        - name: api
          image: broad-way:latest
          command:
            - uvicorn
            - inference.api:app
            - --host
            - "0.0.0.0"
            - --port
            - "8000"
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: broad-way-api
spec:
  selector:
    app: broad-way-api
  ports:
    - port: 80
      targetPort: 8000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: broad-way-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: broad-way-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
