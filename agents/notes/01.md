Yes — for **diagnostic #1 (mean/function specification)**, this is enough to design the first implementation cleanly.

I would **not modify `AnalysisPlan` yet**. It is currently a generic statistical-result container, while this new layer wants a more explicit structure:

**Question → Evidence → Ramification**

The first small step should be a new typed result, probably in `stats/diagnostics.py` or a new `stats/diagnostic_models.py`:

```python
class DiagnosticResult(BaseModel):
    question: str
    evidence: list[str]
    ramification: str
    warnings: list[str] = []
```

Then diagnostic #1 can produce something conceptually like:

```text
question:
"Is the mean relationship correctly specified?"

evidence:
- residual-vs-fitted plot persisted
- residual-vs-predictor plots persisted
- quantitative summaries, where useful

ramification:
"Systematic residual structure suggests the mean function may be misspecified;
consider an appropriate transformation, nonlinear term, spline, or interaction,
then refit and re-diagnose."
```

One important change to the existing plotting code: `plot_residuals()` currently bundles three unrelated questions into one PNG:

* residual-vs-fitted → functional form / variance clues
* Q-Q → residual distribution
* histogram → residual distribution

For the walkthrough/router architecture, I would **split these plots by diagnostic question** rather than keep one omnibus `plot_residuals()` artifact.

So **next single implementation step**:

**D1.0 — extract `plot_residuals_vs_fitted(model, out_path)` as the visual evidence for “Is the mean relationship correctly specified?”**

No automated U-shape/S-shape classifier yet. First generate and persist the evidence; then inspect it manually before deciding what typed pattern detection is worth automating.

## Status — 2026-08-26 (post-landing addendum)

All of the above has landed; this note's plan is complete except the last line (the plot split).

- **`DiagnosticResult`** exists exactly as proposed, at `src/broadway/stats/diagnostic_models.py` (pydantic, `extra="forbid"`, fields `question/evidence/ramification/warnings`).
- **D1.0 done** — `plot_residuals_vs_fitted(model, out_path)` extracted at `src/broadway/stats/diagnostics.py:45`, one PNG per question. `plot_residuals()` still bundles Q-Q + histogram (only the residual-vs-fitted pane was split out).
- **D1 typed result landed** — `mean_specification_diagnostic()` (question "Is the mean relationship correctly specified?").
- **D3 typed result landed** — `constant_variance_diagnostic()` (question "Is the error variance constant?"), Breusch-Pagan + plot as evidence (commit `7c9627b`).
- **Wire-up landed** — `AnalysisStep.diagnostic: DiagnosticResult | None` on the timeline model, and the report renderer emits a `## Evidence` (+ `## Warnings`) section when present (commit `0ea2267`). The typed result is now load-bearing end to end without adding a regression step to the group-comparison walkthrough.
- **Plot split landed** — the omnibus `plot_residuals()` now delegates each pane to a question-specific plotter: `_plot_residuals_vs_fitted` (already), `_plot_residuals_qq`, `_plot_residuals_histogram`, with public `plot_residuals_qq` / `plot_residuals_histogram` wrappers and a `residual_distribution_diagnostic()` (question "Is residual non-normality problematic for inference?", Q-Q plot + Jarque-Bera evidence).
- **D4 landed** — `influence_diagnostic()` (question "Is the result being driven by a few observations?"), `plot_cooks_distance()` + `_influence_statistics()` private helper, Cook's-distance plot with 4/n threshold line and max-Cook's/leverage evidence.

With these, the diagnostic matrix is: D1 mean specification, D3 constant variance, D4 influence, D5 residual distribution (normality) — all typed `DiagnosticResult`s following the same question → evidence → ramification shape, all data-agnostic. The notes' plan is now fully landed.
