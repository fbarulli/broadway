yes — and the platform is clean (per the 2026-08-26 Taxi-MENTION-CENSUS
verdict in STATE.md:206). Every "taxi" hit inside src/broadway/ is generic
MLflow vocabulary (champion/candidate) or onboarding schema-inference
(datetime_candidate). No literal `taxi`, `borough`, `PULocationID`,
`fare_amount`, `passenger_count`, `yellow_tripdata`, `tpep_`, `RatecodeID`,
`congestion_surcharge`, or `airport_fee` occurs in src/broadway/.

So `AnalysisStep`/`DiagnosticResult` never needs to know the dataset name.
The first D1.0 plot (`plot_residuals_vs_fitted`) and the typed
`DiagnosticResult(question, evidence, ramification, warnings)` proposed in
01.md work against whatever residual-vs-fitted the fitted model emits —
fully agnostic of whether the underlying rows are taxi, bikeshare, or
synthetic.

The one place the platform carries the dataset name is the MLflow
registered-model namespace: `src/broadway/training/mlflow_utils.py:163`
calls `register_model(cfg.dataset.name, …)`, so `models:/taxi@champion`
is the production alias today. For a non-taxi swap, that single namespace
moves with the dataset (pinned by GATE-CUST-148). Everything else in
`src/broadway/` is data-agnostic by design.

For the full sweep of where taxi lives outside the agnostic main
(raw parquets, lookup symlink, configs/*taxi*.yaml chain, project/
teaching layer, experiments/, k8s configmap, CI gating, MLflow
namespace), the in-repo sources are the 2026-08-26 census verdicts at
`agents/ledger/STATE.md:206` (Taxi-MENTION-CENSUS — zero doctrine
violations in src/broadway @ main) and `STATE.md:207` (Taxi-GROUND-TRUTH —
no real taxi data reachable from main), plus the gates.yaml rows cited
therein and `D-ops-security-verdicts` SEC-S7 (out-of-repo lookup
symlink).

## Status — 2026-08-26 (post-landing addendum)

The "next single step" below is now done and superseded:

- **D1.0 done** — `plot_residuals_vs_fitted` exists at
  `src/broadway/stats/diagnostics.py:45` (a real extraction, not a
  wrapper around the omnibus `plot_residuals`).
- **D1 typed result** — `mean_specification_diagnostic()` landed.
- **D3 typed result** — `constant_variance_diagnostic()` landed (commit
  `7c9627b`), reusing `plot_residuals_vs_fitted` + `bp_test`.
- **Wire-up** — `AnalysisStep.diagnostic: DiagnosticResult | None` on
  the timeline model plus the report renderer's `## Evidence` / `## Warnings`
  sections (commit `0ea2267`). The typed `DiagnosticResult` is now
  load-bearing end to end.
- **D4 + plot split landed** — `influence_diagnostic()` (Cook's distance,
  question "Is the result being driven by a few observations?"),
  `residual_distribution_diagnostic()` (Q-Q + Jarque-Bera), and the
  omnibus `plot_residuals()` now delegates each pane to
  `plot_residuals_vs_fitted` / `plot_residuals_qq` /
  `plot_residuals_histogram`. The D1–D5 diagnostic matrix is complete in
  the same question → evidence → ramification shape, all data-agnostic.
  API.md + dataflow.md updated in the same landing.

The diagnostic track described across 01.md / 02.md is now fully landed.
