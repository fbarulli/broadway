# PACKET C — PLATFORM-WIRING · SENIOR RULING (verbatim, 2026-08-24)

## Step-0 echo

- **Hash gate:** dispatch stamp `5016e93` == actual HEAD `5016e937e6aa67b301e3b54b78b1891f85784c67`, branch `sklearn`. PASS.
- **Tree state:** 40 dirty entries, all matching STATE.md's declared prior-session batches (untracked experiments/more_modeling/{16..22} + governance ledger landing; modified ETL/tests WIP). No lane owns my target files mid-flight. Read-only custody held: zero writes, zero staging.
- **Context gate:** STATE.md has no active lanes; backlog line 92 ("audit.py/qq.py render branches") matches packet dedupe note folding into #17. No contradictions with instructions.

## Ruling table

Evidence re-derived live: runners.py:314-321/:394-395/:472-473 forks · decide.py:27-34 · walkthrough.py `_print_*` hard-indexing + unprescribable-kind else-branch · module.py:19-46 vs records.py:37-40 writers · describe.py:138-140 + runners.py:138 dual `describe.json` · ids.py:1-2 + records.py:39 collision · graph.py:18-35 KIND_LABELS (zero production references) · state.py:5-9 · etl/module.py coercion write · resolver.py passthrough · loader.py:34/:109-110/:126 · schema.py:51-79 (no `extra=`) · pandera.py:34-43 pa.Object tail · generic.py:16-19 silent skip · samples/loader.py:38,41 `.get` · builders.py kw.get policy defaults · qq.py:435-437 compute-in-plot + :301 shared helper · evidence.py:16-20 (no show_log field) · production.yaml `${VAR}` + zeroed sample sizes · test_surface_integrity caps html/png only · FIXES.md B1/C1 queued · DECISIONS.md D11/D13.

| # | FINDING (restated) | VERDICT | root | rationale / strongest alternative | now-fix |
|---|---|---|---|---|---|
| 1 | GATE-TLINE-52a method vocab double-owned (YAML allowlist vs executor forks), never cross-validated | **ADOPT** | admitted-vocabulary fact owned by two modules with no join; admission ≠ executability | Alt: derive dispatch wholly from YAML registry — loses on size now; shared constant + record-time executability check + tripwire kills the class cheap | S2 |
| 2 | GATE-TLINE-52b fork ends disagree on bad kind: hard-index KeyError vs .get; prescription record() will reject | **ADOPT** | `decide_*` ids become kinds via removeprefix with no membership gate at load | Alt: fix only the two `[]` accesses — treats symptom; validate decision-step ids against WalkthroughConfig.decisions at sequence load, keep else-stop as defense | S3 |
| 3 | GATE-TLINE-53 writers-of-record duplicated (timeline/module vs lineage/records), durability fixes land twice | **REJECT**(do-nothing now) | no shared persistence primitive — but also no driver yet | Two 4-line writers over distinct env roots/filename rules; a shared helper today is wrapper-machinery saving nothing. Board watch-row: single primitive becomes mandatory BEFORE a third writer appears | DEFER→WIRE-PERSIST |
| 4 | GATE-TLINE-55 execute-time forks = unvalidated twin; conclusion else KeyErrors epsilon_squared on future method | **MODIFY**(to: subsumed by #1) | same root as #1 — one vocabulary, two owners | METHOD_DISPATCH map makes conclusion's effect-size branch exhaustive by membership, else-branch unreachable; zero extra files beyond S2 | ride S2 |
| 5 | GATE-TLINE-56a six evidence JSONs read by nobody; surfaces render duplicated result_summary copy | **HUMAN-CALL**(owner: human, ask: declare evidence/*.json debug-provenance-not-contract (docstring note, desync accepted) OR fund an explicit reader task) | artifact contract aspirational: machine pointers never dereferenced | Alt chosen between two honest end-states (read-back vs demotion) changes product contract — not senior-rulable | — |
| 6 | GATE-TLINE-56b two modules own name describe.json with divergent freshness | **MODIFY**(to: timeline copy renamed `describe_step.json`; stats/describe.py sole owner of the name) | run_describe re-executes what another module persists under the same filename | Alt: runner consumes stats' file — loses: walkthrough must run standalone in isolated TIMELINE_DIR | S11 |
| 7 | GATE-TLINE-58a decisions_dir reader-no-writer; published open/resolved fed by hand fixture (always empty in prod) | **HUMAN-CALL**(owner: human, ask: wire a producer joining timeline+lineage decision ledgers, or delete the dead lineage-decision subsystem incl. DecisionRecord consumption) | a published surface consumes a record stream nothing mints | Both options are contract declarations; deletion-first leans delete but >4-file blast exceeds window | — |
| 8 | GATE-TLINE-58b node_id ':'→'_' lossy sanitize silently overwrites ('a:b','c' ≡ 'a','b:c') | **ADOPT** | primary-key sanitization lossy with no uniqueness assertion | Alt: percent-encode filenames — breaks every existing artifact/glob reader; fail-loud detection first at ~6 lines | S8 |
| 9 | GATE-TLINE-58c KIND_LABELS/LINEAGE_STEPS omit 'coercion' | **MODIFY**(to: DELETE dead KIND_LABELS dict + its test pin; do NOT add coercion to LINEAGE_STEPS) | kind vocabulary maintained as parallel literals neither wired nor deleted | Packet's add-everywhere loses twice: KIND_LABELS has ZERO production references (mermaid renders node.label), and coercion is conditional — listing it as a stage puts it in not_yet_run forever on coercion-free datasets. Coercion folds under the etl stage by decision | S9 |
| 10 | GATE-TLINE-59a project/scripts/* 11 entry points keep no records | **HUMAN-CALL**(owner: human, ask: declare project/scripts record-free teaching surfaces in contract docs, or fund a record-writing shim) | two sanctioned front doors, asymmetric record obligations never contracted | Note: numbering gap — no `03_*.py` exists (assumption audit) | — |
| 11 | GATE-TLINE-59b decide branch unpinned end-to-end at CLI level | **REJECT**(with: stale census — tests exist) | gate evidence not re-derived against tests/test_cli_dispatch.py | `test_cli_decide_records_and_saves_decision` (:263) drives `cli.main()` via sys.argv through require_mode→record→save_decision with spies + output asserts; `:295` pins --kind posthoc choices. The claimed wiring gap does not exist at HEAD | none |
| 12 | F-SURF-1 plot module computes z-standardization + probplot from raw arrays | **REJECT**(with: do-nothing) | mislocated purity boundary: figure producers are compute-by-design (GATE-SURF-66 precedent), purity governs markdown renderers of record | z-scores/probplot are figure geometry intrinsic to drawing a QQ plot; parsing persisted evidence to redraw them adds coupling, changes nothing. Real defect is #13's unpersisted choice, ruled separately | none |
| 13 | F-SURF-1b pooled-skew raw-vs-log layout choice unverifiable from artifacts | **ADOPT** | rendering decision derived from data not recorded | Persist show_log + pooled skew on NormalityEvidence/result_summary; test-first | S10 |
| 14 | F-SURF-2 plot_describe_figures re-derives groups caller already built | **MODIFY**(to: thread caller's arrays in; NOT plot-from-GroupSummary) | signature forces re-derivation because raw arrays aren't passed | Presented "read persisted summary" is impossible — boxplots need raw arrays GroupSummary doesn't carry; pass `arrays` from the caller that already has them | S12 |
| 15 | F-SURF-3 dead render_index probes wrong filenames; wiring it = two writers on results/index.md | **DEFER** (verdict-class ADOPT-direction) | second renderer kept alive by test imports only | Deletion measured: index.py:16-58 ≈43 lines + test import/2 tests ≈19 = **62 lines > 60 window** → honest DEFER, refusal condition already recorded in SURFACE MAP | DEFER→SURF-KILL-1 |
| 16 | F-SURF-4 .svg/.jpg land tracked with no size gate | **ADOPT** | integrity gate enumerates formats instead of bounding binary class | Zero .svg/.jpg exist today (verified) — pure prevention, test-is-the-gate so trivially test-first | S5 |
| 17 | F-SURF-5 viz.yaml names every figure but cannot suppress emission | **ADOPT**(direction) — oversized | emission policy lives in code control-flow, config owns names only | Knob plumbing ×6 figure families across discover/qq/runners/config >4 files; folds the audit.py/qq.py optional-render backlog rider (STATE.md:92) into ONE board row | DEFER→VIZ-SUPPRESS |
| 18 | SURFACE-MAP watch _plot_raw_log_pairs serves surfaces 18/21 via different chains | **REJECT**(watch-only, no action) | shared save-path helper across two artifact owners — documented hazard, no defect | qq.py:301 verified called at :409 and :773, different out_paths; splitting a working helper to preempt a hypothetical refactor is speculative machinery; refusal condition already on the map | none |
| 19 | GATE-CFG-70a YAML-SSOT doctrine zero enforcement; violations in-tree | **ADOPT**(scoped form) | doctrine declared in prose, no executable probe | Tripwire over named config-consumption modules only; blunt repo-wide `.get(` scan would flag dozens of legitimate uses. samples/loader.py:38 required-field `.get("nullable")` fixed; builders kw.get policy-defaults are the separate hpo-.get annex class (backlog) | S13 |
| 20 | GATE-CFG-70b BROADWAY_CONFIGS_DIR silently repoints every load | **ADOPT** | escape-hatch override lacks provenance signal | Alt: remove env override — breaks test fixture machinery (tests/test_cli.py); warn-and-continue keeps deliberate use visible | S14 |
| 21 | GATE-CFG-71a unset-${VAR} literal passthrough, loud only by int-coercion accident | **ADOPT** — C owns the FIX side (D carries deploy consequences) | expansion layer has no postcondition; residual-placeholder scan absent | Post-resolve residual-`${` walk raising ValueError naming vars at config load. Alt: custom expander returning None-on-unset — equivalent outcome, more code | S1 |
| 22 | GATE-CFG-71b resolver module zero tests | **MODIFY**(to: subsumed by S1's failing-tests-first landing) | pure-function module never pinned | tests/test_resolver.py covers interpolation/expansion/unset-raise/walk; merge-order rides #23 | ride S1 |
| 23 | GATE-CFG-72 deep-merge precedence + list-replacement untested | **ADOPT** (test-only) | merge semantics pinned by nothing; merge-before-resolve order unpinned | Pure test addition pinning override-wins, list-replacement, partial-${VAR}-after-merge resolves once | S15 |
| 24 | GATE-CFG-74a DatasetContract/EnvironmentConfig lack extra='forbid' | **ADOPT** | truth-pinning layer trusts pydantic ignore-extra default | Typo'd yaml key (`datbase_host`) fails loudly instead of vanishing; shipped-config roundtrip must stay green (run_local_ci configs gate) | S7 |
| 25 | GATE-CFG-74b five dead EnvironmentConfig fields; prod hardcodes 0, no ge constraint; D11 rules straight delete | **HUMAN-CALL**(owner: human, ask: authorize executing D11 straight-delete now — touches deployed env contracts across 3 environment yamls + schema.py; arithmetic ready, ~−25 lines/4 files) | D11 authority exists; execution deletes fields deployment configs still declare | Packet mandate flags this class human-only rather than silently deferred | — |
| 26 | GATE-CFG-75 unrecognized dtype → pa.Object validates almost anything | **ADOPT** | closed dtype vocabulary enforced nowhere at derivation | Raise at pandera_dtype derivation. Verified safe: shipped taxi.yaml uses datetime64/float64/int32 only — all recognized | S4 |
| 27 | GATE-CFG-76 B1 include-validation queued; typo'd include silently shrinks feature surface | **MODIFY**(to: exact ratified B1 end-state — raise on unresolved include vs contract ∪ joined_schema_columns + tripwire) | silent skip = missing else-contract on declared surface | Implementation belongs to the ratified Tier2 lane (FIXES.md slate v4 ⭐; D13 absorbs B1/B2) — pre-empting here spawns a duplicate-custody clash with a queued contract that is strictly larger (∪ joined-lookup) | DEFER→B1 row (existing) |
| 28 | GATE-CFG-79 boundary tripwire hand-maintains _SAMPLE_SCHEMA literal beside shipped config | **ADOPT** | second declaration surface drifts invisibly | Derive expectation from configs/sample/fare_prediction_1m.yaml schema block (verified byte-equivalent today — swap is behavior-neutral now, pins drift later); strengthens the tripwire, single declaration | S16 |

## SLATE (ready-to-execute, budgeted; each item ≤4 files / ≤60 lines / reversible / test-first)

| id | findings | edits | files × lines | acceptance |
|---|---|---|---|---|
| S1 | #21+#22 | `resolver.py`: post-resolve residual-`${` scan raising ValueError naming vars (+8); NEW `tests/test_resolver.py`: interpolation, expanduser, unset→raises, dict/list walk (+34) | 2 × 42 | `uv run pytest tests/test_resolver.py tests/test_config.py -q` |
| S2 | #1+#4 | `runners.py`: `METHOD_DISPATCH` frozenset map, both forks consult it, conclusion branch keyed on membership (+10); `decide.py`: executability check vs dispatch (+5); `tests/test_decide.py`: tripwire walkthrough.yaml methods ⊆ dispatch (+12) | 3 × 27 | `uv run pytest tests/test_decide.py tests/test_walkthrough.py tests/test_timeline.py -q` |
| S3 | #2 | `sequence.py`: validator — decision steps' ids must map to WalkthroughConfig.decisions keys (+7); `tests/test_timeline.py`: bad decide_x id raises at load (+9) | 2 × 16 | `uv run pytest tests/test_timeline.py tests/test_walkthrough.py -q` |
| S4 | #26 | `contracts/pandera.py`: unknown dtype raises naming string (+3); `tests/test_boundary_contracts.py`: misspelled-dtype tripwire (+7) | 2 × 10 | `uv run pytest tests/test_boundary_contracts.py tests/test_contracts.py -q` |
| S5 | #16 | `tests/test_surface_integrity.py`: SVG_CAP_BYTES + jpg→PNG cap, extend suffix loops (+12) | 1 × 12 | `uv run pytest tests/test_surface_integrity.py -q` |
| S7 | #24 | `config/schema.py`: `model_config = ConfigDict(extra="forbid")` on DatasetContract + EnvironmentConfig (+4); `tests/test_config.py`: typo'd-key-raises tripwire (+10) | 2 × 14 | `uv run pytest tests/test_config.py tests/test_loader.py -q && bash scripts/run_local_ci.sh --tier=fast` |
| S8 | #8 | `lineage/records.py`: existing-filename ∧ differing node_id ⇒ ValueError naming both (+6); `tests/test_lineage_models.py`: collision tripwire (+8) | 2 × 14 | `uv run pytest tests/test_lineage_models.py tests/test_lineage_graph.py -q` |
| S9 | #9 | DELETE `graph.py:18-35` KIND_LABELS (−17); `tests/test_lineage_graph.py`: drop import+pin (−2) | 2 × 19 | `uv run pytest tests/test_lineage_graph.py tests/test_lineage_state.py -q` |
| S10 | #13 | `timeline/evidence.py`: `show_log: bool` + `pooled_skew: float \| None` (+2); `runners.py`: populate (+2); `tests/test_walkthrough.py`: assert persisted (+6) | 3 × 10 | `uv run pytest tests/test_walkthrough.py -q` |
| S11 | #6 | `runners.py`: evidence filename `describe_step.json` (+ref update ~2); `tests/test_walkthrough.py`: readers updated (~4) | 2 × 6 | `uv run pytest tests/test_walkthrough.py -q` |
| S12 | #14 | `stats/describe.py`: `plot_describe_figures` takes `arrays` param, internal build_declared_groups call removed (±6); `runners.py`: pass arrays (+1); `tests/test_describe.py` (+3) | 3 × 10 | `uv run pytest tests/test_describe.py tests/test_figures.py -q` |
| S13 | #19 | `tests/test_platform_hygiene.py`: `.get(`-with-default probe scoped to named config-consumption modules (+14); `samples/loader.py`:38 `entry["nullable"]` required (±2) | 2 × 16 | `uv run pytest tests/test_platform_hygiene.py tests/test_samples.py -q` |
| S14 | #20 | `config/loader.py`: warn when BROADWAY_CONFIGS_DIR set (+3); `tests/test_loader.py`: warning asserted (+6) | 2 × 9 | `uv run pytest tests/test_loader.py -q` |
| S15 | #23 | `tests/test_loader.py`: _deep_merge override-wins, list-replacement, merge-before-resolve partial-${VAR} (+24) | 1 × 24 | `uv run pytest tests/test_loader.py -q` |
| S16 | #28 | `tests/test_boundary_contracts.py`: `_SAMPLE_SCHEMA` derived from shipped fare_prediction_1m.yaml schema block (±10) | 1 × 10 | `uv run pytest tests/test_boundary_contracts.py -q` |

Slate total ≈ **225 changed lines across 22 file-touches**, every item independently revertible; S1/S2/S4 are the priority spine (fail-loud at boundaries).

## DEFER (each → CHANGE BOARD issue #5 row)

- **#3** → `WIRE-PERSIST`: unify record-persistence into one primitive when the first real durability requirement lands; refusal condition at a THIRD record-writer appearing.
- **#15** → `SURF-KILL-1`: delete reports/index.py render_index + its 2 tests (measured 62 lines, over window by 2 — pure deletion, zero risk) before anyone wires it.
- **#17** → `VIZ-SUPPRESS`: viz.yaml emission-suppression knobs family, absorbing the audit.py/qq.py optional-render backlog rider (STATE.md:92) as one scoped row.
- **#27** → routed to **existing ratified B1 row** (FIXES.md slate v4, D13 Tier2) — do not double-implement; this packet claims no slice of it.

## Tally

`VERDICTS: 13 adopt, 6 modify, 4 reject, 4 human-call`

## Judgment

The wiring hazard most likely to bite a future session is the one behind #1/#4/#21 together: **this platform's failure philosophy is "accidentally loud"** — nothing validates executability at admission (a YAML-plausible method records "resolved" then detonates mid-run_omnibus), nothing checks the resolved config tree for residual `${VAR}` (production credentials become directory names, caught only when an int field happens to choke), nothing pins the merge order that makes those two layers compose. Each bug individually looks like user error; collectively they mean the pipeline's contract layer detects failures by type-collision coincidence, not by design, and every future feature that threads config → decision → execution inherits that silence. Fix S1+S2+S3 first; they convert three classes of delayed crash into same-second load errors for under 90 lines. On the kills: #11 died because the gate's census predates tests/test_cli_dispatch.py — a reminder that GATES.md FINDING blocks age faster than the code they audit; #12 and #18 die because "compute happens near plotting" is design, not drift, and pre-splitting working helpers to preempt hypothetical refactors is exactly the speculative machinery Q2 exists to hunt.

## Assumption audit

Things checked beyond the brief: (1) #11's refutation — CLI decide tests exist at tests/test_cli_dispatch.py:263/:295 driving cli.main(); (2) KIND_LABELS is wholly production-dead (mermaid renders node.label; zero src references), so the packet's proposed fix fed a corpse; (3) project/scripts has a numbering gap (no `03_*.py`; brief implied 01–12 contiguous); (4) zero .svg/.jpg exist under reports/ today — #16 is pure prevention; (5) taxi.yaml dtypes (datetime64/float64/int32) all sit inside the recognized map, making #26's raise safe to ship; (6) shipped fare_prediction_1m.yaml schema block is currently byte-equivalent to the _SAMPLE_SCHEMA literal, making S16 behavior-neutral.

## OPEN QUESTIONS

none — all surfaced items resolved into rulings above; #5/#7/#10/#25 carry exact asks for the human.
