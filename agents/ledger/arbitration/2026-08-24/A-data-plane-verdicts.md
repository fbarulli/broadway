# PACKET A — DATA-PLANE · SENIOR ARBITRATION REPORT (verbatim, 2026-08-24)

## Step-0 echo

- `git rev-parse --short HEAD` → **5016e93** = dispatch stamp ✅ (`git log -1`: "DOCS-TRUTH r1…")
- Tree: uncommitted WIP confirmed — `M project/etl/process.py` (**73 inserted lines: the entire `_ROW_DELTA`/`StageLedger`/`_stage_ledger`/`_ingest_audit` apparatus is WIP**, verified via `git diff HEAD -- project/etl/process.py`), `M src/broadway/cleaning/structural.py`, `M tests/test_structural_cleaning.py`, `M tests/test_lookup_value_audit.py`; untracked `project/tests/test_ingest_{audit,surface}.py` (R2 property — confirmed via `git status --porcelain project/tests/`). Slate below touches **none** of these.
- STATE.md contradiction echo: **none**. Nuance only: STATE says "active lanes: (none)" while the packet cites an R2 lane; resolved by STATE's own pending-change inventory (R1–R5 board rows cover the ETL batch + ingest tests) — board rows exist, live agents don't. Not stop-worthy.

## Ruling table

| # | Finding | VERDICT | root | rationale (alt angle → why it loses) |
|---|---|---|---|---|
| 1 | INGEST-01 FileNotFoundError branch untested | **ADOPT** | discovery gate behavior pinned nowhere; branch correctness rests on accident | Verified: `get_raw_files` has 0 refs outside def+call site. Prefix-generalization rider → DEFER DP-A-01. Alt: fix glob generality first → loses: behavior change to WIP-owned legacy ingest; tripwire first |
| 2 | INGEST-02 unsupported-ext ValueError untested; `read_raw_data` bypasses gate | **ADOPT** | two ingests, one gated — the ungated twin makes every gate opt-in by import choice | Verified: `grep 'unsupported format' tests/` → 0 hits; process.py:36-40 polars scan, no checks. Bypass rider → DEFER DP-A-02 (with #3/#7 family). Alt: gate read_raw_data now → loses: direct WIP collision |
| 3 | INGEST-04 drop-order divergence (dropna→dedup vs dedup→…→target-null) | **MODIFY**(to: one shared order primitive) | two hand-rolled cleaning orders guaranteeing divergent NaN-twin accounting | The dedup in `select_and_clean_columns` **is itself WIP** (diff hunk). Fix cannot land cleanly around R2 → DEFER DP-A-03. rename_map-outside-contract clause rides same row |
| 4 | INGEST-05 NaT recorded-not-raised; nullable=True lets it pass | **ADOPT** | evidence channels with no enforcer (shared family root, ruled at #6) | Verified structural.py:49-61 record-only; pandera.py:57 `nullable=True`. Killed by #6's raise at the channel's consumer. Residual "legacy never calls parse_datetime" → DEFER DP-A-02 |
| 5 | INGEST-06 FX-A05 refusal unreachable on NaN-bearing columns | **MODIFY**(to: refusal-before-NaN-guard reorder) | guard conflates "can't astype with NaN" with "skip fractional diagnosis" | **Severity correction:** for int-declared cols the outcome is imprecise diagnosis, NOT silence — float64 still fails the strict dtype gate (`coerce=False`, pandera.py:34-43). True silence requires float-declared cols (=#4/#6 family). Fix edits WIP-modified structural.py → DEFER DP-A-04 |
| 6 | INGEST-07 ParseFailure channel: nobody raises | **ADOPT** — the family root | evidence written to JSON no gate reads back; loudness delegated to a dtype gate that nullable=True blinds for NaT/NaN-coerce | Verified: `parse_failures` flows collect→persist→render, zero raise sites repo-wide. Alt: threshold config → loses: any unparseable declared-datetime value is contract violation; volume tolerance already owned by max_drop_fraction. Alt: raise inside primitives → loses: primitives stay pure/reusable; single enforcement point at orchestrator |
| 7 | INGEST-08 locked order binds CONTRACT pipe only; grammar validated nowhere | **MODIFY**(to: grammar SSOT now via #12; order unification deferred) | sole cross-pipeline invariant is a textual convention duplicated by comment | Grammar half slated (S3, incl. tripwire pinning the **WIP copy** read-only). Order half → DEFER DP-A-03. Alt: runtime grammar validator on both pipes → loses: same shared-symbol fix cheaper and total |
| 8 | INGEST-09 vacuous empty-contract schema; legacy pre-write-only | **ADOPT** | schema derived from config with no config-side invariant: `columns: dict[str, ColumnSchema]` (schema.py:57) has no min-length → `pa.DataFrameSchema({})` validates anything | Verified pydantic model plain. Fix = config-load rejection (S6). Legacy-reload clause folds into #13. Alt: pandera-side guard → loses: wrong layer; a meaningless contract should die at load, before any frame exists |
| 9 | ETL-11 NA semantics asymmetric across one merge | **HUMAN-CALL**(owner: human, ask: declare main-frame NA tokens in DatasetContract to align both merge sides, or ratify current asymmetry as documented intent) | one merge, two NA vocabularies chosen independently per side | Verified loader.py:129 vs :134. Lookup side is deliberate authored-token design (pinned by test_keep_default_na_false_preserves_literal_tokens); aligning means data-semantics renegotiation — not deferrable silently per packet. Divergence is observable in JoinAudit, so not silent corruption |
| 10 | ETL-14 lookup-value audit evidence-only | **ADOPT** | audit computed, persisted, parented in lineage — and consumed by nothing | Opt-in `max_affected_rate` per lookup (default None = today). Alt counter considered seriously: "config nobody varies" = speculative param → loses: the finding IS a missing consumer; policy number is genuinely per-dataset (sparse lookups legit); default-off keeps platform neutral. Ratifying taxi.yaml values → OPEN QUESTION |
| 11 | ETL-15 coercion evidence partial; persistence branch untested | **ADOPT** (persistence pin) | evidence chain's last hop (collector→JSON artifact) unpinned; verified `grep coercion_audit tests/` → 0 hits | Slate pins the artifact. rows_affected=len(column) semantics + float/datetime record gaps → DEFER DP-A-05 (API semantics change, structural.py is WIP-touched) |
| 12 | ETL-18 grammar regex duplicated, no common symbol | **ADOPT** | accounting correctness hangs on byte-identical independent literals | Verified copies at process.py:23, module.py:35 — **plus a third the brief missed: reports/audit.py:526**. Shared symbol + equality tripwire (S3); audit.py copy → DEFER DP-A-06 rider |
| 13 | ETL-16 post-write reload validation absent | **MODIFY**(to: pinned parquet round-trip TEST; reject runtime re-validation) | trust boundary between written bytes and re-read bytes never exercised in CI | Alt: reload+validate after every `to_parquet` → REJECTED: re-reads 8.5M rows per run to duplicate what read-side contracts B2–B4 already catch loudly at consumption; a CI round-trip pin kills the class at zero runtime cost (S8) |
| 14 | ETL-17 StageLedger stores rows-after, appends zero-drop stages | **MODIFY**(to: deltas or documented rows-after contract) | ledger representation chosen for convenience of append, not for its consumers | Entire mechanism is WIP code → DEFER DP-A-07 (decide with R2 owner) |
| 15 | ETL-19 enforce_drop_fraction never called at ingest | **MODIFY**(to: enforce at ingest with ProjectConfig threshold) | unexplained-loss enforcement stops at the band boundary; ingest record ships unexplained counts unconsumed | Verified callers: only module.py:127, features/module.py:68. Call site + config knob live in WIP files → DEFER DP-A-08 bound to R2 landing |
| 16 | FEAT F1 contract file/symbol drift | **ADOPT** (doc truthing) | gate law names symbols that never existed; law and tree diverge at the citation layer | Verified: `builder.py` absent; `build_distance_features`/`validate_engineered_schema` → 0 hits in src/+tests. Stale strings live ONLY in **untracked** GATES.md (:347,:355,:381,:1280) — governance-batch property → DEFER DP-A-09 with exact corrections supplied |
| 17 | FEAT F2 log1p pinned only via double-log chaining | **ADOPT** | transform output rides on guard vehicles; regression in the lambda itself fails nothing directly | Verified: BUILDERS['log_distance'] builders.py:53; chaining test re-implements the formula (asserts tautology), guards assert raises only (S10) |
| 18 | FEAT F3 TargetEncoding.fit y-is-None unpinned | **ADOPT** | sklearn-contract raise site with zero test node ids | Verified `grep 'requires y' tests/` → 0; raise at transformers.py:52-55 (S11) |
| 19 | FEAT F4 features run()/write path no direct node id | **MODIFY**(to: direct run() smoke test) | step entry point exercised only through other bands' fixtures | Full smoke test needs split-artifact fixture chain ≫60 lines → DEFER DP-A-10 (coverage-completion batch) |
| 20 | FEAT F5 write-side schema unordered vs read ordered=True | **ADOPT** | the write gate structurally cannot see the drift the read gate exists to catch | Verified schema.py:29-37 (no ordered) vs generic.py:55 (ordered=True); parity test already proves producer order == specs order, so flipping ordered=True is safe today (S4, RED-first) |
| 21 | FEAT F6 engineered read contract tested only transitively | **ADOPT** | refactor survival depends on tests that name other modules' loaders | Verified 0 direct refs (only a docstring mention). Direct pins (S5) |
| 22 | BACKLOG read_sample bypasses load_with_audit | **REJECT**(with: keep the lazy fast path; B8 stays documented no-validation; board decides on lightweight label-guard only if samples ever feed joined schemas) | sample surface is deliberately a raw-exploration reader, not a contract consumer | Verified loader.py:153-176 + boundary header B8 "no validation" + existing unit tests for size/seed/pruning/full. Routing through load_with_audit destroys scan-time column pruning and imposes join audits on exploratory reads — cost with no corrupted-consumer identified |
| 23 | BACKLOG Option C typed-source hard rejection | **MODIFY**(to: typed-source rejection layered on Option E collector) | coerce-and-record still launders fundamentally mistyped sources into NaN | Explicitly unscheduled follow-up; implementation surface = structural.py (WIP-touched) → DEFER DP-A-11 |

## SLATE (worker-executable; avoids all WIP/M/untracked-R2 files; per-item arithmetic shown)

| Item | Findings | Files | ~Lines | Edit | Acceptance command |
|---|---|---|---|---|---|
| S1 | #1+#2 | NEW `tests/test_raw_discovery.py`, NEW `tests/test_loader_unsupported_ext.py` | 15+15=30 | Test A: empty raw dir → `FileNotFoundError` naming raw_dir (mirror tracked `project/tests/test_process.py` import pattern; root tests already import `project` — verified via `tests/test_platform_hygiene.py`). Test B: `.txt` path → `ValueError("unsupported format: .txt")` at loader.py:127 | `uv run pytest tests/test_raw_discovery.py tests/test_loader_unsupported_ext.py -q` |
| S2 | #6+#11 | `src/broadway/etl/module.py`, NEW `tests/test_parse_failure_enforcement.py` | 4+32=36 | After canonicalize (module.py:~90): `if parse_failures: raise ValueError(f"{n} unparseable value(s) recorded: …")`. Tests: (a) run with garbage datetime col → raises, names column; (b) clean run unaffected; (c) int-coercion run writes `<name>_coercion_audit.json` + lineage record (pins the UNVERIFIED persistence branch). **RED-first**: (a) fails on current code | `uv run pytest tests/test_parse_failure_enforcement.py tests/test_structural_cleaning.py -q` |
| S3 | #12+#7(grammar) | `src/broadway/lineage/records.py`, `src/broadway/etl/module.py`, NEW `tests/test_row_delta_grammar.py` | 3+1+14=18 | Export `ROW_DELTA_PATTERN = re.compile(r"-(\d+) rows$")`; module.py:35 imports it. Tripwire asserts `project.etl.process._ROW_DELTA.pattern == ROW_DELTA_PATTERN.pattern` (reads WIP symbol, edits nothing) | `uv run pytest tests/test_row_delta_grammar.py tests/test_loud_failures.py -q` |
| S4 | #20 | `src/broadway/features/schema.py`, NEW `tests/test_engineered_write_order.py` | 1+16=17 | Add `ordered=True` to `build_engineered_schema`. Tripwire FIRST: column-reordered producer frame must fail write-side validate (RED pre-fix, GREEN post) | `uv run pytest tests/test_engineered_write_order.py tests/test_boundary_contracts.py tests/test_engineered_order_parity.py -q` |
| S5 | #21 | NEW `tests/test_engineered_read_contract.py` | ≈52 | Direct pins: `engineered_schema_for` ordered violation; `_assert_no_extra_columns` extra-column; `validate_target_dtype` missing+dtype-mismatch; `validate_engineered_frame` SchemaErrors→SchemaError normalization | `uv run pytest tests/test_engineered_read_contract.py -q` |
| S6 | #8 | `src/broadway/config/schema.py`, NEW `tests/test_empty_contract_rejected.py` | 5+10=15 | `field_validator("columns")` → reject empty dict at config load ("dataset contract declares no columns") | `uv run pytest tests/test_empty_contract_rejected.py tests/test_recipe.py -q` |
| S7 | #10 | `src/broadway/config/schema.py`, `src/broadway/data/loader.py`, NEW `tests/test_lookup_threshold.py` | 3+6+14=23 | `LookupSpec.max_affected_rate: float \| None = None`; in `load_with_audit` post-`audit_lookup_values`: exceed → ValueError naming lookup+rate | `uv run pytest tests/test_lookup_threshold.py tests/test_join_audit.py -q` |
| S8 | #13 | `tests/test_structural_cleaning.py` is **WIP-M — instead**: extend via assertion inside NEW file `tests/test_canonical_round_trip.py` re-using the existing etl-run fixture pattern | ≈20 | Run etl on tmp data → `pd.read_parquet` written canonical → `build_raw_schema(dataset).validate(readback)` + per-column dtype equality | `uv run pytest tests/test_canonical_round_trip.py -q` |
| S10 | #17 | `tests/test_builders.py` (clean/tracked) | +10 | Single-hop golden: `BUILDERS["log_distance"](df,"x")` vs `np.log1p` reference on known inputs incl. 0 and 1, `pytest.approx` | `uv run pytest tests/test_builders.py -q` |
| S11 | #18 | `tests/test_transformers.py` (clean/tracked) | +8 | `pytest.raises(ValueError, match="requires y")` pin on `TargetEncoding.fit(X)` | `uv run pytest tests/test_transformers.py -q` |

Budget check: every item ≤4 files and ≤60 lines; largest S5 ≈52. No slate edit touches `project/etl/process.py`, `src/broadway/cleaning/structural.py`, `tests/test_structural_cleaning.py`, `tests/test_lookup_value_audit.py`, `project/tests/test_ingest_*`.

## DEFER → CHANGE BOARD (#5) rows

- **DP-A-01** ← #1 rider: generalize `yellow_tripdata_*` discovery prefix so CFG-DATASET-CONTRACT datasets can route (design: contract-declared glob).
- **DP-A-02** ← #2/#4 riders: legacy `read_raw_data` bypasses extension/merge/collision gates; legacy ingest never calls parse_datetime — reconcile when R2 lands.
- **DP-A-03** ← #3/#7(order): unify drop-order semantics behind one primitive (dropna→dedup vs dedup→…→target-null); rename_map ownership into DatasetContract.
- **DP-A-04** ← #5: FX-A05 refusal-before-NaN-guard reorder in `structural.py` (post-WIP).
- **DP-A-05** ← #11 riders: CoercionRecord for float/datetime drift; `rows_affected` = actually-changed count, not column length.
- **DP-A-06** ← #12 rider: third grammar parser `reports/audit.py:526` — fold into shared symbol or prove render-side independence.
- **DP-A-07** ← #14: StageLedger rows-after vs delta semantics + per-column dropna attribution (decide with R2 owner).
- **DP-A-08** ← #15: call `enforce_drop_fraction` at ingest with a ProjectConfig threshold once ETL batch merges.
- **DP-A-09** ← #16: doc-truth GATES.md stale symbols → `builders.py:53 BUILDERS['log_distance']`, `generic.py` trio (`validate_engineered_frame`/`engineered_schema_for`/`_assert_no_extra_columns`/`validate_target_dtype`); blocked only because GATES.md is untracked governance-batch property.
- **DP-A-10** ← #19: direct features `run()` smoke test (fixture-heavy, >60 lines).
- **DP-A-11** ← #23: Option C typed-source hard rejection scheduled after Option E settles.
- **HUMAN-CALL** (#9) is **not** deferred: main-frame NA token set becomes contract-declared, or asymmetry ratified as intent — human decision required.

## Tally

```
VERDICTS: 13 adopt, 8 modify, 1 reject, 1 human-call
```

## Judgment

The single worst silent-corruption path today is the **unenforced ParseFailure channel meeting the nullable-by-design dtype gate and ending at validation-free readers**: a float- or datetime-declared non-target column carrying unparseable tokens is coerced to NaN/NaT (`structural.py:50,89 errors="coerce"`), the ParseFailure is written into `<dataset>_clean.json` — a file whose only consumers render it, none enforce it (verified: zero raise sites on `parse_failures` repo-wide) — while `build_raw_schema` (names+dtypes only, `nullable=True` at pandera.py:57) waves the frame through pre-write validation; the poisoned canonical parquet then flows undisturbed into the two bands with *no* read contract at all (B6 stats, B7 timeline per the boundary map), so a biased analysis artifact ships green end-to-end. That is why #6 is the keystone adoption and S2 its test-first execution; everything else in this packet is either a tripwire for a gate that already works, or a deferral respecting the R2 WIP minefield.

## Assumption audit & open questions

**Re-verified beyond brief (≥3):** unsupported-format zero test hits; enforce_drop_fraction caller set; regex-copy set; nullable=True; y-is-None zero hits; F1 symbol absence; coercion-audit zero test refs. **Found that the brief never mentioned:** (a) **third** grammar copy at `reports/audit.py:526`; (b) the entire StageLedger/audit apparatus in process.py is uncommitted WIP, so #3/#14/#15 rule against not-yet-committed code; (c) FX-A05 degradation is diagnostic loss, not silence, for int-declared columns (dtype gate stays loud); (d) only `test_ingest_{audit,surface}.py` are untracked — `test_process.py` etc. are committed and safe slate anchors; (e) root tests already import the `project` package. **Open questions:** should shipped `taxi.yaml` set `max_affected_rate` values once S7 lands (board); does any legitimate flow rely on empty `contract.columns` (none found; S6 assumes no).
