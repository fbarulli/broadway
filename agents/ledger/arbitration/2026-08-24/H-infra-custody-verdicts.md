# PACKET H — INFRA / K8S / OBJECT-CUSTODY · SENIOR RULING (verbatim, 2026-08-24)

## Step-0 echo

- `git rev-parse --short HEAD` → **5016e93** == dispatch stamp == `gates.yaml meta.head` ✓ · branch **sklearn** ✓
- **Zero gh ops honored.** Git/docker/kubectl used read-only only (`GIT_OPTIONAL_LOCKS=0` discipline preserved; no refs mutated, no cluster ops).
- Tree: WIP-hot files material to THIS packet: `k8s/optuna/teardown.sh` (M) and `scripts/ship.sh` (M) — **both carry uncommitted remedies for gaps gated below**; every ruling states the HEAD-vs-worktree split explicitly. Slate was written against HEAD registry content; so are these verdicts.
- Scope ruled: GATE-INFRA-121..146 (band 09-infra-meta, all 26) · GATE-CUST-147..153 (new band 81, all 7) · cross-band items NAMED into this packet: TRAIN-120 (MLflow marker widening) · CFG-107 (symlink/bootstrap-existence). Total **35 rows**.

## LIVE-STATE DRIFT (verified this session — supersedes morning-census facts where they conflict)

1. **All six project container images are GONE from the local docker store** (`docker images`: only `kindest/node:<none>` 1.47 GB remains), and **container `broadway-view-pg` is GONE** (`docker ps -a` empty). The census's Plane-3 objects were destroyed between the census and now. Every gate aimed at those objects is adjudicated below under the creation-time-law rule.
2. Repo-root `.uv-cache` / `.mplconfig` also absent now (fork objects cleaned; home-side `~/.cache/uv` persists per census).
3. **Ref `sp_probe` is DELETED** (`git rev-parse refs/heads/sp_probe` fails) — census danger #1 self-resolved.
4. **Tag `tier-1-complete` is NOT local-only: `git ls-remote --tags origin` returns both the tag object (`015c5049`) and peeled (`0d383125`).** The gap-sheet claim "absent from origin" is FALSE as of now and is corrected here per the HC-2 ledger-truthing duty. This makes tag-minting custody *more* urgent (published, unminted-law object), not less.
5. `pr-1`/`pr-2` confirmed present, local-only (remote-tracking refs remain main/sklearn/taxi/taxi_work).

## BINDING PRECEDENTS APPLIED

- **HC-1 VOIDED** (00-resolutions.md §1): the SECRET-1/DB_PASSWORD matter is disregarded entirely ⇒ items 141/142 are grounded on **custody-hygiene law**, never on credential exposure.
- **Resolution #5** ("delete unless a producer is named") governs CUST-153. **Resolution #4** (debug-provenance demotion) bars ledger machinery without an enforcement consumer.
- Packet-D routings respected: DEPLOY-F1-API-LANE (#8/#9), DEPLOY-F2-TRAINJOB-LANE (#10/#18), IMAGE-TAG-COHERENCE (#15/#16/#17), CI-BUILD-BROADWAY-LATEST (#19), K8S-CONFIGMAP-ENV (#11 + kubeconform-scope rider), D#14 (manifest HPA = declared SSOT), D#17 (compose broken context), D#25 (`__main__`-guarded demo code is not a defect class).

## RULING TABLE

| # | ITEM | VERDICT | root | rationale (evidence; alt considered → why it loses) |
|---|---|---|---|---|
| 121 | qq demo `__main__` stray-write block | **OVERRULE→NOT-GATE** | deletion-first | Confirms slate REJECT. Block sits behind `if __name__ == "__main__":` (qq.py:819-820); zero library-path effect; writes fire only when invoked as a script. Same logic as D#25: refuse to edit working demo code to fix an effect impossible on import. Remedy = delete the block OR classify qq as a declared teaching surface (resolution #6). Id 121 **burned**. Alt "gate the write path" loses: gating a demo's CWD litter is machinery defending debris. |
| 122 | Root experiments CLI dispatcher custody | **CONFIRM-LAND** | ungated entrypoint owning plots/results/verify outputs | Dispatcher verified live: `add_subparsers` + ols/diagnostics/qq_legend/verify (experiments.py:494-501). Real write-surface, zero coverage elsewhere. |
| 123 | experiments_ui `__main__` uvicorn entry | **CONFIRM-LAND** | ungated server entrypoint | `uvicorn.run(app, host="127.0.0.1", port=8000)` (experiments_ui.py:894) verified. Co-tenancy with SURF-102 (FastAPI app custody) is legal: entry vs app-body aspects. |
| 124 | lifecycle.sh command-surface gate **incl. view stop-pairing** | **AMEND** | every host-object-creating verb must own its teardown pair | Verified: `view_local` backgrounds mlflow `( cd "$ROOT" && uv run mlflow server … & )` (:205-208) with NO stop verb in the dispatch table (train\|up\|view\|dump\|down, :221-231); cleanup is an echoed hint only — and :215 carries a doubled-`echo` BUG printing the hint literally (`echo   echo "[lifecycle] stop: …"`). AMENDMENT: the `stop` subcommand is a **binding part of this gate**, not a rider — `up`↔`down` pairing exists; `view`↔`stop` must be added (capture child PID to `$BACKUP_DIR/view.pid`; keep pgrep fallback; fix :215). The orphaned `broadway-view-pg` (now gone) was this function's own product — the pairing gap is proven, not hypothetical. validated_by: [] + FINDING(:215). |
| 125 | optuna-init.yaml container command custody | **CONFIRM-LAND** | init-job semantics live inside kubeconform scope | `command: ["python","/app/worker.py"]`, `args: ["--init-only"]` (:19-20) verified; structurally validated today, semantically owned by nobody. |
| 126 | Dockerfile.worker CMD custody | **CONFIRM-LAND** | published image entrypoint without owner | CI builds worker at `$sha` (ci.yml:178-179) and CD pushes it; CMD decides what every Job runs. |
| 127 | Root Dockerfile bare `CMD ["ds-pipeline"]` | **CONFIRM-LAND** | default entrypoint of the unbuilt deployable image | `Dockerfile:19` verified. Cross-ref: the IMAGE is 136's lane (CI-BUILD-BROADWAY-LATEST); THIS gate owns CMD semantics so building and meaning don't land as one tangled change. |
| 128 | compose mlflow/postgres command custody | **CONFIRM-LAND** | definition-without-runtime stack rots independently | `build: ./docker/mlflow` (:3) / `./docker/postgres` (:16) + named volumes (:26) verified; census observed the stack NEVER materialized. Single file, single compose project ⇒ single-writer artifact, no split. D#17's broken-context fact rides as FINDING. |
| 129 | experiments/** `__main__` lane (~60 scripts) | **AMEND** | split-owner needs named sublanes BEFORE ids attach | Split-owner disposition ratified, but a gate whose owner is "~60 runnable scripts" violates precise-owners. AMENDED: three named sublanes (teaching-surface / model-battle / optuna-worker) with exact path prefixes enumerated at attach time; resolution #6 forces the intentional-record-free classification to be written down per sublane. |
| 130 | api-deployment image/command coherence (+GH1-07 rider) | **OVERRULE→DEFER** → DEPLOY-F1-API-LANE | duplicate custody rejected (packet-C #27 precedent) | Confirms slate MERGE. F1 already owns the stub-app/target/env contract (D#8/#9); a second gate on the same manifest splits one defect surface. Id **burned**; rider rides F1. |
| 131 | api HPA bounds custody | **CONFIRM-LAND** | post-D#14 the manifest is the declared SSOT and SSOTs get custody | `minReplicas: 2 / maxReplicas: 10 / averageUtilization: 70` (:48-56) verified. D#14 killed the dead config key and crowned the manifest; leaving the newly-crowned SSOT unguarded would repeat the two-owner defect in mirror form. |
| 132 | train-job Job template custody (+GH1-07 rider) | **OVERRULE→DEFER** → DEPLOY-F2-TRAINJOB-LANE | duplicate custody rejected | Confirms slate MERGE. F2 owns env/secret/volumes/PVC for the same template (D#10/#18). Id **burned**. |
| 133 | postgres StatefulSet custody | **CONFIRM-LAND** | durable-state workload unowned | `kind: StatefulSet` (:2) verified; top-level manifests sit OUTSIDE kubeconform scope (see 145-rider note under DRIFT/arithmetic) so nothing even parses it today. |
| 134 | ABSENT-OBJECT manifest gate | **OVERRULE→DEFER** → K8S-CONFIGMAP-ENV lane | carried at title level only; concrete instance already owned | Flagged in slate §5 as not re-derived. The one verified instance (ConfigMap demanded by configMapKeyRef, no declaring manifest) is D#11's lane including the kubeconform-scope expansion rider that would catch the whole CLASS. Minting a standalone gate from a title would mint a gate nobody can pin transforms to. Id **burned**; generalize inside that lane. |
| 135 | mlflow-deployment name mismatch (`broadway-mlflow` demanded, CI builds `mlflow-server`) | **OVERRULE→DEFER** → IMAGE-TAG-COHERENCE | same fact already routed (#15) | Demand/builder mismatch re-verified (k8s/mlflow-deployment.yaml:17 `broadway-mlflow:latest`; ci.yml builds `-f k8s/optuna/Dockerfile.mlflow` → `mlflow-server:$sha`). Lane, not a second gate. Id **burned**. |
| 136 | Root multi-stage builder ↔ unbuilt `broadway` image | **OVERRULE→DEFER** → CI-BUILD-BROADWAY-LATEST | same fact already routed (#19) | Merge GH1-09≡GH4-07 confirmed: root Dockerfile referenced by NO workflow (only Dockerfile.base/.worker/.mlflow built, ci.yml:164/178/185); `broadway:latest` demanded twice (api-deployment:17, train-job:10). Id **burned**. |
| 137 | compose-stack build/volume law | **AMEND** | compose file is one artifact; law must say what "valid" means | Land, amended: fold D#17 (postgres context lacks Dockerfile) as FINDING root; declare the never-materialized stack "definition-aspiration" until first runtime use; gate owns FILE law (contexts resolve, volumes declared), not runtime objects that do not exist. |
| 138 | conftest snapshot-dir hygiene | **CONFIRM-LAND** | test-infra writes into working tree unowned | `_SNAPSHOT_DIRS = ["artifacts","reports"]` (tests/conftest.py:8) verified. |
| 139 | uv editable-rebuild probe guard formalization | **CONFIRM-LAND** | rare case with an existing suite | tests/test_uv_probe_guard.py present — attach as validated_by; formalization = naming the guard's contract, not writing new machinery. |
| 140 | ci-fixtures k8s-config orchestrator-fixture | **OVERRULE→DEFER** → K8S-CONFIGMAP-ENV lane (fixture-side rider) | confirms slate DEFER | Fixture mirrors the very configmap-env lane it would ride; independent gate would fork the lane's truth. Id **burned**. |
| 141 | Git-tag minting custody (`tier-1-complete`) | **AMEND** | minting-law vacuum over a PUBLISHED object | Evidence CORRECTED (see DRIFT #4): tag is on origin, not local-only; credential rationale INADMISSIBLE (HC-1 void). Grounds that survive: zero scripted minter (repo-wide grep: none), no custody law names tags, yet the tag is published and pins history permanently. Law lands as creation-time law — creator site (manual `git tag -a` + ship.sh push path) persists: annotated tags minted only via a named procedure carrying a ledger row. validated_by: [] + FINDING(census 1.1 + ls-remote correction). |
| 142 | Stale-ref retirement law (pr-1/pr-2) | **AMEND** | gc-immune named refs must have owners or die | Refs verified local-only ✓. Census danger #1 (sp_probe) self-resolved before ruling — recorded, and proof the law's detect-and-retire loop works when someone executes it. Grounds are hygiene-only (closed-unmerged snapshots, ownerless named refs); HC-1 void bars credential framing. Reflog-only mass (367 commits) decays by default policy — accepted-local, out of gate scope. Creation-time law YES (fetch-of-refs/pull/N/head creator pattern persists). |
| 143 | CI cache retention/dedupe law | **AMEND** | only silent global LRU governs CI caches; key saved twice = restore/save asymmetry | Locally verified structure: `actions/cache@v4` restoring `broadway-base-${hashFiles(...)}` tarball (ci.yml:145-149). GitHub-side numbers (19 keys ≈10.39 GiB at LRU ceiling; setup-uv key duplicated ×2) accepted as the census's recorded measurements and honestly marked **NOT re-verifiable under zero-gh-ops**. Law: one save-site per key family (dup proves two paths saved one key), size-budget watch at lane level, purge = invoked workflow step not folklore. No runtime teeth exist GitHub-side beyond workflow edits — the gate is workflow-custody law, stated as such. |
| 144 | GHCR registry-write ownership + version cap | **AMEND** | writer identity defined and serialized; inventory honestly unverifiable; cap attaches to the persistent push site | Verified: CD job `needs:[platform, build-and-boot]`, concurrency-serialized, pushes ONLY `broadway-optuna-worker`+`mlflow-server`, `$sha` always, `:latest` main-only, `:taxi` taxi-only (ci.yml:280-301). Package inventory 403/404 (token lacks read:packages) — recorded, not invented. Ruling: single-writer custody CONFIRMED at the job; version-cap law lands as creation-time law ON THE PUSH SITE (site persists while versions may or may not): mutable-tag writes are append-by-design, capping/pruning requires registry-side operations outside repo reach — declare the cap + the unverifiability; do NOT mint teeth the repo cannot execute. |
| 145 | Container-image retention / teardown zero-footprint law | **AMEND** | gap TRUE AT HEAD; remedy already drafted uncommitted; objects gone ⇒ creation-time law | At HEAD `teardown.sh` contains **zero** rmi/residual lines (verified: grep count 0) — the census premise holds for the registry commit. The worktree WIP already implements the exact right shape: rmi across BOTH vocabularies (CI-minted broadway-base*/broadway-optuna-worker*/mlflow-server* + manifest-demanded broadway*/broadway-mlflow*/broadway-postgres* + judged postgres:16-alpine), volume prune, and a LOUD residual assertion exiting 1 (:63-73). Codify that shape as law: teardown leaves ZERO project containers/images/volumes and FAILS LOUD otherwise; land the WIP through normal tier gates. Objects deleted today (DRIFT #1) ⇒ creator sites persist (ci.yml builds; lifecycle.sh:100 `kind load`; view_local docker run) ⇒ **gate lands as creation-time law: YES**. Rider: `kindest/node:<none>` remnant is machine-local tooling (accepted-local), outside project-vocabulary scope — do not widen the grep pattern to chase it. |
| 146 | Cache-root fork law (ship.sh UV_CACHE_DIR) | **AMEND** | one sanctioned uv cache root; fork site persists at HEAD | HEAD `scripts/ship.sh:10` verified exporting `UV_CACHE_DIR=${UV_CACHE_DIR:-$PWD/.uv-cache}` (plus MPLCONFIGDIR) — the 11.45 G two-root fork's creator site. Worktree WIP already deletes the UV_CACHE_DIR export with doctrine comment (single root `$HOME/.cache/uv`; MPLCONFIGDIR STAYS — matplotlib font-cache determinism, /tmp variant documented at run_local_ci.sh:18). Fork objects already cleaned locally (DRIFT #3) ⇒ **creation-time law: YES** on the export site. Deletion-first satisfied: the fix IS a deletion. |
| 147 | Champion-alias single-writer custody | **CONFIRM-LAND** | the one pointer production resolves, guarded only by convention | Chokepoint is factually singular TODAY: `register_model`/`set_registered_model_alias` exist ONLY at mlflow_utils.py:163-164; sole external caller evaluate/module.py:186. Worker pods hold full tracking reachability (configmap `allowed_hosts: "*"`) and check_champion_manifest.sh declares "the manifest is not a gate". Import-lint forbidding alias-write symbols outside training.mlflow_utils is cheap and codifies a currently-true invariant BEFORE it erodes. Adjacent pin: tests/test_evaluate_contracts.py promotion-ordering suite. |
| 148 | Registered-model-name pin (= cfg.dataset.name) | **CONFIRM-LAND** | namespace fork would silently blind get_champion/list_champions | Evidence correction: census says "= cfg.dataset.name at both call sites"; live grep finds **ONE** live caller (evaluate/module.py:186 fed by dataset_name; setup pairing at :88). Pin is currently true by construction at one site — the gate's entire job is keeping a one-site accident true as sites multiply. |
| 149 | Run-tag closed vocabulary {model, study, seed} | **AMEND** | free-string tags across five sites; but runtime rejection is false precision | Writer scatter verified (hpo.py:81 callback; battle 01:99/:233; worker 03:111-113; plus module/experiments writers per census B1). AMENDED enforcement vehicle: documented closed vocabulary + the SAME import-lint/test net as 147 over the writer sites — NOT runtime rejection, because `mlflow.set_tag` accepts arbitrary keys server-side and wrapping every call to filter them buys machinery, not safety. Say what the vocabulary is; lint who writes it. |
| 150 | Experiment-name pin (two lawful namespaces) | **CONFIRM-LAND** | single auto-create chokepoint; orphans invisible until consumers miss them | `setup_mlflow` → `mlflow.set_experiment(experiment_name)` (mlflow_utils.py:53-63) is the only auto-creation path; refusing anything outside `ratecode1_model_battle` family ∪ dataset-name experiments turns silent orphan-farming into a loud error at the chokepoint. |
| 151 | Artifact-retention/lifecycle law | **AMEND** | append-only by accident across TWO stores; co-tenancy declared, owners line-anchored | Zero deletion calls repo-wide (census B1 verified); `dump_state` appends unbounded `$BACKUP_DIR` tarballs (lifecycle.sh:149-165). AMENDED: ONE gate, justified co-tenancy declared explicitly per house law — (a) registry-prune policy owned at the training plane (non-champion version pruning), (b) snapshot-generation cap owned at lifecycle.sh (keep-N). Retention executes as INVOKED operations with logged outcomes; "accidental append-only" is the defect being outlawed. Deletion-first governs the POLICY (smallest lawful keep-N), not the data. |
| 152 | Run-name convention gate | **AMEND** | determinism-diff and champion manifest consume run identity silently | Conventions verified de-facto: `{study} trial {n}` (hpo.py:81), `optuna_{model}` (03_optuna_worker.py:106), battle names (01:86/:230). AMENDED: gate owns the convention TEXT; enforcement rides the 147/149 lint net — no third mechanism. Consumers of record: check_e2e_determinism.sh, champion manifest. |
| 153 | tracked_run wrapper + object-ledger.jsonl | **OVERRULE→NOT-GATE (as minted) → LAND-LAW-FIRST, CODE-FOLLOWS** | gate owner points at code that DOES NOT EXIST | Direction RATIFIED (human-endorsed); the GATE rejected. As minted it violates precise-owners (every one of the 99 live gates owns a real path:symbol; this owner is "a new wrapper layer"), and resolutions #4/#5 bar building unproduced subsystems whose enforcement nobody consumes — an object-ledger with zero readers is debug provenance wearing a gate costume. RULE: the LAW lands now in band 81 as text — every external-store object creation (MLflow constructors, optuna create_study, git-tag mints, cache-root forks) must route through a NAMED chokepoint, and a created object earns exactly one append-only ledger row — while id 153 **burns**; when tracked_run exists with real owners + a named ledger consumer, re-mint at ≥154 as creation-time law. See Judgment for the one-liner. |
| X1 | TRAIN-120 — MLflow connectivity marker widening | **AMEND** | wrapped-refusal detection keyed on 3 literal substrings | Current net verified: `_CONNECTION_REFUSED_MARKERS = ("Connection refused", "Failed to establish a new connection", "Max retries exceeded")` (mlflow_utils.py:46-50) + `_is_unreachable_http_store` gate + README-hint RuntimeError; pinned by tests/test_mlflow_utils_unreachable.py::test_unreachable_http_store_raises_clear_error (raw ConnectionError + wrapped MlflowException params). AMENDED: widen with NAMED additions (name-resolution failure, connection reset, read timeout) each as a new test param; KEEP the negative path pinned (non-matching MlflowException re-raises raw — over-broad matching would swallow genuine server errors as connectivity). Worker preflight stays its own lane. |
| X2 | CFG-107 — Bootstrap-existence gate for out-of-repo lookup symlink | **AMEND** | dependency on a path OUTSIDE the repo, checked by nobody | Symlink verified live: `data/raw/taxi_zone_lookup.csv -> /home/opc/ONE/learning/data/raw/taxi_zone_lookup.csv` (configs/dataset/taxi.yaml:31-34 declares it). AMENDED: gate lands as a PRE-READ existence assertion at `load_with_audit` — dangling-symlink-aware (Path.exists() follows links) error NAMING the bootstrap step — not as a yaml-side gate; co-tenancy with ETL-11/INGEST-02 on the same symbol declared with a line-anchored aspect (pre-read admission check), per O5 precedent. Owner string moves from the config path to the checking site. Complements D#7's README bootstrap edit (docs side). |

## SLOT / BAND ARITHMETIC (adjudicated against gates.yaml meta, independently recomputed)

- **Corpus density:** extracted all 99 `id:`/`order:` pairs — orders are exactly **1…99, unique, gapless**; band ceilings match slate §0 precisely: INGEST-01..09 · ETL-10..19 · FEAT-20..29 · TRAIN-30..39 · STATS-40..49 · TLINE-50..59 · SURF-60..69 · CFG-70..79 · HPO-80..89 · INFRA-90..99. **Zero free slots in bands 01–09 + 80–89 — slate's exhaustion claim VERIFIED.**
- **Meta self-consistency:** `counts.total: 99` ✓; `by_band.gates` sum 9+10×9 = **99** ✓; `flagged` sum = **67** = `gates_with_findings` ✓; `finding_strings` sum = **91** ✓; `source_fragments` = 10 (01–09 + 80) ✓.
- **Appended region:** 54 proposed ids = exactly 100…153 with no gaps or duplicates in the slate's map (SURF 100-102 · CFG 103-112 · STATS 113 · TLINE 114 · ETL 115-117 · TRAIN 118-120 · INFRA 121-146 · CUST 147-153; band tallies sum 54 ✓). **Ids 100–153 SATISFY the dense-order invariants**: global uniqueness held, no slot reused from exhausted ranges, continuation begins at 100, future mints append ≥154.
- **Band number "81": STANDS AS MINTED — with one mandatory semantic declaration.** Fragment numbering has TWO historical regimes: 01–09 are ordinal positions (their id ranges drift upward), while 80-hpo-optuna is decade-aligned (number = first id, 80–89). Band 81 holding ids 147–153 follows NEITHER alone; left undeclared, the 80/81 adjacency falsely implies decade contiguity (81 ⇒ ids starting at 81 — impossible, exhausted). REQUIRED one-line band-header law: *"From 81 onward, fragment numbers are ordinal band identifiers decoupled from id decades; 80-hpo-optuna is the last decade-aligned band."* With that declaration the mint is legal, lexically sorts clean (01–09 < 80 < 81), and collides with nothing.
- **Burned slots from THIS packet:** 121, 130, 132, 134, 135, 136, 140, 153 — **burned, never reused** (uniqueness/no-reuse outranks retroactive density in the appended region; next mint ≥154 regardless). Burned ids stay recorded here so the gaps are auditable, not mysterious.

## CREATION-TIME-LAW SUMMARY (objects deleted today)

Every object this packet gates that vanished between census and ruling — six project images, `broadway-view-pg`, repo-root uv/mpl caches, ref `sp_probe` — had a **persistent creator site** (CI build steps, `kind load`, `view_local`'s docker run, ship.sh:10 export, fetch-of-pull-head). Per the recommended rule, all such gates **land as creation-time law: YES** — the law binds the site that mints the next instance, not the corpse of the last one. No gate dies with its object; 145 and 146 additionally inherit worktree-WIP implementations that must simply land through tier gates.

## DEFER / BURNED rows (for board propagation)

130→DEPLOY-F1-API-LANE · 132→DEPLOY-F2-TRAINJOB-LANE · 134+140→K8S-CONFIGMAP-ENV (generalized-absent-object + fixture riders) · 135→IMAGE-TAG-COHERENCE · 136→CI-BUILD-BROADWAY-LATEST · 121 NOT-GATE (delete/classify qq demo) · 153 law-first (re-mint ≥154 on wrapper existence).

**VERDICTS: 13 confirm-land, 14 amend, 2 overrule→not-gate, 6 overrule→defer (35 rows)**

## Judgment

The packet's center of gravity is not any single gate — it is that **half the custody gaps already have correct remedies drafted in the working tree** (teardown rmi law, cache-root deletion) and the arbitration's real function is converting WIP into LAW with loud-failure assertions attached, so hygiene stops depending on whoever remembers to run a script. Kills I would defend first: **153-as-minted** (a gate whose owner is imaginary is self-attestation at the registry level — the exact disease these packets treat), and **134** (a title minted into a gate is a gate nobody can pin a transform to). The most consequential landing is **147**: the champion alias is the single pointer production resolves, it is one import-lint away from being legally single-writer, and the window where codification is cheap is exactly while the writer set is still one site. Assumption audit: GitHub-side cache/GHCR inventories taken from the census, not re-measured (zero-gh-ops honored — flagged inline where relied upon); census "both call sites" for register_model corrected to one live caller; "LOCAL ONLY" tag claim overturned by ls-remote; sp_probe found deleted; docker live-state re-grounded rather than trusted. OPEN QUESTIONS: ① who executes the registry-side version cap (outside repo reach)? ② does the qq demo block die or get classified as teaching surface (resolution #6 owner: bench)?

---
*Ruled strictly against HEAD 5016e93 registry content + live read-only probes this session; the only file created is this one.*
