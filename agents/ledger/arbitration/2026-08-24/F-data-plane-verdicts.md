# PACKET F — DATA/ETL/CONFIG/TIMELINE PLANE · SENIOR ARBITRATION REPORT (verbatim, 2026-08-24)

## Step-0 echo

- `git rev-parse --short HEAD` → **5016e93** = dispatch stamp ✅ (full: `5016e937e6aa67b301e3b54b78b1891f85784c67`). Zero gh ops. Read-only except this file.
- Tree: 48 dirty/untracked entries. In-packet-relevant: `M project/etl/process.py`, `M src/broadway/cleaning/structural.py`, `M tests/test_structural_cleaning.py`, `M tests/test_lookup_value_audit.py`, untracked `project/tests/test_ingest_{audit,surface}.py`, **untracked `tests/test_gate_registry.py`**, **untracked `agents/ledger/gates.yaml`** (the SSOT itself is working-tree-only — matches packet E's step-0 note).
- **Plane finding (governs several rulings below):** although `gates.yaml meta.head = 5016e93`, multiple data-plane owner/transform anchors in the registry and slate match the **WIP working tree, not HEAD**. Verified exemplars: (a) `select_and_clean_columns` is at **HEAD :90** with **no StageLedger**; the registry's `:98` def-anchor and slate's `:110-119` ledger-append anchors exist only in the modified worktree; (b) `parse_numeric`'s collector-append is **HEAD :83-90** vs slate's `:107-115` (worktree). Ruling vocabulary below maps onto the contract set as: CONFIRM-LAND→LAND · AMEND→LAND-with-mandatory-corrections · OVERRULE→NOT-GATE→REJECT-as-NOT-GATE · (no DEFER required this packet).

## §1 · TWO-OWNER ARBITRATION (in-scope groups; evidence re-derived from source, pasted)

Ruled: 11 of 19 groups touch F-paths (`project/`, `src/broadway/{data,etl,features,config,samples}`, `configs/**`) or timeline/lineage. Out-of-packet: O2/O3 (cleaning/, contracts/), O7/O8 (stats/), N1/W5/W6/W7 (training/hpo/eval) → routed to their plane seniors; courtesy anchor-drift warning for O2 in §4.

| # | Group | VERDICT | Reason + own evidence |
|---|---|---|---|
| 1 | O1 `project/etl/process.py::select_and_clean_columns` — INGEST-04 + ETL-17 **PRECISE-SPLIT** | **AMEND** | Split direction (projection/drop semantics vs accounting grammar) is right; **execution as written blesses WIP-only code**. At HEAD: `def select_and_clean_columns(df, contract)` at **:90**; body ends `df = df[cols].dropna()` :98 — **no `ledger` param, no `ledger.append`, no `drop_duplicates`**. Slate's "ledger appended ('dropna') at :114 … ('duplicates') at :119" exists only in `M project/etl/process.py` worktree (`if ledger is not None: ledger.append(("dropna", len(df)))` :113-114, `("duplicates", len(df))` :118-119). Amendment: (a) INGEST-04 owner stays symbol-level `process.py select_and_clean_columns()` projection/missing/extra/dropna aspects — **strip all numeric anchors** until R2 lands; (b) ETL-17's narrowed "ledger-append sites only" owner **may not land before the StageLedger batch merges** (bind to DP-A-07/R2); until then the collision stays flagged, not resolved. Registry's `process.py:98` def-anchor is likewise WIP-relative (HEAD def = :90). |
| 2 | O4 `src/broadway/data/loader.py::load_with_audit` — INGEST-02 + ETL-11 **PRECISE-SPLIT** | **CONFIRM-LAND** | Anchors HEAD-valid. Admission leg: `READERS = {".csv"… ".xls"}` :18-23; `if ext not in READERS: raise ValueError(f"unsupported format: {ext}")` :127-128; single `df = READERS[ext](path)` :129. Join leg: `audit = audit_join(df, col, lookup, lookup_df)` :135 **before** `df = df.merge(lookup_df, …, how=MERGE_HOW, suffixes=("", _LOOKUP_SUFFIX))` :139; `merged_lookup_column_names(...)` consulted at :137 pre-merge. Raw-read admission vs lookup-join semantics cleanly disjoint. |
| 3 | O5 `src/broadway/etl/module.py::run` (3-way) — INGEST-03 + INGEST-07 + ETL-10 **CO-TENANCY-LEGAL** | **CONFIRM-LAND** | All anchors HEAD-valid in a clean tracked file: orchestrator `def run(cfg)` :61 + `_assert_data_source_supported(cfg)` :67 + `enforce_drop_fraction(audit, …)` :127; CI branch `if cfg.etl.ci_sample_size > 0 and os.getenv("CI") == "true":` :74 → `df.sample(n=min(...), random_state=rs)` :76 → `reasons.append(f"CI sampling: -{n_before - len(df)} rows")` :78; evidence surfacing `result = StructuralCleanResult(...)` :129-134 → `{dataset.name}_clean.json` write :135-137. Three genuine aspects of one function; line-anchored strings legal here because module.py is committed-clean. Teeth rider: INGEST-03's validated_by = one test in WIP-M `tests/test_structural_cleaning.py` + one in **untracked** `project/tests/test_ingest_audit.py`; both must land tracked with the batch or the teeth are self-attestation. |
| 4 | O6 `src/broadway/features/pipeline.py::FeaturePipeline` — FEAT-23 + FEAT-24 **PRECISE-SPLIT (string repair)** | **CONFIRM-LAND** | Registry strings are **already method-level**: FEAT-23 `pipeline.py:21 FeaturePipeline.fit()` (verified `def fit(self, df, target, smoothing)` :21; encodings-config-order loop :24-31); FEAT-24 `pipeline.py:34 FeaturePipeline.transform()` (verified `def transform(...)` :34; copy→build_derived→freq→target order :35-52). Residual repair is scanner-side only (collision key matches `path::ClassName`); no owner-text change needed beyond keeping method anchors. |
| 5 | O9 `src/broadway/timeline/walkthrough.py::_write_timeline` — SURF-60 + SURF-61 **PRECISE-SPLIT (string repair)** | **CONFIRM-LAND** | Both render legs verified: `paths.TIMELINE_PATH.write_text(timeline_md, …)` :85 (timeline leg) and `paths.INDEX_PATH.write_text(render_dashboard(analysis.name, …))` :87-90 (dashboard leg). Render targets exist: `src/broadway/reports/index.py:61 def render_dashboard(`, `src/broadway/reports/timeline.py:16 def render_timeline(`. Cosmetic amendment optional: registry comments say `reports/…` — full repo path is `src/broadway/reports/…`. |
| 6 | W1 ARTIFACT-CANONICAL-FRAME — INGEST-05+06+08+09 **CO-TENANCY-LEGAL (benign)** | **CONFIRM-LAND** | Sequential refinement chain verified: `cleaner.py:3-4` docstring locks the order ("``canonicalize`` performs the locked structural-cleaning order for the etl step: duplicates → missing-encoding normalization → datetime parsing → target-null drop."), `canonicalize(` at cleaner.py:34; INGEST-05/06 primitives in structural.py; schema.validate consumed at etl/module.py:95. Single-threaded hand-off, no competing writers. Accuracy note: the slate's restatement inserts `parse_numeric` into the "LOCKED ORDER (docstring :3-4)" quote — the docstring's four-stage list does **not** name parse_numeric; quote the docstring verbatim when landing. |
| 7 | W2 ARTIFACT-CANONICAL-PARQUET — ETL-10 + ETL-16 **CO-TENANCY-LEGAL (benign)** | **CONFIRM-LAND** | One physical write site, two lawful aspects: `canonical_path_ = canonical_path(dataset, cfg.environment)` :97 → `df.to_parquet(canonical_path_, index=False)` :98 (module.py, clean); location law `Path(environment.data_dir) / environment.processed_subdir / f"{dataset.name}_canonical.parquet"` at loader.py:112-117. Writer-vs-location-law split documented, gates unsplit — correct. |
| 8 | W3 ARTIFACT-CLEAN-EVIDENCE — INGEST-05+06+07+08 **CO-TENANCY-LEGAL (benign)** | **CONFIRM-LAND** | Producer-collector-persister chain verified: failures built in structural.py primitives; collected by `canonicalize` (cleaner.py:34); persisted `result_path = out_dir / f"{dataset.name}_clean.json"` → `result_path.write_text(result.model_dump_json(indent=2)…)` module.py:135-136. Append-only accumulation; no competing writers. |
| 9 | W4 ARTIFACT-COERCION-AUDIT — ETL-15 + INGEST-06 + INGEST-07 **CO-TENANCY-LEGAL (benign)** | **AMEND** | Disposition stands (same triad shape as W3); **evidence anchors are worktree-only**. Slate cites collector append ":107-115" — at **HEAD that block is :83-90** (`coercions.append(CoercionRecord(column=…, rows_affected=len(coerced)))`), shifted by WIP edits above it; `pd.to_numeric(errors="coerce")` likewise moves off :89. Persistence channel verified clean: `coercion_audit_path = out_dir / f"{dataset.name}_coercion_audit.json"` etl/module.py:146-147 (+ lineage record :154). Amendment: re-anchor all structural.py citations to HEAD or de-line them to symbol level until the WIP batch merges. |
| 10 | W8 ARTIFACT-PIPELINE-PICKLE — FEAT-23 + FEAT-25 **CO-TENANCY-LEGAL (state-owner vs serializer)** | **CONFIRM-LAND** | Single dump site verified in clean file: `pipeline_path = out_dir / cfg.features.pipeline_file` :52 → `with open(pipeline_path, "wb") as f: pickle.dump(pipeline, f)` :53-54 (features/module.py); fitted state produced by `FeaturePipeline.fit` (pipeline.py:21-32). One serializer, one state-owner. |
| 11 | W9 ARTIFACT-RAW-FRAME — INGEST-02 + INGEST-03 **CO-TENANCY-LEGAL (benign)** | **CONFIRM-LAND** | Loader produces (`READERS[ext](path)` loader.py:129 — single read of dataset.path); CI-sample guard immediately mutates row count (module.py:74-78, quoted under #3). Sequential same-pipeline mutation; folds under O5 co-tenancy. Same teeth rider as #3 (INGEST-03 validated_by mixes WIP-M + untracked tests). |

## §2 · SLATE ITEM RULINGS (in-scope §2 ids)

| # | Item (quoted slate line) | VERDICT | Reason / amendment diff |
|---|---|---|---|
| 12 | **GATE-CFG-103** ← PROPOSE-GH1-01 · "STEP_MODULES dispatch-map custody … owner `src/broadway/config/loader.py STEP_MODULES (composite)` … validated_by: [tests/test_gate_registry.py:691 …] RULING: LAND" | **AMEND** | Owner symbol verified: `STEP_MODULES = {` loader.py:**:50**; split vs CFG-78 (`resolve_full_steps()` :155) is real — distinct symbols, dispatch map vs merge law. Two mandatory corrections: (a) **landing flips an existing tripwire by design** — untracked `tests/test_gate_registry.py:689-695` pins `STEP_MODULES` as "documented UNOWNED surface … If a future gate does, this pin fails on purpose": CFG-103's landing batch MUST rewrite that test into an ownership assertion or CI goes red; (b) cited validated_by lives in an **untracked** file — land the test tracked in the same batch (house law: no self-attestation teeth). Add line anchor `:50` to the owner string; drop the "(composite)" hedge. |
| 13 | **GATE-CFG-104** ← PROPOSE-GH3-02 · "Single configs-root owner; kill the project-plane split-brain · owner `project/data.py` configs resolution sites … RULING: LAND" | **AMEND** | Split-brain verified and worse than stated: project/data.py resolves **seven** configs by CWD-relative literals `_DATASET_YAML = Path("configs/dataset/taxi.yaml")` :31 … `_FEATURES_YAML = Path("configs/step/features.yaml")` :46-47, while the platform dialect is env-aware `CONFIGS_DIR = Path(os.getenv("BROADWAY_CONFIGS_DIR") or "configs")` (config/loader.py:34) — which project/etl/process_config.py:6,10 already imports. Amendment: owner string → `project/data.py:31-47 configs-root resolution block`; record coexistence with TRAIN-32 (`project/data.py:166 read_training_sample()`, symbol-disjoint) as file-shared/symbol-disjoint co-tenancy so the OVERLAP-PATH flag closes; validated_by `[]` acceptable with this FINDING as root. |
| 14 | **GATE-CFG-105** (onboard init scaffolder) | *out-of-packet* | Owner `src/broadway/onboard/module.py` — outside F paths; routed to platform-wiring/config senior with no objection recorded. |
| 15 | **GATE-CFG-106** (contracts-step parity → MERGE-WITH-CFG-75) | *out-of-packet* | Owner `src/broadway/contracts/module.py` — outside F paths; MERGE-WITH-GATE-CFG-75 not opposed from the data side (CFG-75 owns pandera dtype-vocabulary law; contracts/run checks are a distinct surface). |
| 16 | **GATE-CFG-107** ← PROPOSE-GH3-01 · "Bootstrap-existence gate for out-of-repo lookup symlink · owner `configs/dataset/taxi.yaml lookup_tables path` … RULING: LAND" | **CONFIRM-LAND** | Artifact owner verified: `configs/dataset/taxi.yaml:31 lookup_tables:` → `pickup_location_id.path: data/raw/taxi_zone_lookup.csv` :32-34 (+ sentinel policy on Borough :36-37). Declared-lookup-existence custody is real (load_with_audit reads lookup csvs at loader.py:134); artifact-side owner string is precise per artifact-gate convention. |
| 17 | **GATE-CFG-108** (env-parse teeth, onboard/infer.py) | *out-of-packet* (courtesy check) | Owner outside F paths. Courtesy finding for the receiving senior: exactly ONE numeric env-parse site exists at HEAD — `onboard/infer.py:10 _IDENTIFIER_THRESHOLD = float(os.getenv("BROADWAY_IDENTIFIER_THRESHOLD", "0.95"))` (import-time, bare ValueError); the slate's plural "threshold vars"/dedup framing overstates the surface. |
| 18 | **GATE-CFG-109** ← PROPOSE-GH3-04 · "Provenance tooth for the ambient CI switch · owner `module.py` ambient-switch site … RULING: LAND" | **AMEND** | Owner ambiguous as written; resolved by exhaustion: the only ambient-CI read in the runtime is `os.getenv("CI") == "true"` at **src/broadway/etl/module.py:74** — the exact line-site already co-owned by GATE-INGEST-03 (O5). A second standalone gate on the same branch violates single-writer. Amendment: demote CFG-109 from standalone gate to a **rider on GATE-INGEST-03** ("CI-fired sampling must leave provenance in the reasons/audit trail"); owner string `src/broadway/etl/module.py:74 run() CI branch`; delete the free-floating "module.py" citation. |
| 19 | **GATE-CFG-110** ← PROPOSE-GH3-06 · "One root-owner for the five output-tree env vars · owner `module.py` output-tree var cluster … RULING: LAND" | **OVERRULE→NOT-GATE** | The gated thing does not exist. Repo-wide scan finds exactly two `BROADWAY_*` env reads — `BROADWAY_CONFIGS_DIR` (config/loader.py:34) and `BROADWAY_ARTIFACTS_DIR` (onboard/module.py:33) — plus string-valued `DATA_MODE` (project/data.py:77). There is no five-var output-tree cluster at HEAD; output roots come from `EnvironmentConfig` fields, not env. A gate whose owner is a hypothetical variable cluster gates nothing (speculative-gate / deletion-first violation). Re-propose only if/when the env overrides actually land, with the real site list; the two existing reads can ride CFG-104's resolution-dialect law meanwhile. |
| 20 | **GATE-CFG-111** (BROADWAY_MLFLOW_CONFIG dialect) | *out-of-packet* | Owner `training/mlflow_utils.py` — model-plane; routed (note: `BROADWAY_MLFLOW_CONFIG` did not surface in the F-path env scan above; verify it exists before landing). |
| 21 | **GATE-CFG-112** ← FINDING E2E-D1 · "SampleSpec.column_mapping DIRECTION LAW (logical→source) + fixture fix · owner `src/broadway/samples/loader.py SampleSpec` + `configs/sample/taxi_diagnostic.yaml:5` … FINDING: shipped yaml writes `{Borough: pickup_borough}` (source→logical) … ⇒ remap silently never fires at HEAD · RULING: LAND" | **AMEND** | **The D1 headline is FALSE at HEAD; the gate survives with corrected rationale and owner.** Own evidence: (a) `SampleSpec` lives at **src/broadway/lineage/models.py:37** (`column_mapping: dict[str,str] = {}` :43) — there is no `SampleSpec` and zero `column_mapping` occurrences in `src/broadway/samples/`; (b) consumer law is analysis-name→physical-column, pinned verbatim: `group_col = sample.column_mapping.get(analysis.hypothesis.group_column, …)` stats/module.py:**:42**, `source_group_column = sample.column_mapping.get(group_column, group_column)` stats/describe.py:**:132** and timeline/runners.py:**:100**; (c) shipped analysis declares `group_column: Borough` (configs/analysis/taxi_hypothesis.yaml:13) and the yaml key IS `Borough` (taxi_diagnostic.yaml:5-6) → **`.get('Borough')` HITS**; (d) live parquet probe: `joined_sample_live.parquet` contains `pickup_borough` (200130 non-null) and **no `Borough`** column — the remap fires and resolves correctly. Nothing is silently inverted. Amendments: strike the "fixture fix / inverted yaml" framing; keep the gate as **column-mapping direction-law + load-time existence custody** (nothing anywhere validates that mapped values exist in the sample artifact — the real residual gap; a bad value today surfaces only as a downstream `group column 'X' not found` ValueError, and only if that path runs); owner → `src/broadway/lineage/models.py:37 SampleSpec.column_mapping (+ consumers stats/module.py:42, stats/describe.py:132, timeline/runners.py:100)`; the three cited tests exist and are TRACKED and pin the correct direction (describe test builds `source_{group}` physical columns) — they validate the law, not the retracted finding. Dependency note: §4-ordering item 3 still holds, minus the fixture-fix payload. |
| 22 | **GATE-STATS-113** ← FINDING E2E-D3 (small-group floor) | *out-of-packet* (courtesy check) | Owner `src/broadway/stats/guards.py` — outside F paths; routed to stats senior. Anchors verified incidentally: `def validate_groups(...)` guards.py:**:8** exact; `test_run_describe_flags_imbalance_and_absent_groups` exists at tests/test_walkthrough.py:**:751**. No objection from the sample-consumer side, post-CFG-112 amendment. |
| 23 | **GATE-TLINE-114** ← FINDING E2E-D2 · "Flow↔lineage profile-parent completeness gate · owner `src/broadway/lineage/state.py:5 EXPECTED` + `src/broadway/baseline/module.py:88 parents` … RULING: LAND" | **AMEND** | Substance verified; owner symbol misnamed. `state.py:5` defines **`LINEAGE_STEPS`**, not `EXPECTED`: `"prediction": ["profile","etl","baseline","features","training","evaluation"]`, `"hypothesis": ["profile","etl","baseline","stats"]`, `"causal": ["profile","etl","baseline","causal"]` :5-9 — all three lead with `"profile"` as claimed. Baseline declares the parent regardless of whether profile ran: `parents = ([node_id("analysis",…), node_id("profile", cfg.dataset.name)] if cfg.dataset is not None else […])` baseline/module.py:87-91. Amendment: owner string → `src/broadway/lineage/state.py:5 LINEAGE_STEPS + src/broadway/baseline/module.py:87-91 parents`; validated_by `[]` with FINDING-D2 root conforms to house law. |
| 24 | **GATE-ETL-115** ← PROPOSE-GH2-06 · "Orphan fetch-helper ruling: wire-or-delete download() … owner `src/broadway/data/download.py:16 download()` … validated_by: [tests/test_download.py::test_download_writes_chunks_under_raw_dir] · RULING: LAND" | **CONFIRM-LAND** | Owner exact: `def download(url: str, env: EnvironmentConfig)` download.py:**:16**; URL-basename hazard verified (`filename = Path(unquote(urlparse(url).path)).name or "download"` :17 → stream-write under raw_dir :18-25). Orphan status confirmed: zero non-test callers repo-wide. Wire-or-delete framing satisfies deletion-first; validated_by real AND tracked (tests/test_download.py:57). Gate the ruling, not just the helper: whichever horn is chosen, the other dies. |
| 25 | **GATE-ETL-116** ← PROPOSE-GH1-06 · "generate_samples producer gate (immutable artifact + provenance sha256 pair) · owner `src/broadway/samples/generate.py generate_samples()` … RULING: LAND" | **AMEND** | Purpose verified, symbol misnamed: the producer is `def generate_sample(name: str, samples_dir: Path | None = None) -> Path` generate.py:**:95** (singular; no `generate_samples` exists). Provenance pair exact: `"definition_sha256": _canonical_spec_sha256(spec)` :144 (digest of canonical model dump, :32-39) + `"artifact_sha256": _file_sha256(artifact)` :145. Amendment: owner string → `generate_sample() :95`; validated_by rides the tracked test_sample.py round-trip pins (file verified tracked). |
| 26 | **GATE-ETL-117** ← PROPOSE-GH1-06 family · "Legacy twin config-loader retirement/fork guard · owner `project/etl/process_config.py legacy twin config path` … RULING: LAND" | **CONFIRM-LAND** | Twin verified: module-level loads of `configs/project/taxi.yaml` + `configs/step/etl.yaml` at process_config.py:10-14 exposing legacy globals `raw_dir/processed_dir/processed_file/…` :16-25 — a parallel configs dialect alongside broadway.loader. Module-scope constants ARE the symbol here (no functions in file); owner string accepted. Rider: feeds CFG-104 (#13) — the twin imports `CONFIGS_DIR` from broadway.config.loader while project/data.py hardcodes relative Paths; retirement/fork-guard should cite both dialect facts. |

## §3 · ROUTING TABLE (slate items examined and NOT ruled here — owner path outside F)

SURF-100..102 (inference/api, discover/columns, experiments_ui) · CFG-105,106,108,111 (onboard/, contracts/, training/) · STATS-113 (routed, courtesy-verified #22) · TRAIN-118..120 (baseline/, causal/, training/) · INFRA-121..146 (k8s, docker, workflows, scripts, tests/conftest) · CUST-147..153 (mlflow custody layer) · two-owner groups O2, O3, O7, O8, N1, W5, W6, W7 (cleaning/, contracts/, stats/, training/hpo). None of these appear in packets A–E; coverage must be assigned before the cycle closes.

## §4 · CROSS-PACKET WARNING (anchor drift)

Every registry/slate citation into **`M project/etl/process.py`** and **`M src/broadway/cleaning/structural.py`** is currently worktree-relative (O1, W4 proven; ETL-15/17/18/19 transform texts suspect by extension — e.g. slate's O2 ":89 coerce / :107-115 append" is HEAD :60s/:83-90). Packets ruling those planes must either re-anchor to `git show HEAD:` content or de-line to symbol level until the WIP batch merges; otherwise landed owner strings point at lines that will move again on merge.

## Tally

```
IN-SCOPE ITEMS RULED: 21 (11 two-owner groups §1 + 10 slate ids §2)
CONFIRM-LAND: 12   (§1: O4,O5,O6,O9,W1,W2,W3,W8,W9 · §2: #16 CFG-107, #24 ETL-115, #26 ETL-117)
AMEND:         8   (§1: #1 O1, #9 W4 · §2: #12 CFG-103, #13 CFG-104, #18 CFG-109, #21 CFG-112, #23 TLINE-114, #25 ETL-116)
OVERRULE→NOT-GATE: 1  (#19 CFG-110 — gated surface does not exist at HEAD)
OVERRULE→DEFER: 0
ROUTED OUT-OF-PACKET: 8 slate ids + 8 two-owner groups (§3)
```

## Judgment

The data/ETL/config/timeline plane's slate is fundamentally sound — every two-owner disposition I re-derived from source survived (10 clean confirms, 1 split kept with a landing precondition, 1 benign chain needing re-anchored evidence) — but the cycle has one systemic defect it must not paper over: **the registry's head-stamp is true while part of its content is not of that head.** O1's entire ETL-17 narrowing and W4's evidence text describe uncommitted code; blessing them as-is would mint gates whose owner lines vanish on the next `git checkout`. Hence the packet's only structural amendments are anchoring discipline (O1, W4, plus the §4 warning), two owner-symbol repairs that any careful landing would have caught anyway (TLINE-114 `EXPECTED`→`LINEAGE_STEPS`, ETL-116 `generate_samples`→`generate_sample`), one owner-path repair (CFG-112's `samples/loader.py SampleSpec` → `lineage/models.py:37`), and one kill (CFG-110, a gate over five env vars that do not exist). The most consequential finding is CFG-112: the E2E-D1 "inverted column_mapping, remap never fires" story is disproven at HEAD — analysis says `Borough`, the yaml keys on `Borough`, the parquet physically carries `pickup_borough`, the lookup hits. What legitimately remains is worth having: nobody validates a sample's `column_mapping` values against the artifact at load time, and the direction law lives only in three consumer call-sites. Gate that, and retire the ghost.

## Assumption audit & open questions

**Re-verified beyond brief (≥3):** parquet column probe (decisive for CFG-112); HEAD-vs-worktree diffs for process.py and structural.py; tracking status of every cited test file (`test_gate_registry.py` UNTRACKED, `test_ingest_{audit,surface}.py` UNTRACKED, all other cited tests TRACKED); uniqueness of the `getenv("CI")` site; zero callers of `download()`; absence of any `column_mapping` handling in `samples/`; `BROADWAY_*` env census (2 sites + DATA_MODE); zero gate ids ≥100 pre-existing in gates.yaml (band-map density invariant holds). **Open questions:** (1) who owns CFG-105/106/108/111, STATS-113, TRAIN-118..120, SURF/INFRA/CUST bands — §3 must be claimed before cycle close; (2) should CFG-103's landing flip `test_has_step_modules_stays_a_drift_proof_miss` in the same commit as the gate row (recommended) or via a sanctioned two-step (needs board ruling); (3) does any flow consume `taxi_diagnostic` through `timeline/runners.py:100` in CI, or is the mapping law exercised only by the three pinned tests (affects how loud the new existence validation can be).
