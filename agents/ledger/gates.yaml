# Gate registry SSOT — hand-maintained; the {01..09} gate fragments it consolidated predate git custody.
meta:
  head: 1bce284
  assembled: '2026-08-24'
  source_fragments:
  - agents/ledger/gates/01-ingest.md
  - agents/ledger/gates/02-etl-lookup.md
  - agents/ledger/gates/03-features.md
  - agents/ledger/gates/04-training.md
  - agents/ledger/gates/05-stats.md
  - agents/ledger/gates/06-timeline.md
  - agents/ledger/gates/07-surfaces.md
  - agents/ledger/gates/08-config.md
  - agents/ledger/gates/09-infra.md
  - agents/ledger/gates/80-hpo-optuna.md
  counts:
    total: 142
    gates_with_findings: 85
    finding_strings: 110
    by_band:
      01-ingest:
        gates: 9
        flagged: 9
        finding_strings: 9
      02-etl-lookup:
        gates: 12
        flagged: 7
        finding_strings: 8
      03-features:
        gates: 10
        flagged: 4
        finding_strings: 6
      04-training:
        gates: 13
        flagged: 12
        finding_strings: 14
      05-stats:
        gates: 11
        flagged: 7
        finding_strings: 7
      06-timeline:
        gates: 11
        flagged: 7
        finding_strings: 12
      07-surfaces:
        gates: 13
        flagged: 1
        finding_strings: 1
      08-config:
        gates: 17
        flagged: 12
        finding_strings: 15
      80-hpo-optuna:
        gates: 10
        flagged: 9
        finding_strings: 13
      09-infra:
        gates: 30
        flagged: 17
        finding_strings: 25
      81-object-custody:
        gates: 6
        flagged: 0
        finding_strings: 0
gates:
- id: GATE-INGEST-01
  phase: ingest
  order: 1
  owner: project/etl/process.py:28 get_raw_files()
  inputs:
  - CFG-TAXI-PROJECT
  outputs:
  - ARTIFACT-RAW-PARQUET
  transforms:
  - glob `<raw_dir>/yellow_tripdata_*.parquet`, sorted() for deterministic order (:30)
  - empty result → FileNotFoundError naming raw_dir (:31-32)
  touched_by:
  - pandas@2.3.3
  - polars@1.43.2
  - pandera@0.32.1
  - pyyaml@6.0.2
  validated_by:
  - project/tests/test_process.py::test_process_data_writes_ingest_lineage
  if_changed:
  - ARTIFACT-RAW-PARQUET set/order
  - GATE-INGEST-02
  - GATE-INGEST-03
  - project/tests/test_process.py
  - project/tests/test_ingest_audit.py fixtures
  findings:
  - the FileNotFoundError branch has NO test anywhere — repo-wide grep finds get_raw_files only at its definition and its process_data call site; the sole covering test exercises the happy path with a seeded parquet. The `yellow_tripdata_*.parquet` prefix is hardcoded, so no contract-declared dataset can ever route through this discovery gate.
- id: GATE-INGEST-02
  phase: ingest
  order: 2
  owner: src/broadway/data/loader.py:124 load_with_audit()
  inputs:
  - CFG-DATASET-CONTRACT
  - ARTIFACT-RAW-PARQUET
  outputs:
  - ARTIFACT-RAW-FRAME
  transforms:
  - extension whitelist READERS {.csv,.parquet,.xlsx,.xls}, else ValueError 'unsupported format' (:18-23, :126-128)
  - single pd.read_* of dataset.path (:129)
  - per-lookup audit_join then merged_lookup_column_names '_lookup' suffix rule (:132-137)
  - '_assert_unique_merged_labels: pd.Index(...).is_unique over PRODUCED label list (phantom key excluded), raises pandera SchemaError BEFORE the left merge (:46-109, call :138)'
  touched_by:
  - pandas@2.3.3
  - polars@1.43.2
  - pandera@0.32.1
  validated_by:
  - tests/test_join_audit.py::test_load_with_audit_returns_df_and_audits
  - tests/test_boundary_contracts.py::test_joined_loader_rejects_pre_existing_lookup_suffix_collision
  - tests/test_boundary_contracts.py::test_joined_loader_duplicate_label_error_names_all_collisions
  - tests/test_recipe.py::test_joined_schema_matches_loader_output
  if_changed:
  - ARTIFACT-RAW-FRAME shape/columns
  - GATE-INGEST-03..09
  - tests/test_lookup_value_audit.py
  - tests/test_join_audit.py
  - features joined-schema pin
  findings:
  - 'the unsupported-extension ValueError branch (loader.py:127-128) has zero test coverage — grep for ''unsupported format'' in tests/ returns nothing. Legacy twin read_raw_data (project/etl/process.py:36) bypasses this whole gate: polars scan_parquet with no extension check, no lookup merges, no label-collision guard.'
- id: GATE-INGEST-03
  phase: ingest
  order: 3
  owner: src/broadway/etl/module.py:74 run() CI-sample guard
  inputs:
  - ARTIFACT-RAW-FRAME
  - CFG-ETL-STEP
  - CFG-TAXI-PROJECT
  outputs:
  - ARTIFACT-RAW-FRAME
  transforms:
  - 'CONTRACT pipeline: CI=true AND etl.ci_sample_size>0 → df.sample(n=min(ci_sample_size, len), random_state=experiment.random_state), reason ''CI sampling: -<N> rows'' (:74-78)'
  - 'CFG-109 RIDER (demoted from standalone gate per packet F #18 — a second standalone gate on this branch would violate single-writer): CI-fired sampling must leave provenance in the reasons/audit trail; the ONLY ambient-CI read in the runtime is THIS branch os.getenv(''CI'')==''true'' (:74)'
  - 'LEGACY twin sample_for_ci: os.getenv(''CI'')==''true'' AND len(df)>ci_sample_size → df.sample(n=ci_sample_size, random_state=cfg.random_state) (project/etl/process.py:43-48)'
  - LEGACY ledger appends ('ci_sample', rows_out) only when rows shrank, keeping the '-<N> rows' drop grammar (project/etl/process.py:151-153, grammar :21-23)
  touched_by: []
  validated_by:
  - tests/test_structural_cleaning.py::test_etl_ci_sampling_gated
  - project/tests/test_ingest_audit.py::test_ci_sampling_reorder_becomes_visible_in_ledger
  if_changed:
  - row counts of every downstream artifact
  - TransformAudit.reasons grammar consumed by enforce_drop_fraction (src/broadway/lineage/records.py:43)
  - reproducibility of splits
  findings:
  - 'teeth-status rider (packet F O5/W9 CONFIRM-LAND condition): one pin lives in WIP-M tests/test_structural_cleaning.py and one in untracked project/tests/test_ingest_audit.py — both must land TRACKED with the StageLedger batch or these teeth are self-attestation.'
- id: GATE-INGEST-04
  phase: ingest
  order: 4
  owner: 'project/etl/process.py: select_and_clean_columns() projection/missing/extra/dropna aspects (numeric anchors stripped pending DP-A07/R2 StageLedger merge)'
  inputs:
  - CFG-DATASET-CONTRACT
  - CFG-TAXI-PROJECT
  - ARTIFACT-RAW-FRAME
  outputs:
  - ARTIFACT-SELECTED-FRAME
  transforms:
  - rename_columns applies cfg.rename_map (project/etl/process.py:87)
  - missing contract column(s) → ValueError listing sorted(missing) (:93-94)
  - extra columns logged and silently dropped (:95-97)
  - df[cols].dropna() with before/after count log (:98-100) — NO ledger append, NO dedup at HEAD 5016e93
  - 'accounting grammar over these same lines is co-owned by GATE-ETL-17 (PRECISE-SPLIT per arbitration O1); ledger/dedup halves bind to DP-A07/R2'
  touched_by:
  - pandas@2.3.3
  - polars@1.43.2
  - pandera@0.32.1
  - pyyaml@6.0.2
  validated_by:
  - project/tests/test_process.py::test_rename_columns
  - project/tests/test_process.py::test_select_and_clean_columns
  - project/tests/test_ingest_contract.py::test_missing_contract_column_raises
  - project/tests/test_ingest_contract.py::test_extra_column_not_in_contract_is_dropped_not_error
  - project/tests/test_ingest_surface.py::test_select_and_clean_columns_nan_twins_die_at_dropna_not_dedup
  if_changed:
  - contract column set seen by every later step
  - GATE-INGEST-05..09 inputs
  - project/tests/test_ingest_*.py
  findings:
  - this gate duplicates drop-semantics that the CONTRACT pipeline re-implements inside canonicalize (dedup + null drops) with a DIFFERENT order (here dropna→dedup; there dedup→…→target-null-only) — NaN-twins accounting differs by construction (see project/tests/test_ingest_surface.py::test_select_and_clean_columns_nan_twins_die_at_dropna_not_dedup). rename_map keys are defined in configs/project/taxi.yaml (contents UNVERIFIED — not opened) and reach the code only via process_config.py:27, outside DatasetContract; drift is caught only reactively by the missing-column ValueError.
- id: GATE-INGEST-05
  phase: ingest
  order: 5
  owner: src/broadway/cleaning/structural.py:49 parse_datetime()
  inputs:
  - CFG-DATASET-CONTRACT
  - ARTIFACT-RAW-FRAME
  outputs:
  - ARTIFACT-CANONICAL-FRAME
  - ARTIFACT-CLEAN-EVIDENCE
  transforms:
  - pd.to_datetime(series, errors='coerce') (:50)
  - failed mask = input.notna() & coerced.isna() (:51)
  - any failure → ParseFailure(column, count, examples[:5], target_dtype='datetime') (:53-60)
  - column selection via datetime_columns(contract) selector (src/broadway/contracts/selectors.py:9, applied in cleaner.py:59-63)
  touched_by:
  - pandas@2.3.3
  - pydantic@2.11.7
  validated_by:
  - tests/test_structural_cleaning.py::test_parse_datetime_records_failures
  - tests/test_structural_cleaning.py::test_parse_datetime_no_drop
  if_changed:
  - datetime64 columns of ARTIFACT-CANONICAL-FRAME
  - GATE-INGEST-07 evidence volume
  - GATE-INGEST-09 outcome for datetime columns
  findings:
  - unparseable values become NaT and are only RECORDED, never raised (see GATE-INGEST-07); because build_raw_schema declares every column nullable=True (src/broadway/contracts/pandera.py:57), NaT-filled non-target datetime rows PASS the dtype gate silently and survive into canonical parquet — only a null TARGET dies later at cleaner.py:71-75. LEGACY pipeline never calls parse_datetime at all (tpep_* parsed by parquet reader alone, filtered at process.py:51-60).
- id: GATE-INGEST-06
  phase: ingest
  order: 6
  owner: src/broadway/cleaning/structural.py:83 parse_numeric()
  inputs:
  - CFG-DATASET-CONTRACT
  - ARTIFACT-RAW-FRAME
  outputs:
  - ARTIFACT-CANONICAL-FRAME
  - ARTIFACT-COERCION-AUDIT
  - ARTIFACT-CLEAN-EVIDENCE
  transforms:
  - pd.to_numeric(series, errors='coerce') (:67)
  - unparseable-notna → ParseFailure(examples[:5]) (:68-77)
  - 'int8/16/32/64 target AND zero NaN AND arriving dtype ≠ target → plain astype(target_dtype) with CoercionRecord appended to the caller-supplied collector (:78-90); the _fractional_refusal FX-A05 guard (slate :100-105 + helper :64-80) exists ONLY in the pending structural.py WIP batch — de-lined until it merges (packet F W4/§4)'
  - non-int targets and no-dtype-drift int columns return coerced unchanged at HEAD (:91)
  touched_by:
  - pandas@2.3.3
  - pydantic@2.11.7
  validated_by:
  - tests/test_structural_cleaning.py::test_parse_numeric_records_failures
  - tests/test_structural_cleaning.py::test_parse_numeric_clean_int_stays_int
  - tests/test_structural_cleaning.py::test_parse_numeric_failure_stays_float
  - tests/test_structural_cleaning.py::test_parse_numeric_fractional_records_failure_and_stays_float
  - tests/test_structural_cleaning.py::test_parse_numeric_negative_fraction_refuses_toward_zero
  - tests/test_structural_cleaning.py::test_parse_numeric_infinity_records_failure_instead_of_raising
  - tests/test_structural_cleaning.py::test_parse_numeric_huge_float_precision_loss_refused
  if_changed:
  - numeric dtypes of ARTIFACT-CANONICAL-FRAME
  - GATE-INGEST-09 SchemaError surface
  - ARTIFACT-COERCION-AUDIT consumers
  - lineage node coercion:<dataset>
  findings:
  - 'FX-A05 fractional-refusal branch is WIP-ONLY at HEAD 5016e93 — the HEAD astype-back has NO refusal (any int-target column with zero NaN gets plain astype :78-81). When the batch merges, its known caveat applies: reachable ONLY when the column has no NaN, so a column mixing unparseable strings AND fractional floats records just the unparseable ParseFailure and skips the refusal via the NaN condition — FX-A05 protection degrades to "float64 survives to the dtype gate". LEGACY pipeline imports neither parse_numeric nor any structural primitive (repo grep: zero callers in project/) — its dtype correctness rests entirely on parquet-read dtypes plus GATE-INGEST-09''s legacy twin.'
- id: GATE-INGEST-07
  phase: ingest
  order: 7
  owner: src/broadway/etl/module.py:129 run() evidence surfacing
  inputs:
  - ARTIFACT-CLEAN-EVIDENCE
  - ARTIFACT-COERCION-AUDIT
  outputs:
  - ARTIFACT-CLEAN-EVIDENCE
  - ARTIFACT-COERCION-AUDIT
  transforms:
  - ParseFailure is a pydantic extra=forbid model {column, count, examples, target_dtype} (src/broadway/cleaning/models.py:8-14)
  - failures collected from parse_datetime/parse_numeric inside canonicalize (src/broadway/data/cleaner.py:44, :59-69)
  - persisted as StructuralCleanResult.parse_failures in <dataset>_clean.json (etl/module.py:129-137)
  - coercions persisted as CoercionAuditReport in <dataset>_coercion_audit.json + lineage record node coercion:<dataset> (etl/module.py:145-156)
  - rendered downstream by reports/audit.py render_transform (src/broadway/reports/audit.py:278; loaded at :669/:688)
  touched_by:
  - pydantic@2.11.7
  validated_by:
  - tests/test_structural_cleaning.py::test_etl_module_writes_canonical_and_result
  - tests/test_boundary_contracts.py::test_gap_raw_boundary_int_float_coercion_mask
  if_changed:
  - <dataset>_clean.json / <dataset>_coercion_audit.json schemas
  - reports/audit.py rendering
  - trust/evidence surfaces reading those files
  findings:
  - 'GUARANTEE NOBODY CHECKS — the ParseFailure channel is recorded-not-enforced: nothing in the repo raises on a non-empty parse_failures list; loud failure is delegated entirely to GATE-INGEST-09''s dtype mismatch, which for nullable=True columns masks datetime NaT (and any float-typed int column only via strict dtype comparison). A run with thousands of unparseable timestamps still exits green with evidence written to JSON that no gate reads back.'
- id: GATE-INGEST-08
  phase: ingest
  order: 8
  owner: src/broadway/data/cleaner.py:34 canonicalize()
  inputs:
  - CFG-DATASET-CONTRACT
  - CFG-ETL-STEP
  - ARTIFACT-RAW-FRAME
  outputs:
  - ARTIFACT-CANONICAL-FRAME
  - ARTIFACT-CLEAN-EVIDENCE
  transforms:
  - 'LOCKED ORDER (docstring :3-4): drop_duplicates → standardize_missing on object/string cols → parse_datetime over datetime_columns → parse_numeric over numeric_map → dropna(subset=[target]) (:47-75)'
  - standardize_missing swaps configured encodings for None and reports observed encodings (src/broadway/cleaning/structural.py:119-127, applied :53-57)
  - 'drop accounting emitted strictly as ''<reason>: -<N> rows'' (:51, :75) so enforce_drop_fraction can parse explained losses'
  - numeric_map built from contract dtypes via numeric_columns selector (src/broadway/contracts/selectors.py:15, wired at etl/module.py:80)
  touched_by:
  - pandas@2.3.3
  validated_by:
  - tests/test_structural_cleaning.py::test_canonicalize_order
  - tests/test_structural_cleaning.py::test_canonicalize_duplicates_before_normalization
  - tests/test_structural_cleaning.py::test_canonicalize_target_null
  - tests/test_structural_cleaning.py::test_canonicalize_coerces_numeric
  - tests/test_structural_cleaning.py::test_standardize_missing_observed_only
  if_changed:
  - row set of ARTIFACT-CANONICAL-FRAME
  - TransformAudit.reasons
  - GATE-INGEST-09 input
  - features/stats/timeline steps consuming canonical parquet
  findings:
  - CHECK WITH NO UPSTREAM GUARANTEE — the locked order binds only the CONTRACT pipeline; the LEGACY pipeline (project/etl/process.py) shares none of this code and enforces its own order (filters→duration→passenger_count→rename→select/dropna/dedup, process.py:145-165). The ONLY shared invariant between the two ingests is the textual '-<N> rows' reason grammar (process.py:21-23 comment), which nothing validates at runtime.
- id: GATE-INGEST-09
  phase: ingest
  order: 9
  owner: src/broadway/contracts/pandera.py:46 build_raw_schema()
  inputs:
  - CFG-DATASET-CONTRACT
  - ARTIFACT-CANONICAL-FRAME
  outputs:
  - ARTIFACT-CANONICAL-FRAME
  transforms:
  - one pa.Column(pandera_dtype(col.dtype), nullable=True) per contract.columns entry — schema derived at runtime, no hand-maintained DataFrameModel (:55-59)
  - dtype whitelist int8-64 / float32-64 / datetime64[us|ns] / object|str|string, everything else maps to pa.Object sentinel (:14-27, :34-43)
  - 'CONTRACT enforcement: build_raw_schema(dataset).validate(df) AFTER coercion, coerce=False strict (etl/module.py:95; B1 tripwire map tests/test_boundary_contracts.py:28-34)'
  - 'LEGACY twin validate_contract_schema: blank-dtype ValueError + pa.Object unsupported-dtype ValueError + schema.validate pre-save (project/etl/process.py:123-129)'
  touched_by:
  - pandera@0.32.1
  - pyyaml@6.0.2
  validated_by:
  - project/tests/test_ingest_contract.py::test_missing_dtype_raises
  - project/tests/test_ingest_contract.py::test_dtype_mismatch_raises
  - tests/test_boundary_contracts.py::test_raw_canonical_boundary_rejects_dtype_mutation
  if_changed:
  - every consumer of the canonical parquet (features B2, stats B6, timeline B7 per tests/test_boundary_contracts.py header)
  - tests/test_boundary_contracts.py
  - tests/test_loud_failures.py expectations
  findings:
  - nullable=True is deliberate ("null_count observed, not an invariant", pandera.py:51-53), so this gate checks NAMES and DTYPES only — null-ratio drift, value ranges and uniqueness are out of contract here. An EMPTY contract.columns dict would build a vacuous schema that validates any frame (pydantic does not require columns to be non-empty; no test covers that case). The legacy blank-dtype/pa.Object ValueErrors are tested, but the legacy validate call itself runs before save_processed_data with no re-validation after write.
- id: GATE-ETL-10
  phase: etl-lookup
  order: 10
  owner: src/broadway/etl/module.py:61 run()
  inputs:
  - PipelineConfig (cfg.dataset + cfg.etl + cfg.experiment)
  - raw dataset at DatasetContract.path via load_with_audit
  outputs:
  - <processed_subdir>/<name>_canonical.parquet (ARTIFACT-CANONICAL-PARQUET)
  - <name>_clean.json (ARTIFACT-STRUCTURAL-CLEAN)
  - audit JSONs (GATE-ETL-13/14/15)
  - lineage records node_id("etl")
  transforms:
  - loader guard _assert_data_source_supported() rejects non raw-ingest loaders (named_sample/pinned) and missing experiment binding
  - 'CI-gated sampling appends ''CI sampling: -N rows'' reason'
  - canonicalize() numeric/datetime/missing repair with coercions collector
  - pre-write contract validation build_raw_schema(dataset).validate(df)
  - TransformAudit assembled; unexplained drops enforced vs EtlStep.max_drop_fraction
  touched_by:
  - logging
  - os
  - re
  - pathlib (stdlib only — NO direct pandas/polars/pydantic import in this file)
  - broadway.cleaning.models
  - broadway.cleaning.structural
  - broadway.config.schema
  - broadway.contracts.pandera
  - broadway.contracts.selectors
  - broadway.data.cleaner
  - broadway.data.join_audit
  - broadway.data.loader
  - broadway.data.lookup_value_audit
  - broadway.data.splitter
  - broadway.lineage.ids
  - broadway.lineage.models
  - broadway.lineage.records
  validated_by:
  - tests/test_structural_cleaning.py::test_etl_module_writes_canonical_and_result
  - tests/test_structural_cleaning.py::test_etl_data_source_loader_drives_step_eligibility
  - tests/test_structural_cleaning.py::test_etl_without_experiment_fails_loud
  - tests/test_structural_cleaning.py::test_etl_ci_sampling_gated
  - tests/test_structural_cleaning.py::test_etl_parent_defaults_to_dataset
  - tests/test_structural_cleaning.py::test_etl_parent_ingest_when_present
  if_changed:
  - EtlStep schema keys
  - step-module registry mapping "etl"→broadway.etl.module (src/broadway/config/loader.py:52)
  - canonical artifact name
  - TransformAudit fields
  findings: []
- id: GATE-ETL-11
  phase: etl-lookup
  order: 11
  owner: src/broadway/data/loader.py:124 load_with_audit()
  inputs:
  - DatasetContract.path (csv/parquet/xlsx via READERS dict keyed on suffix)
  - dataset.lookup_tables CSVs read with keep_default_na=False + authored na_values
  outputs:
  - (df_merged, list[JoinAudit], list[LookupValueAudit]) — left-join frame with lookup columns renamed by _lookup suffix rule
  transforms:
  - format dispatch purely by Path.suffix (READERS, unsupported ext raises ValueError)
  - 'per lookup: audit_join BEFORE merge, then df.merge(left_on=col, right_on=key, how=''left'', suffixes=('''', ''_lookup'')) at :139'
  - merged_lookup_column_names() (:32) is the SSOT of the '_lookup' collision rename, shared with schemas/joined.py:12 so rule cannot drift from joined schema (Decision 6)
  touched_by:
  - collections.Counter
  - collections.abc.Iterable
  - pathlib.Path
  - pandas
  - polars
  - pandera.errors.SchemaError
  - broadway.config.schema
  - broadway.contracts.pandera
  - broadway.data.join_audit
  - broadway.data.lookup_value_audit
  validated_by:
  - tests/test_join_audit.py::test_load_with_audit_returns_df_and_audits
  - tests/test_join_audit.py::test_load_returns_only_df
  - tests/test_recipe.py::test_joined_schema_matches_loader_output
  - tests/test_lookup_value_audit.py::test_keep_default_na_false_preserves_literal_tokens
  - tests/test_lookup_value_audit.py::test_na_values_only_converts_authored_tokens
  if_changed:
  - _LOOKUP_SUFFIX value (:29)
  - MERGE_HOW (:25)
  - READERS keys
  - DatasetContract.lookup_tables shape
  - LookupSpec.key/na_values/value_policies
  findings:
  - NA semantics are ASYMMETRIC across the same merge — main frame read through READERS uses pandas' default NA set while the lookup side restricts conversion to authored tokens (keep_default_na=False, :134); a literal like "NA" becomes NaN on the left but stays a string on the right, so audit_join matching and sentinel counting operate on divergent representations with no warning.
- id: GATE-ETL-12
  phase: etl-lookup
  order: 12
  owner: src/broadway/data/loader.py:46 _assert_unique_merged_labels()
  inputs:
  - DatasetContract
  - pre-merge df.columns
  - merged_names from merged_lookup_column_names()
  - left_key/right_key
  outputs:
  - SchemaError naming every duplicated label + provenance (raises before merge)
  - or silently returns when produced label list is unique
  transforms:
  - list-level pd.Index(...).is_unique multiplicity check on ACTUAL produced labels (FIX_4 G2, closes 2026-08-23 duplicate-label incident class)
  - 'phantom-key exclusion: when left_key == right_key the ''<key>_lookup'' mapping is dropped because pandas emits a single key column'
  touched_by:
  - collections.Counter
  - pandas
  - pandera.errors.SchemaError
  - broadway.contracts.pandera (build_raw_schema)
  validated_by:
  - tests/test_boundary_contracts.py::test_joined_loader_rejects_pre_existing_lookup_suffix_collision
  - tests/test_boundary_contracts.py::test_joined_loader_duplicate_label_error_names_all_collisions
  if_changed:
  - _LOOKUP_SUFFIX
  - phantom-key rule (:76)
  - SchemaError failure_cases payload shape
  findings: []
- id: GATE-ETL-13
  phase: etl-lookup
  order: 13
  owner: src/broadway/data/join_audit.py:27 audit_join()
  inputs:
  - left df
  - left_key column
  - LookupSpec (path/key)
  - lookup_df
  outputs:
  - JoinAudit{rows_attempted, matched, unmatched, null_keys, unmatched_rate}
  transforms:
  - matched counted pre-merge via isin(lookup_df[key]); null keys excluded from BOTH matched and unmatched; unmatched_rate = unmatched / (matched+unmatched) rounded 6dp (0.0 when no non-null keys)
  - 'persistence channel: JoinAuditReport → <processed_subdir>/<name>_join_audit.json (ARTIFACT-JOIN-AUDIT) + lineage node_id(''join'') written at src/broadway/etl/module.py:157-162, gated on join_audits non-empty'
  touched_by:
  - pandas
  - pydantic
  - broadway.config.schema.LookupSpec
  validated_by:
  - tests/test_join_audit.py::test_audit_join_matched_unmatched_null_keys
  - tests/test_join_audit.py::test_audit_join_all_matched
  - tests/test_join_audit.py::test_audit_join_no_non_null_keys
  - tests/test_join_audit.py::test_join_audit_report_round_trip
  - tests/test_structural_cleaning.py::test_etl_with_lookups_writes_join_audit
  if_changed:
  - JoinAudit field set
  - unmatched_rate denominator policy
  - ARTIFACT-JOIN-AUDIT filename
  - node_id('join') kind string
  findings: []
- id: GATE-ETL-14
  phase: etl-lookup
  order: 14
  owner: src/broadway/data/lookup_value_audit.py:33 audit_lookup_values()
  inputs:
  - post-merge df
  - left_key
  - LookupSpec + value_policies sentinels
  - lookup_df
  - merged_names
  - matched count from JoinAudit
  outputs:
  - LookupValueAudit → LookupColumnValueAudit per non-key lookup column {null_count, sentinel_counts, affected_rows, affected_rate, affected_lookup_keys}; persisted as <name>_lookup_value_audit.json (ARTIFACT-LOOKUP-VALUE-AUDIT) + lineage node_id('lookup_value') parented under node_id('join') at src/broadway/etl/module.py:163-167
  transforms:
  - matched_mask = merged right-key notna() restricts accounting to MATCHED rows (unmatched NaNs excluded from null_count)
  - sentinel counts via exact equality against configured values; affected_rate = affected/matched rounded 6dp, forced 0.0 when matched==0
  - affected_lookup_keys enumerated from the RIGHT table where value isna or sentinel
  touched_by:
  - pandas
  - pydantic
  - broadway.config.schema.LookupSpec/LookupValuePolicy
  validated_by:
  - tests/test_lookup_value_audit.py::test_unmatched_rows_excluded_from_null_count
  - tests/test_lookup_value_audit.py::test_null_count_counts_only_matched_null_values
  - tests/test_lookup_value_audit.py::test_sentinel_counts_only_configured_sentinels
  - tests/test_lookup_value_audit.py::test_affected_rate_is_affected_rows_over_matched
  - tests/test_lookup_value_audit.py::test_affected_lookup_keys_come_from_right_key
  if_changed:
  - LookupColumnValueAudit fields
  - affected_rate rounding
  - ARTIFACT-LOOKUP-VALUE-AUDIT filename
  - lineage parenting lookup_value←join
  findings:
  - this channel is EVIDENCE-ONLY — no threshold or gate ever consumes affected_rate/sentinel_counts; a lookup whose every matched value is null or sentinel still passes etl.run silently (module.py writes the report and continues).
- id: GATE-ETL-15
  phase: etl-lookup
  order: 15
  owner: src/broadway/cleaning/structural.py:83 parse_numeric()
  inputs:
  - pd.Series
  - declared target_dtype
  - optional caller-supplied coercions collector (CoercionRecord list threaded from etl.run through canonicalize/data.cleaner — anchors de-lined per packet F §4)
  outputs:
  - '(repaired series, ParseFailure | None); collector append CoercionRecord{column, declared_dtype, arriving_dtype, rows_affected} at :83-90 at HEAD; persisted by etl.run as <name>_coercion_audit.json (ARTIFACT-COERCION-AUDIT) + lineage node_id(''coercion'') at src/broadway/etl/module.py:145-156'
  transforms:
  - pd.to_numeric(errors='coerce') with failures recorded as ParseFailure (never raised) (:67-77)
  - 'int-target astype-back branch at HEAD is a PLAIN astype gated on zero-NaN + dtype-drift (:78-81); the FX-A05 fractional REFUSAL (_fractional_refusal) lives only in the pending structural.py WIP batch — de-lined until merge'
  - 'non-fractional astype-back is not silent — every int-target dtype-drift cast appends the CoercionRecord evidence event (:82-90; FIX_4 Option E grammar)'
  touched_by:
  - pandas
  - pydantic
  - broadway.cleaning.models.ParseFailure
  validated_by:
  - tests/test_structural_cleaning.py::test_parse_numeric_clean_int_stays_int
  - tests/test_structural_cleaning.py::test_canonicalize_coerces_numeric
  - tests/test_structural_cleaning.py::test_parse_numeric_fractional_records_failure_and_stays_float
  - tests/test_structural_cleaning.py::test_parse_numeric_negative_fraction_refuses_toward_zero
  - tests/test_structural_cleaning.py::test_parse_numeric_huge_float_precision_loss_refused
  - tests/test_boundary_contracts.py::test_gap_raw_boundary_int_float_coercion_mask
  if_changed:
  - CoercionRecord field set
  - int-dtype tuple (:78)
  - fractional-refusal policy (WIP-pending)
  - ARTIFACT-COERCION-AUDIT filename
  - node_id('coercion') upstream wiring
  findings:
  - coercion evidence is PARTIAL by construction — only int-target astype-backs are recorded; float-target arriving-dtype drift and parse_datetime's errors="coerce" (:50) emit NO CoercionRecord, and rows_affected is len(coerced) i.e. the whole column even when zero values actually changed. Additionally the module.py:145-156 persistence branch has no test asserting the coercion_audit.json artifact (see UNVERIFIED).
- id: GATE-ETL-16
  phase: etl-lookup
  order: 16
  owner: src/broadway/data/loader.py:112 canonical_path()
  inputs:
  - EnvironmentConfig.data_dir + EnvironmentConfig.processed_subdir (CFG-ENV-PROCESSED-SUBDIR)
  - dataset.name; sibling writes keyed off EtlStep.train_file/val_file/training_data_file (CFG-ETL-SPLIT-FILES) at src/broadway/etl/module.py:104-109
  outputs:
  - <data_dir>/<processed_subdir>/<dataset.name>_canonical.parquet (ARTIFACT-CANONICAL-PARQUET)
  - written index=False at module.py:98; plus split pair train/val or flat training_data parquet; out_dir mkdir(parents=True, exist_ok=True)
  transforms:
  - 'PRE-WRITE contract validation only: build_raw_schema(dataset).validate(df) at module.py:95 runs on the IN-MEMORY frame before to_parquet'
  - row-count log line after write; StructuralCleanResult.canonical_path records str(canonical_path_) into <name>_clean.json
  touched_by:
  - pandas (via df.to_parquet at call site)
  - pathlib.Path
  - broadway.config.schema.EnvironmentConfig/EtlStep
  validated_by:
  - tests/test_structural_cleaning.py::test_etl_module_writes_canonical_and_result
  - tests/test_structural_cleaning.py::test_etl_ci_sampling_gated
  - tests/test_boundary_contracts.py::test_raw_canonical_boundary_rejects_dtype_mutation
  if_changed:
  - ARTIFACT-CANONICAL-PARQUET name/location
  - CFG-ENV-PROCESSED-SUBDIR keys
  - CFG-ETL-SPLIT-FILES keys
  - index=False write flag
  findings:
  - POST-WRITE RELOAD VALIDATION DOES NOT EXIST in this band — etl never re-reads the parquet bytes it just wrote; schema validation happens exclusively pre-write on the in-memory frame (module.py:95), so any parquet round-trip dtype drift (e.g. datetime→int64, categorical loss) would surface only when features/training readers open the file (src/broadway/features/module.py:25, src/broadway/training/module.py:42 — different band). The only reload today is inside tests (tests/test_structural_cleaning.py:236).
- id: GATE-ETL-17
  phase: etl-lookup
  order: 17
  owner: 'project/etl/process.py: select_and_clean_columns() ledger-accounting grammar aspect (narrowed ledger-append-sites owner BLOCKED pending DP-A07/R2 StageLedger merge — packet F #1b)'
  inputs:
  - renamed post-filter df
  - DatasetContract (contract.columns order IS the projection order)
  - 'optional StageLedger param (list[tuple[str,int]]; WIP-pending, see FINDING)'
  outputs:
  - contract-projected
  - dropna'd
  - 'deduplicated df; ledger appended with ("dropna", rows_after) then ("duplicates", rows_after) — line anchors stripped pending DP-A07/R2'
  transforms:
  - missing contract columns raise ValueError (:93-94); extra columns dropped with logger.info ONLY (:95-97)
  - single df[cols].dropna() — NaN in ANY kept column (target included) silently removes the row, accounted only as an aggregate count
  - drop_duplicates AFTER dropna so NaN twins die at dropna, keeping the duplicates stage honest (FX-A02/FX-A03a)
  touched_by:
  - logging
  - pandas (df ops)
  - broadway.config.schema.DatasetContract
  - project.etl.process_config (indirectly via caller chain)
  validated_by:
  - project/tests/test_process.py::test_select_and_clean_columns
  - project/tests/test_ingest_surface.py::test_select_and_clean_columns_dedups
  - project/tests/test_ingest_surface.py::test_select_and_clean_columns_nan_twins_die_at_dropna_not_dedup
  - project/tests/test_ingest_surface.py::test_select_and_clean_columns_zero_duplicates_keeps_counts_equal
  - project/tests/test_ingest_contract.py::test_select_and_clean_columns_keeps_exactly_contract_columns
  if_changed:
  - StageLedger tuple arity/order
  - ledger stage names ("dropna"/"duplicates")
  - projection-order semantics
  - extra-column logging level
  findings:
  - StageLedger stores ROWS-AFTER, not deltas, and appends even for zero-drop stages; the "-N rows" deltas only materialize later in _ingest_audit (:178-181). Any consumer treating ledger entries as drop counts overcounts; per-column attribution of dropna losses does not exist anywhere on this path.
  - 'WIP-PENDENCY (packet F #1): the StageLedger param/appends and the dedup ordering exist ONLY in the M project/etl/process.py worktree — at HEAD 5016e93 the body ends df[cols].dropna() (:99) with no ledger param, no ledger.append, no drop_duplicates. This row''s accounting grammar lands tracked WITH that batch (DP-A07/R2); until then the GATE-INGEST-04 co-tenancy flag stays deliberately unresolved.'
- id: GATE-ETL-18
  phase: etl-lookup
  order: 18
  owner: project/etl/process.py:23 _ROW_DELTA
  inputs:
  - 'reason strings produced by _ingest_audit (:168) in the form "<stage>: -<N> rows"; mirrored consumer broadway.etl.module._explained_rows() at src/broadway/etl/module.py:32 parsing etl-step reasons incl. "CI sampling: -N rows"'
  outputs:
  - explained row total subtracted from dropped_total → TransformAudit.rows_dropped_unexplained (process.py:183-187; module.py:115)
  transforms:
  - regex r'-(\d+) rows$' anchored at END-of-string; stages with zero delta emit NO reason line (:180-181) so they parse as zero
  - any reason NOT ending in '-<N> rows' is silently unparseable → counts toward rows_dropped_unexplained → trips enforce_drop_fraction (loud) rather than vanishing
  touched_by:
  - re (both sides)
  - broadway.lineage.models.TransformAudit
  - broadway.lineage.records.enforce_drop_fraction (downstream consumer)
  validated_by:
  - project/tests/test_ingest_audit.py::test_ingest_record_carries_balanced_audit
  - project/tests/test_ingest_audit.py::test_audit_zeros_on_zero_drop_run
  - project/tests/test_ingest_audit.py::test_ci_sampling_reorder_becomes_visible_in_ledger
  - project/tests/test_ingest_audit.py::test_nan_twins_account_at_dropna_through_full_pipeline
  - tests/test_loud_failures.py::test_enforce_drop_fraction_raises
  - tests/test_loud_failures.py::test_enforce_drop_fraction_zero_rows_ok
  if_changed:
  - _ROW_DELTA pattern OR module.py:35 inline r"-(\\d+) rows$"
  - 'reason-string format "<stage>: -N rows"'
  - TransformAudit.rows_dropped_unexplained semantics
  findings:
  - the "shared grammar" is shared BY COMMENT ONLY — process.py:21-23 and module.py:32-38 carry two independent copies of the identical regex with no common symbol (project imports broadway, but neither side imports the other's parser); editing either pattern alone silently desyncs explained/unexplained accounting between the ingest and etl steps.
- id: GATE-ETL-19
  phase: etl-lookup
  order: 19
  owner: project/etl/process.py:194 process_data()
  inputs:
  - dataset name → configs/dataset/<name>.yaml via _load_contract (:140)
  - raw yellow_tripdata_*.parquet glob (get_raw_files :28 raises FileNotFoundError when empty)
  - taxi knobs from configs/project/taxi.yaml via process_config.py:16-18 (raw_dir/processed_dir/processed_file)
  outputs:
  - training_data.parquet via save_processed_data (:132, to_parquet index=False)
  - lineage record node_id('ingest') with full _ingest_audit (columns_before/after, added/removed, balanced drop accounting) at :211-217
  transforms:
  - polars lazy scan → collect → to_pandas (:37), CI-gated sample_for_ci
  - _stage_ledger chains sample/filter/duration/rename/select recording (stage, rows_out) per stage
  - validate_contract_schema (:123) rejects empty dtype strings, unsupported pandera dtypes, and fails build_raw_schema().validate BEFORE the write
  - audit built AFTER write from captured rows_in/stages/column snapshots
  touched_by:
  - logging
  - os
  - re
  - pathlib.Path
  - pandera
  - pandas
  - polars
  - yaml
  - broadway.config.loader.CONFIGS_DIR
  - broadway.config.schema.DatasetContract
  - broadway.contracts.pandera
  - project.etl.process_config
  - broadway.lineage.ids.node_id
  - broadway.lineage.models.TransformAudit
  - broadway.lineage.records.write_record
  validated_by:
  - project/tests/test_process.py::test_process_data_writes_ingest_lineage
  - project/tests/test_ingest_audit.py::test_ingest_record_carries_balanced_audit
  - project/tests/test_ingest_audit.py::test_audit_zeros_on_zero_drop_run
  - project/tests/test_ingest_audit.py::test_ci_sampling_reorder_becomes_visible_in_ledger
  - project/tests/test_ingest_audit.py::test_nan_twins_account_at_dropna_through_full_pipeline
  if_changed:
  - configs/project/taxi.yaml keys consumed by process_config
  - processed_file name
  - node_id('ingest') parents ([node_id('dataset')])
  - CLI wiring src/broadway/cli.py:144-146
  findings:
  - the ingest path NEVER calls enforce_drop_fraction — repo-wide the only callers are etl (module.py:127) and features (features/module.py:68); process_data persists rows_dropped_unexplained on the ingest record with no max_drop_fraction enforcement, so unexplained loss at the project ingest boundary is recorded but never loud.
- id: GATE-ETL-116
  phase: etl-lookup
  order: 20
  owner: 'src/broadway/samples/generate.py:95 generate_sample()'
  inputs:
  - SampleSpec definition
  outputs:
  - '<name>@<version>.parquet + provenance json {artifact_sha256, definition_sha256, row_count}'
  transforms:
  - 'immutable-artifact producer with provenance sha256 pair: "definition_sha256": _canonical_spec_sha256(spec) (:144, canonical model-dump digest :32-39) + "artifact_sha256": _file_sha256(artifact) (:145)'
  - 'symbol repair per packet F #25: producer is generate_sample SINGULAR (:95) — no generate_samples exists; slate owner string corrected'
  touched_by:
  - pandas
  - pydantic
  validated_by:
  - tests/test_sample.py::test_load_sample_round_trip
  if_changed:
  - provenance json schema consumed at sample load time
  findings: []
- id: GATE-ETL-117
  phase: etl-lookup
  order: 21
  owner: 'project/etl/process_config.py:10 _PROJECT_YAML/_ETL_YAML module-load constants (legacy globals raw_dir/processed_dir/processed_file :16-25)'
  inputs:
  - 'configs/project/taxi.yaml knobs'
  outputs:
  - 'raw_dir/processed_dir/processed_file for the LEGACY pipeline'
  transforms:
  - 'twin verified: module-level loads of configs/project/taxi.yaml + configs/step/etl.yaml (:10-14) expose legacy module globals (:16-25) — a parallel configs dialect alongside broadway.loader; module-scope constants ARE the symbol here (no functions in file, packet F #26)'
  - 'retirement/fork guard cites BOTH dialect facts (packet F #26 rider feeding GATE-CFG-104): the twin imports CONFIGS_DIR from broadway.config.loader (:6) while project/data.py hardcodes relative Paths (:31-47)'
  touched_by:
  - project.etl.process (caller chain)
  validated_by:
  - project/tests/test_process_config.py::test_project_config_matches_process_constants
  if_changed:
  - legacy global names consumed by process_data (GATE-ETL-19)
  findings: []
- id: GATE-FEAT-20
  phase: features
  order: 22
  owner: src/broadway/features/builders.py:46 BUILDERS (log1p at builders.py:53)
  inputs:
  - ARTIFACT-SPLIT (etl train/val parquet frame)
  - CFG-FEATURES (features.derived[].func/source)
  - DerivedFeature(name,func,source)
  outputs:
  - derived columns appended onto a copy of df (builders.py:118 result=df.copy(), :145 result[feat.name]=...)
  - declared dtypes via _BUILDER_DTYPES (builders.py:62-73)
  transforms:
  - 'trip_distance → distance_log = np.log1p(trip_distance)  # BUILDERS[''log_distance''], builders.py:53'
  - 'source → source_copy = df[src].astype(''float64'')  # dtype SSOT _BUILDER_DTYPES, builders.py:59'
  - 'pickup==dropoff → same_group = (df[group_col]==df[lookup_col]).astype(int)  # builders.py:22-29'
  touched_by:
  - importlib
  - numpy(np.log1p)
  - pandas(pd.to_datetime)
  - broadway.config.schema.DerivedFeature
  validated_by:
  - tests/test_builders.py::test_build_derived_ok
  - tests/test_builders.py::test_build_derived_chaining_earlier_derived_source_allowed
  - tests/test_generic_features.py::test_datetime_builders
  - tests/test_generic_features.py::test_source_copy_casts_to_declared_float64
  if_changed:
  - GATE-FEAT-21 collision guard surface
  - GATE-FEAT-24 transform order
  - GATE-FEAT-26 specs/dtypes (_BUILDER_DTYPES feeds builder_dtype→specs)
  - ARTIFACT-TRAIN-PARQUET columns
  - tests/test_engineered_order_parity.py::test_specs_order_matches_producer_write_order
  findings:
  - log1p output is numerically pinned ONLY by the double-log chaining test (test_builders.py:78 asserts np.log1p∘np.log1p); no direct single-hop golden test for BUILDERS['log_distance'] exists — the transform itself rides unpinned on guard-test vehicles (test_builders.py:43,64).
- id: GATE-FEAT-21
  phase: features
  order: 23
  owner: 'src/broadway/features/builders.py:122 build_derived()  # F6-GUARD label-collision guard'
  inputs:
  - df (input frame)
  - features.derived[] in config order
  - target (dataset target name)
  - registry=BUILDERS∪extra_builders (builders.py:115-117)
  outputs:
  - ValueError raise sites — derived name == target ⇒ ValueError (builders.py:123-127); name ∈ result.columns ⇒ ValueError with origin 'input data column' vs 'previously built derived feature' (builders.py:133-138); unknown func ⇒ ValueError (builders.py:140); missing source ⇒ ValueError (builders.py:144)
  transforms:
  - per-feature pre-write check feat.name ∉ {target} ∪ result.columns before result[feat.name] = registry[feat.func](result, feat.source)
  - 'chaining carve-out: source resolves against accumulating result frame, not raw input (builders.py:143) — earlier derived column may be consumed'
  touched_by:
  - pandas(df.copy/columns)
  - numpy(via BUILDERS lambdas)
  - broadway.config.schema.DerivedFeature
  validated_by:
  - tests/test_builders.py::test_build_derived_target_collision_raises
  - tests/test_builders.py::test_build_derived_duplicate_names_raise
  - tests/test_builders.py::test_build_derived_existing_column_collision_raises
  - tests/test_builders.py::test_build_derived_chaining_earlier_derived_source_allowed
  - tests/test_builders.py::test_build_derived_unknown_func_raises
  - tests/test_builders.py::test_build_derived_missing_source_raises
  - tests/test_generic_features.py::test_feature_pipeline_same_group_without_builder_params_raises
  if_changed:
  - agents/ledger/FIXES.md F6-GUARD row (FIXES.md:65)
  - all downstream gates consuming build_derived (24,25,26)
  - tests/test_builders.py message-match regexes
  findings: []
- id: GATE-FEAT-22
  phase: features
  order: 24
  owner: src/broadway/features/builders.py:76 load_custom_builders()
  inputs:
  - CFG-FEATURES (features.builder_module str|None)
  outputs:
  - 'extra_builders dict merged over BUILDERS inside build_derived (builders.py:115-117); raise sites: unimportable module ⇒ ValueError (builders.py:82)'
  - module without dict BUILDERS ⇒ ValueError (builders.py:85)
  - generic-name collision ⇒ ValueError naming collisions (builders.py:88-90)
  transforms:
  - builder_module=None → {} short-circuit (builders.py:77-78)
  - 'custom BUILDERS names shadow nothing: intersection with generic registry fails loud (builders.py:86-90)'
  touched_by:
  - importlib.import_module
  - broadway.features.builders.BUILDERS
  validated_by:
  - tests/test_generic_features.py::test_load_custom_builders_import_error
  - tests/test_generic_features.py::test_load_custom_builders_missing_builders
  - tests/test_generic_features.py::test_load_custom_builders_collision
  - tests/test_generic_features.py::test_load_custom_builders_valid
  if_changed:
  - GATE-FEAT-21 registry merge
  - GATE-FEAT-24 (transform calls this per invocation)
  - tests/test_generic_features.py custom-builder cases
  findings: []
- id: GATE-FEAT-23
  phase: features
  order: 25
  owner: src/broadway/features/pipeline.py:21 FeaturePipeline.fit()
  inputs:
  - ARTIFACT-SPLIT train frame (loaded at src/broadway/features/module.py:25)
  - cfg.dataset.target
  - cfg.features.encoding_smoothing
  - cfg.experiment.features.encodings (pipeline ctor arg, module.py:35)
  outputs:
  - fitted encoder state _target_encoders/_freq_encoders (pipeline.py:22-23 reset then append :27-31) persisted later into ARTIFACT-PIPELINE-PICKLE (module.py:52-54)
  transforms:
  - 'fit order = encodings config order; per column: type==''target'' → TargetEncoding(columns=[col], smoothing).fit(df, df[target]) (pipeline.py:26-28); type==''frequency'' → FrequencyEncoding(columns=[col]).fit(df) (pipeline.py:29-31)'
  - target series reaches the encoder via the estimator y argument, never read out of X (transformers.py:30-32)
  touched_by:
  - dataclasses(field)
  - typing(Any)
  - pandas
  - broadway.config.schema(EncodingConfig, FeatureConfig)
  - broadway.features.builders(build_derived, load_custom_builders)
  - broadway.features.transformers(TargetEncoding, FrequencyEncoding)
  validated_by:
  - tests/test_transformers.py::test_pipeline_reexpression_output_identical
  - tests/test_boundary_contracts.py::test_split_to_features_boundary_rejects_dtype_mutation
  - tests/test_engineered_order_parity.py::test_specs_order_matches_producer_write_order
  if_changed:
  - ARTIFACT-PIPELINE-PICKLE schema
  - GATE-FEAT-24 ordering
  - training read path (src/broadway/training/module.py:95-103)
  - tests/test_transformers.py golden values
  findings:
  - TargetEncoding.fit y-is-None raise (src/broadway/features/transformers.py:52-55) is an UNPINNED raise site — no test node id exercises it (grep tests/ for 'requires y' → 0 hits).
- id: GATE-FEAT-24
  phase: features
  order: 26
  owner: src/broadway/features/pipeline.py:34 FeaturePipeline.transform()
  inputs:
  - train/val frame
  - cfg.experiment.features (FeatureConfig)
  - target
  - freq_fill (cfg.features.frequency_fill)
  - builder_kwargs from cfg.features.builder_params (pipeline.py:37-41)
  outputs:
  - 'engineered frame: input copy + derived cols + <col>_freq_enc float64 + <col>_target_enc float64'
  transforms:
  - 'PINNED ORDER: df.copy() → build_derived (pipeline.py:42-48) → ALL frequency encoders (pipeline.py:49-50, fill=freq_fill for unseen keys) → ALL target encoders (pipeline.py:51-52)'
  - builder_kwargs forwards exactly group_col+lookup_col (pipeline.py:38-41); BuilderParams declares only those two fields (config/schema.py:93-97), so nothing else can drift
  touched_by:
  - typing(Any)
  - pandas(df.copy)
  - broadway.config.schema(EncodingConfig, FeatureConfig)
  - broadway.features.builders(build_derived, load_custom_builders)
  - broadway.features.transformers(FrequencyEncoding.transform, TargetEncoding.transform)
  validated_by:
  - tests/test_generic_features.py::test_feature_pipeline_transform_with_custom_builder
  - tests/test_generic_features.py::test_feature_pipeline_same_group_with_builder_params
  - tests/test_generic_features.py::test_feature_pipeline_same_group_without_builder_params_raises
  - tests/test_generic_features.py::test_feature_pipeline_absent_builder_params_is_inert
  - tests/test_transformers.py::test_index_and_row_order_preserved
  - tests/test_engineered_order_parity.py::test_specs_order_matches_producer_write_order
  if_changed:
  - GATE-FEAT-25 write shape
  - GATE-FEAT-26 emission-order parity (generic.py:22-24 mirrors THIS write order)
  - ARTIFACT-TRAIN-PARQUET column order
  - tests/test_engineered_order_parity.py
  findings: []
- id: GATE-FEAT-25
  phase: features
  order: 27
  owner: 'src/broadway/features/module.py:45 run()  # ARTIFACT-TRAIN-PARQUET write'
  inputs:
  - ARTIFACT-SPLIT train parquet (module.py:25)
  - optional val parquet (:27)
  - CFG-FEATURES/DATASET/EXPERIMENT/ETL precondition raise (module.py:32-33)
  - encoding_smoothing/frequency_fill/max_drop_fraction
  outputs:
  - ARTIFACT-TRAIN-PARQUET out_dir/cfg.etl.train_features_file index=False (module.py:45)
  - ARTIFACT-VAL-PARQUET val_features_file (:50)
  - ARTIFACT-PIPELINE-PICKLE pickle.dump (:52-54)
  - lineage record node_id("features", dataset) with TransformAudit rows/columns_before/after/added/removed (module.py:57-69)
  transforms:
  - 'step chain: pipeline.fit(train, target, smoothing) (:36) → pipeline.transform(train,...) (:39) → build_generic_feature_specs (:40) → build_engineered_schema(specs).validate(train_out) (:41) → WRITE (:45); val branch repeats transform+validate+write (:48-50)'
  - 'audit: dropped_total=rows_in−rows_out; rows_dropped_unexplained=max(0,dropped_total) (:60-61); enforce_drop_fraction(audit, max_drop_fraction) (:68)'
  touched_by:
  - logging
  - pickle
  - pathlib.Path
  - pandas(read_parquet/to_parquet)
  - broadway.config.schema.PipelineConfig
  - broadway.features.generic(build_generic_feature_specs)
  - broadway.features.pipeline(FeaturePipeline)
  - broadway.features.schema(build_engineered_schema)
  - broadway.lineage.ids(node_id)
  - broadway.lineage.models(TransformAudit)
  - broadway.lineage.records(enforce_drop_fraction, write_record)
  validated_by:
  - tests/test_boundary_contracts.py::test_split_to_features_boundary_rejects_dtype_mutation
  - tests/test_boundary_contracts.py::test_raw_canonical_boundary_rejects_dtype_mutation
  - tests/test_engineered_order_parity.py::test_specs_order_matches_producer_write_order
  if_changed:
  - training loader reads (src/broadway/training/module.py:42)
  - evaluate loaders (src/broadway/evaluate/module.py:44-58)
  - GATE-FEAT-27 read contract
  - lineage graph node etl→features
  - tests/test_boundary_contracts.py B2/B3-B4 suites
  findings:
  - the ARTIFACT-TRAIN-PARQUET write itself (module.py:45) and run() have NO direct test node id — tests import only _load_split (tests/test_boundary_contracts.py:71); artifact content/order is pinned transitively by boundary + parity suites, never by executing the step's write path.
  - write-side validation (:41,:49) uses build_engineered_schema which is UNORDERED (schema.py:29-37 sets no ordered=True) while the read contract engineered_schema_for is ordered=True (generic.py:55) — column-order drift passes silently through the features-step write gate and only explodes later at training/evaluate read time.
- id: GATE-FEAT-26
  phase: features
  order: 28
  owner: src/broadway/features/generic.py:14 build_generic_feature_specs()
  inputs:
  - DatasetContract.columns (declared dtypes/null_count)
  - CFG-FEATURES include/derived/encodings
  - builder_dtype→_BUILDER_DTYPES (builders.py:94-95)
  outputs:
  - ordered FeatureSpec registry — SSOT for both write-side (GATE-FEAT-25 :40-41) and read-side (GATE-FEAT-27 engineered_schema_for :49) schemas
  transforms:
  - 'emission ORDER PINNED: include∩contract.columns preserving contract dtype/nullable=null_count>0 (generic.py:16-19) → derived w/ builder_dtype(func) (generic.py:20-21) → ALL frequency ''<col>_freq_enc'' float64 (generic.py:25-28) → ALL target ''<col>_target_enc'' float64 (generic.py:29-32) — comment pins this to FeaturePipeline.transform''s write order (generic.py:22-24)'
  touched_by:
  - pandas
  - pandera(pa.DataFrameSchema via sink)
  - pandera.errors(SchemaError, SchemaErrors)
  - broadway.config.schema(DatasetContract, FeatureConfig, PipelineConfig)
  - broadway.contracts.pandera(pandera_dtype)
  - broadway.features.builders(builder_dtype)
  - broadway.features.schema(FeatureSpec)
  - broadway.schemas.joined(joined_schema_columns)
  validated_by:
  - tests/test_generic_features.py::test_build_generic_feature_specs
  - tests/test_engineered_order_parity.py::test_specs_order_matches_producer_write_order
  - tests/test_boundary_contracts.py::test_split_to_features_boundary_rejects_dtype_mutation
  if_changed:
  - GATE-FEAT-25 validate call
  - GATE-FEAT-27 ordered schema
  - GATE-FEAT-28 declared surface via schema_columns('engineered')
  - schemas/__init__.py lazy import (schemas/__init__.py:23)
  findings: []
- id: GATE-FEAT-27
  phase: features
  order: 29
  owner: 'src/broadway/features/generic.py:122 validate_engineered_frame()  # the contract''s ''validate_engineered_schema'''
  inputs:
  - ARTIFACT-TRAIN-PARQUET / ARTIFACT-VAL-PARQUET frames read back (training/module.py:42-44, evaluate/module.py:49,58)
  - PipelineConfig (dataset+experiment)
  outputs:
  - 'SchemaError raise chain: ordered-schema violation incl. SchemaErrors→SchemaError normalization (generic.py:131-138); extra-column guard FIX_4 G2 declared=contract∪joined_lookup∪specs (generic.py:91-119, raise :110); explicit target-dtype hook (generic.py:59-88, raises :69 missing-target and :79 dtype-mismatch)'
  transforms:
  - engineered_schema_for builds pa.DataFrameSchema(..., ordered=True) from the specs SSOT (generic.py:50-56, ordered flag at :55); non-strict by design so extra tolerated at pandera level, then rejected by the explicit subset guard
  - 'TARGET-EXCLUSION SEMANTICS AT INFERENCE TIME: the ordered schema covers FEATURE columns only; the target is deliberately outside it and enforced separately by validate_target_dtype (generic.py:62-64 docstring pin) — consumers then drop the target via eligible_feature_columns before X is built'
  touched_by:
  - pandas
  - pandera
  - pandera.errors(SchemaError, SchemaErrors)
  - broadway.config.schema(DatasetContract, FeatureConfig, PipelineConfig)
  - broadway.contracts.pandera(pandera_dtype)
  - broadway.features.builders(builder_dtype)
  - broadway.features.schema(FeatureSpec)
  - broadway.schemas.joined(joined_schema_columns)
  validated_by:
  - tests/test_boundary_contracts.py::test_engineered_read_boundary_rejects_dtype_mutation
  - tests/test_boundary_contracts.py::test_gap_engineered_boundary_extra_column
  - tests/test_boundary_contracts.py::test_gap_engineered_boundary_column_reorder
  - tests/test_boundary_contracts.py::test_gap_target_dtype_flip_through_training
  if_changed:
  - src/broadway/training/module.py:48-50 and src/broadway/evaluate/module.py:50
  - 59 call sites
  - GATE-FEAT-25 write-side pairing
  - B3/B4 boundary suite expectations
  findings:
  - ZERO test node ids reference validate_engineered_frame / engineered_schema_for / _assert_no_extra_columns / validate_target_dtype directly (grep tests/ → 0 hits) — this whole read contract is covered ONLY transitively through training/evaluate loader boundary tests above; a refactor of generic.py internals would leave no direct failing node id.
  - contract symbol name mismatch recorded in header — 'validate_engineered_schema' does not exist in this tree.
- id: GATE-FEAT-28
  phase: features
  order: 30
  owner: src/broadway/utils.py:16 eligible_feature_columns()
  inputs:
  - engineered frame (post GATE-FEAT-27 validation)
  - PipelineConfig.experiment.data_source.schema_contract
  - PipelineConfig.dataset.target
  - PipelineConfig.experiment.preprocessing claims (utils.py:32)
  outputs:
  - model-input matrix X = declared surface ∩ frame − target
  - 'FRAME ORDER PRESERVED not sorted (utils.py:33,42 docstring :23); raise sites: missing experiment/dataset ⇒ ValueError (utils.py:26); eligible categorical neither preprocessing-claimed nor numeric ⇒ ValueError with exact pinned message (utils.py:37-41)'
  transforms:
  - eligible = [name for name in df.columns if name in declared and name != cfg.dataset.target] (utils.py:33) — TARGET EXCLUDED FROM X at fit AND inference
  - 'Decision-5 pin: numeric-category fallback frozenset {''int32'',''int64'',''float64''} over OBSERVED runtime dtypes (utils.py:11-13); object/datetime64 fail loud'
  touched_by:
  - numpy(np.isinf sibling require_finite)
  - pandas(select_dtypes)
  - broadway.config.schema.PipelineConfig
  - broadway.schemas(schema_columns → GATE-FEAT-29)
  validated_by:
  - tests/test_decision5_columns.py::test_schema_derived_matches_retired_selector_including_frame_order
  - tests/test_decision5_columns.py::test_golden_metric_equality_with_retired_selection
  - tests/test_decision5_columns.py::test_undeclared_numeric_column_is_excluded
  - tests/test_decision5_columns.py::test_unclaimed_categorical_raises_exact_message
  - tests/test_decision5_columns.py::test_recipe_explicit_column_bypasses_assertion
  - tests/test_decision5_columns.py::test_numeric_category_dtypes_pass_unclaimed[int32]
  - tests/test_decision5_columns.py::test_numeric_category_dtypes_pass_unclaimed[int64]
  - tests/test_decision5_columns.py::test_numeric_category_dtypes_pass_unclaimed[float64]
  - tests/test_decision5_columns.py::test_datetime64_fires_the_assertion
  - tests/test_decision5_columns.py::test_missing_experiment_or_dataset_config_raises
  - tests/test_decision5_columns.py::test_shipped_config_passes_and_matches_retired_selector
  - tests/test_training_contracts.py::test_target_encoding_recipe_end_to_end_fits_and_predicts
  if_changed:
  - src/broadway/training/module.py:56 _xy
  - src/broadway/evaluate/module.py:52
  - 61 loaders
  - mlflow infer_signature input schema (training/module.py:144)
  - pyfunc enforcement at predict
  - Decision-5 ledger rows
  findings: []
- id: GATE-FEAT-29
  phase: features
  order: 31
  owner: 'src/broadway/schemas/__init__.py:17 _engineered_schema_columns()  # dispatch at schemas/__init__.py:41 schema_columns()'
  inputs:
  - cfg.experiment.data_source.schema_contract string
  - DatasetContract
  - FeatureConfig|None
  outputs:
  - 'frozenset declared surface feeding GATE-FEAT-28 (utils.py:27-31) and recipe.validate_preprocessing_columns binding check (recipe.py:129-133); raise sites: ''engineered'' without features config ⇒ ValueError (schemas/__init__.py:25-30); unknown schema_contract ⇒ ValueError listing supported modules (schemas/__init__.py:53-57)'
  transforms:
  - registry dispatch _SCHEMA_MODULES {'raw'|'joined'|'engineered'} (schemas/__init__.py:34-38); raw/joined IGNORE features, engineered REQUIRES it (docstring :48-50)
  - lazy import of build_generic_feature_specs inside the resolver (schemas/__init__.py:23) keeps the builders registry off every schemas consumer's package-import path
  touched_by:
  - collections.abc(Callable)
  - broadway.config.schema(DatasetContract, FeatureConfig)
  - broadway.schemas.joined(joined_schema_columns)
  - broadway.features.generic(build_generic_feature_specs — LAZY, function-scope only)
  validated_by:
  - tests/test_decision5_columns.py::test_engineered_requires_features_config
  - tests/test_decision5_columns.py::test_raw_and_joined_ignore_features_argument
  - tests/test_recipe.py::test_unknown_schema_contract_fails_loud
  - tests/test_recipe.py::test_lookup_derived_column_passes_joined_fails_raw
  - tests/test_recipe.py::test_cross_check_rejects_column_outside_bound_schema
  - tests/test_recipe.py::test_config_load_accepts_valid_recipe
  if_changed:
  - GATE-FEAT-26 SSOT resolution
  - GATE-FEAT-28 declared surface
  - recipe config-load gating (recipe.py:102-141)
  - Decision-6 ledger rows
  findings: []
- id: GATE-TRAIN-30
  phase: training-eval
  order: 32
  owner: src/broadway/training/module.py:89 run()
  inputs:
  - CFG-DATASET-CONTRACT
  - CFG-STEP-TRAIN
  - experiment+etl config sections
  - analysis contract
  outputs:
  - ARTIFACT-TRAINING-RESULT
  - lineage node training:<dataset> (:153-158)
  transforms:
  - 'config-section guard: missing dataset/experiment/train/etl → ValueError naming them all (:90-91)'
  - 'analysis-mode gate: require_mode(cfg.analysis, AnalysisMode.PREDICTION) before any IO (:92)'
  - orchestration only — delegates to GATE-TRAIN-31..34 then opens the MLflow run (:95-145)
  touched_by:
  - mlflow@3.15.1
  - pandas@2.3.3
  validated_by:
  - tests/test_cli.py::TestTrainCLI::test_train_without_dataset_exits_1_naming_missing_config
  - tests/test_cli.py::TestTrainCLI::test_train_without_analysis_exits_1_naming_contract_requirement
  - tests/test_training_contracts.py::test_module_run_writes_training_result
  - tests/test_full_dispatch.py::test_flows_dispatch_per_mode
  if_changed:
  - full-flow prediction order (configs/step/full.yaml)
  - ARTIFACT-TRAINING-RESULT existence contract of GATE-TRAIN-35
  - lineage graph node training:<dataset>
  - tests/test_cli.py TestTrainCLI pins
  findings:
  - both entry raises are pinned ONLY through the CLI wrapper (exit-1 stderr substrings in test_cli.py) — no test calls training.module.run with a deficient PipelineConfig directly, so a refactor of run()'s guard clauses (not the CLI dispatch) would pass the whole suite silently.
- id: GATE-TRAIN-31
  phase: training-eval
  order: 33
  owner: src/broadway/training/module.py:39 _load_features()
  inputs:
  - 'ARTIFACT-CANONICAL-FRAME derivatives: etl train_features_file parquet'
  - optional val_features_file parquet
  outputs:
  - train_df
  - val_df (validated engineered frames)
  transforms:
  - pd.read_parquet train always (:42); val read ONLY if val_path.exists(), else None (:43-44)
  - re-enforce engineered contract on READ via validate_engineered_frame(cfg, df) for BOTH frames — dtype/column-order/target-dtype drift dies here as SchemaError instead of mid-fit (:48, :50; impl src/broadway/features/generic.py:122-140)
  - 'no-val fallback: split(train_df, dataset, experiment.split, random_state) (module.py:97-98 → src/broadway/data/splitter.py:29 split(); time-split requires datetime_column ValueError :32-35, stratified requires classification task ValueError :36-39, else seeded random split :40)'
  touched_by:
  - pandas@2.3.3
  - scikit-learn@1.7.2
  validated_by:
  - tests/test_boundary_contracts.py::test_engineered_read_boundary_rejects_dtype_mutation[train_features_feature_1]
  - tests/test_boundary_contracts.py::test_engineered_read_boundary_rejects_dtype_mutation[train_features_feature_2]
  - tests/test_boundary_contracts.py::test_engineered_read_boundary_rejects_dtype_mutation[val_features_feature_1]
  - tests/test_boundary_contracts.py::test_engineered_read_boundary_rejects_dtype_mutation[val_features_feature_2]
  - tests/test_boundary_contracts.py::test_gap_engineered_boundary_extra_column
  - tests/test_boundary_contracts.py::test_gap_engineered_boundary_column_reorder
  - tests/test_boundary_contracts.py::test_gap_target_dtype_flip_through_training
  if_changed:
  - every downstream X/y shape seen by GATE-TRAIN-33/34
  - B3/B4 boundary tripwires (tests/test_boundary_contracts.py:229-296)
  - leakage posture (val file presence decides split path)
  findings:
  - splitter.split's two raise sites are UNPINNED — repo-wide grep of tests/ finds no test exercising time-split-without-datetime_column or stratified-on-regression; only happy-path split() calls exist (tests/test_integration.py:38,:63). A silent re-order or deletion of either guard would land green.
- id: GATE-TRAIN-32
  phase: training-eval
  order: 34
  owner: project/data.py:166 read_training_sample()
  inputs:
  - CFG-DATASET-CONTRACT (taxi.yaml via _contract :31-32)
  - CFG-STEP-TRAIN.random_state
  - optional sample n / columns / full flag
  outputs:
  - raw sampled pd.DataFrame (pre-feature-engineering)
  transforms:
  - 'thin glue: _loader.read_sample(_contract, sample, seed=seed, columns, full) with seed DEFAULTING to RANDOM_STATE pinned from configs/step/train.yaml (:106, :168, delegate :173-175)'
  - 'loader guard: sample=None without full=True → ValueError ''pass sample=<n>, or full=True'' (src/broadway/data/loader.py:168-169)'
  - polars lazy scan → optional column pruning → df.sample(n, seed=seed passthrough) → to_pandas (loader.py:170-176); NOT DATA_MODE-aware by design (dataflow.md:450)
  touched_by:
  - numpy@2.5.2
  - pandas@2.3.3
  - polars@1.43.2
  - pyarrow@19.0.1
  - pyyaml@6.0.2
  validated_by:
  - tests/test_data_loader.py::test_read_sample_respects_sample_size
  - tests/test_data_loader.py::test_read_sample_seed_reproducible
  - tests/test_data_loader.py::test_read_sample_column_pruning
  - tests/test_data_loader.py::test_read_sample_requires_sample_or_full
  - tests/test_data_loader.py::test_read_sample_full_returns_all
  if_changed:
  - every experiments/univariate/fare_amount_trip_distance script importing it (01:16, 06:6)
  - experiments_ui.py external-source audit regex (_EXTERNAL_RE :47 flags the literal 'read_training_sample' in observation JSON)
  - reproducibility of all seeded experiment artifacts
  findings:
  - the seed PIN itself is untested below the glue — all five validated_by ids hit broadway.data.loader.read_sample directly with explicit seeds; nothing asserts read_training_sample()'s default binds to RANDOM_STATE, so decoupling the default (project/data.py:168) would pass the suite while silently changing every experiment's sample. Callers passing seed=None also silently disable reproducibility with no warning.
- id: GATE-TRAIN-33
  phase: training-eval
  order: 35
  owner: src/broadway/training/module.py:59 _resolve_params()
  inputs:
  - CFG-STEP-TRAIN.model.params
  - optional HPO block (models/search_space/total_trials/top_k/storage_url)
  - X/y train+val from GATE-TRAIN-31
  outputs:
  - final model param dict fed to GATE-TRAIN-34
  transforms:
  - hpo absent → return static cfg.experiment.model.params unchanged (:67-68)
  - 'else run_hpo (src/broadway/training/hpo.py:271): guards ≥1 model (:294-295), empty-leaderboard → ValueError after both rounds (:306-307); two rounds — parallel initial studies seeded random_state+model_index (:204-229, offset :223) then bandit_allocate top-k remainder-to-best (hpo.py:180-201, guards :193-194)'
  - 'study target must match cfg.experiment.model.type: best-missing → ValueError ''hpo produced no valid trial for model …'' (:81-85)'
  - non-finite trial objective → optuna.TrialPruned (hpo.py:113-114); leaderboard silently DROPS studies with no valid trial (hpo.py:260-268)
  touched_by:
  - mlflow@3.15.1
  - pandas@2.3.3
  validated_by:
  - tests/test_hpo.py::test_run_hpo_requires_models
  - tests/test_hpo.py::test_run_hpo_no_valid_trial_raises
  - tests/test_hpo.py::test_bandit_allocate_top_k_selection
  - tests/test_hpo.py::test_bandit_allocate_even_split
  - tests/test_hpo.py::test_bandit_allocate_remainder_to_best
  - tests/test_hpo.py::test_bandit_allocate_remaining_less_than_k
  - tests/test_hpo.py::test_run_hpo_deterministic
  - tests/test_hpo.py::test_run_hpo_parallel_equals_sequential
  if_changed:
  - params reaching ARTIFACT-TRAINING-RESULT and the logged model
  - HPO determinism claims (hpo.py docstring :5-7)
  - nested per-trial MLflow runs (hpo.py:66-96)
  findings:
  - 'the model-type mismatch raise (:83-85) — the only guard tying the study to the trained estimator — has ZERO test coverage (grep ''produced no valid trial for model'' in tests/: nothing). An hpo.models list that omits model.type currently passes test_run_hpo_end_to_end-style fixtures because their specs always include the trained type.'
- id: GATE-TRAIN-34
  phase: training-eval
  order: 36
  owner: src/broadway/training/trainer.py:17 build_model_pipeline()
  inputs:
  - PipelineConfig with experiment block
  - model_type registry key
  - params dict (bare + pre__ prefixed)
  outputs:
  - fitted sklearn Pipeline + TrainingResult{train_time_seconds} (via train() :52-69)
  transforms:
  - experiment-required guard → ValueError (:31-32; twin in train() :59-60)
  - 'param partition: bare params vs pre__<step>__<param> applied post-composition via set_params (:33-35, :44-48)'
  - 'seed pinning: random_state injected from cfg.experiment.random_state ONLY if registry allows it and params don''t carry one — explicit wins, YAML stays SSOT (:36-37; allowed_params src/broadway/training/models/registry.py:87-92)'
  - single composition shared by trainer AND HPO objective so preprocessing cannot drift between fit and search (docstring :26-28; consumed hpo.py:54)
  - unknown registry key → get_model ValueError listing valid keys (registry.py:73-74)
  touched_by:
  - pandas@2.3.3
  - scikit-learn@1.7.2
  validated_by:
  - tests/test_training_contracts.py::test_trainer_returns_training_result
  - tests/test_training_contracts.py::test_build_model_pipeline_applies_pre_params
  - tests/test_training_contracts.py::test_build_model_pipeline_seeds_estimator_from_experiment
  - tests/test_registry.py::test_get_model_unknown_key_raises
  - tests/test_hpo.py::test_pre_params_reach_pipeline_params
  - tests/test_hpo.py::test_hpo_trial_refits_preprocessing_each_trial
  if_changed:
  - the exact fitted artifact logged by GATE-TRAIN-39 log_model
  - CV model re-composition in evaluate (evaluate/module.py:135-139 reuses this builder)
  - signature inference at module.py:138-144
  findings:
  - both experiment-required raises (:31-32, :59-60) are unpinned — grep 'requires an experiment config' / 'model pipeline requires' in tests/ returns nothing (only the unrelated baseline 'requires an experiment binding' in test_structural_cleaning.py:434).
- id: GATE-TRAIN-35
  phase: training-eval
  order: 37
  owner: src/broadway/evaluate/module.py:82 run()
  inputs:
  - CFG-STEP-EVALUATE
  - val_features_file parquet (REQUIRED)
  - train_features_file parquet
  - ARTIFACT-TRAINING-RESULT
  - champion via models:/<dataset>@champion
  outputs:
  - ARTIFACT-EVALUATION-RESULT
  - lineage node evaluation:<dataset> (:166-171)
  - conditional champion promotion (:181-194)
  transforms:
  - 'config guard: dataset/experiment/evaluate/etl/train all required → ValueError (:83-84) + require_mode PREDICTION (:85)'
  - '_load_val_features: missing val file → FileNotFoundError ''evaluate requires a held-out set'' — NO silent train-fallback (:45-48), then engineered-contract re-validation (:50)'
  - '_load_training_result: missing file → FileNotFoundError ''run the train step first'' (:67-68); _load_candidate: empty artifact_path → ValueError ''model was not logged to MLflow'', else pyfunc load (:72-75)'
  - 'champion optional-by-design: get_champion None → warning appended to result.warnings, comparison deltas become None (:110-114, :132-133; compare_models zero-champion division guarded to None src/broadway/evaluate/comparison.py:19)'
  touched_by:
  - mlflow@3.15.1
  - pandas@2.3.3
  validated_by:
  - tests/test_evaluate_contracts.py::test_module_run_writes_evaluation_result
  - tests/test_evaluate_contracts.py::test_module_run_writes_baseline_comparison
  - tests/test_pyfunc_inference.py::test_champion_predicts_on_raw_frame
  - tests/test_pyfunc_inference.py::test_wrong_column_set_fails_loud
  - tests/test_pyfunc_inference.py::test_wrong_dtype_fails_loud
  if_changed:
  - GATE-TRAIN-37 ordering guarantees (this function owns the terminal block)
  - EvaluationResult schema consumers (tests/test_evaluate_contracts.py round-trips)
  - full-flow prediction step
  findings:
  - asymmetric loader hardening — _load_train_features (:55-61) lacks the missing-file guard its val twin has (:45-48); a deleted train parquet surfaces as a raw pyarrow FileNotFoundError from pd.read_parquet (:58) instead of the named loud failure pattern the module promises in its docstring (:3-5). No test covers the train-file-missing branch.
- id: GATE-TRAIN-36
  phase: training-eval
  order: 38
  owner: src/broadway/evaluate/metrics.py:23 compute_metrics()
  inputs:
  - y_true/y_pred arrays from holdout predict
  - CV folds via cross_validate (src/broadway/evaluate/validation.py:42)
  outputs:
  - rounded metric dicts persisted in ARTIFACT-EVALUATION-RESULT / ARTIFACT-MLFLOW-RUN
  transforms:
  - 'input finiteness gates: NaN/inf in y_true OR y_pred → ValueError BEFORE scoring (:28-31; CV twin checks X and y pre-fold validation.py:51-54)'
  - fixed 7-key regression vocabulary mae/rmse/r2/mape/max_error/median_ae/explained_var rounded to METRIC_DECIMALS=4 SSOT (:20, :32-42; CV scorer vocabulary mirrors it validation.py:12-20 incl. unprefixed-but-negated max_error :22-29)
  - 'cv_kind dispatch: TimeSeriesSplit deterministic (random_state intentionally unused :33-36), KFold shuffle+seeded (:37-38), unknown kind → ValueError (:39)'
  - 'fold aggregation: negation flip for _NEGATED_METRICS, per-fold round THEN mean THEN round (validation.py:69-80 — unweighted across unequal folds by construction)'
  touched_by:
  - numpy@2.5.2
  - scikit-learn@1.7.2
  validated_by:
  - tests/test_metrics_extended.py::test_compute_metrics_nan_raises
  - tests/test_metrics_extended.py::test_compute_metrics_inf_raises
  - tests/test_cross_validate.py::test_cross_validate_nan_in_x_raises_instead_of_persisting_nan_metrics
  - tests/test_cross_validate.py::test_cross_validate_inf_in_x_raises_instead_of_persisting_nan_metrics
  - tests/test_cross_validate.py::test_make_cv_unknown_kind_raises
  - tests/test_cross_validate.py::test_cross_validate_matches_hand_loop_golden
  - tests/test_cross_validate.py::test_mean_metrics_sign_and_rounding
  - tests/test_cross_validate.py::test_metric_vocabulary_parity_holdout_vs_cv
  - tests/test_evaluate_contracts.py::test_residual_summary
  if_changed:
  - promotion decision input values (GATE-TRAIN-37 reads target_metric from this dict)
  - MLflow-logged metrics keys
  - CV-vs-holdout parity tripwire
  findings:
  - OUTPUT finiteness is never gated — y_true containing exact zeros makes sklearn mean_absolute_percentage_error return inf, which round() keeps and flows silently into the metrics dict, MLflow, and the persisted JSON (:28-31 check inputs only; mape at :36). No test pins a zero-target MAPE case.
  - 'residual_summary mislabels signed semantics — ''mean_residual'' actually computes np.mean(np.abs(residuals)) (validation.py:63) and the test PINS the absolute-value behavior under the signed name (test_evaluate_contracts.py:160-167 asserts +1.0 for residuals [+1,-1,+1,-1], where signed mean is 0.0): consumers reading ''mean_residual'' as bias get |bias| instead, locked in by the pin.'
- id: GATE-TRAIN-37
  phase: training-eval
  order: 39
  owner: src/broadway/evaluate/promotion.py:6 should_promote()
  inputs:
  - candidate_metrics[target_metric]
  - champion_score (None when no champion)
  - CFG-STEP-EVALUATE.promotion_threshold (configs/step/evaluate.yaml:2 → 0.05)
  outputs:
  - '(promote: bool, reason: str) recorded in ARTIFACT-EVALUATION-RESULT; champion alias move on promote'
  transforms:
  - 'T-BUG-2 DECISION placement: decided BEFORE CV with pre-existing semantics (evaluate/module.py:121-130), EXECUTED only as the terminal stage AFTER EvaluationResult persistence + lineage record (:163-171 → promote block :181-186), so a raising CV/persist stage can never leave a promoted model with no evaluation artifact (tests/test_evaluate_contracts.py::test_module_run_promotes_only_after_persistence, ::test_module_run_cv_failure_promotes_nothing)'
  - no champion → promote UNCONDITIONALLY with reason (:7-8)
  - 'degradation: candidate > champion → refuse with formatted reason (:9-10); improvement must STRICTLY exceed threshold — equality never promotes (:11-14)'
  - 'registry failure stays soft: MlflowException during promote_candidate → warning appended AND persisted EvaluationResult REWRITTEN with the skip signal (:187-194)'
  touched_by: []
  validated_by:
  - tests/test_evaluate_contracts.py::test_should_promote_no_champion
  - tests/test_evaluate_contracts.py::test_should_promote_worse_candidate
  - tests/test_evaluate_contracts.py::test_compare_models_champion_none
  - tests/test_evaluate_contracts.py::test_compare_models_with_champion
  - tests/test_evaluate_contracts.py::test_module_run_cv_failure_promotes_nothing
  - tests/test_evaluate_contracts.py::test_module_run_promotes_only_after_persistence
  if_changed:
  - champion alias consumers (get_champion in evaluate + scripts/check_champion_manifest.sh bucket counts)
  - EvaluationResult.promote/reason fields
  - full-flow auto-promotion behavior
  findings:
  - 'lower-is-better is HARDCODED while the caller feeds an arbitrary target_metric — parameter names say rmse (should_promote(candidate_rmse, champion_rmse, threshold)) but run() passes cfg.evaluate.target_metric (:126-129), so flipping evaluate.yaml to r2 (higher-better) INVERTS every decision: candidate improvement reads as degradation and worse candidates clear the threshold. Only the no-champion and degradation branches are unit-pinned; the threshold-exceedance branch has no direct test at all.'
- id: GATE-TRAIN-38
  phase: training-eval
  order: 40
  owner: src/broadway/stats/robust.py:34 estimation_table()
  inputs:
  - any FITTED statsmodels regression results object
  - alpha (default 0.05)
  outputs:
  - coef/HC3_SE/CI_low/CI_high DataFrame
  transforms:
  - 'T-BUG-1 HC3 SELF-FIT: derives HC3 independently of how the input was fitted — robust = model.get_robustcov_results(''HC3'') internally (:54), reads bse/conf_int from THAT object (:56, :60), so labels are truthful by construction even for plain-OLS input (docstring :37-42)'
  - 'fail-loud type gate: object lacking get_robustcov_results → TypeError naming received type (:47-53)'
  - 'idempotency property: already-HC3 input reproduces its own bse exactly (pinned by tests/test_stats_robust.py::test_estimation_table_already_hc3_input_is_idempotent, T-BUG-1 review rider F2)'
  touched_by:
  - pandas@2.3.3
  validated_by:
  - tests/test_stats_robust.py::test_estimation_table_returns_four_columns
  - tests/test_stats_robust.py::test_estimation_table_already_hc3_input_is_idempotent
  - tests/test_stats_robust.py::test_estimation_table_plain_ols_still_yields_hc3_columns
  if_changed:
  - experiments/univariate/fare_amount_trip_distance/_ols_bp.py delegation wrapper (:117-120) and every HC3 CSV/result JSON downstream (31/32/33/29 result files)
  - stats/API.md contract (fit_robust cov_type='HC3')
  findings:
  - neighbors standardized_coefs (:67-82) and scenario_dollars (:85-97) index model.params[term] with NO fitted/TypeError gate — a KeyError on an unknown term or an unfitted-model attribute error is the only failure mode, unpinned by any test in tests/test_stats_robust.py beyond hand-computed happy paths (:152-192).
- id: GATE-TRAIN-39
  phase: training-eval
  order: 41
  owner: src/broadway/training/mlflow_utils.py:195 list_champions()
  inputs:
  - tracking_uri (file store auto-flagged via MLFLOW_ALLOW_FILE_STORE :202-204)
  - alias default 'champion'
  - logged runs/artifacts from the training path
  outputs:
  - ChampionArtifact records bucketed bare_model / pipeline_signature / ambiguous (:21-23)
  - manifest report + retirement verdict from scripts/check_champion_manifest.sh
  transforms:
  - 'setup_mlflow: non-URI tracking target flips the file-store env var (:55-56); unreachable http(s) store detected via connection-refused text markers (MLflow wraps refusals WITHOUT __cause__, :44-51) → RuntimeError with README hint (:60-76)'
  - 'log_dataset: logs dataset_id param ALWAYS; missing parquet source → warn-and-CONTINUE (:100-103); lineage record re-reads the parquet (:104) and float64-recasts int columns purely for metadata (:130) — second-read cost parked as backlog (DECISIONS.md D9; audits/A-DEAD-WEIGHT.md exclusions); LocalArtifactDatasetSource lookup miss → RuntimeError (:115-124), duplicate-name collision silently takes first (:125-129 declared accepted risk)'
  - 'log_model: cloudpickle serialization forced (skops false-positives on trees), explicit signature makes MLflow own the shape contract (:135-149)'
  - 'champion resolution: get_champion misses → warn + None (:152-159); promote_candidate ORDER register_model THEN set alias (:162-164) — the terminal-stage executor in GATE-TRAIN-37 depends on this pair'
  - 'classify_champion buckets from MLmodel metadata ONLY: sklearn+signature → pipeline_signature (:186-188), wrapped ModelPyFunc → bare_model (:190-191), everything else AMBIGUOUS with reason — unreadable metadata, signature-less sklearn (pre-Slice-3 path), no-flavor (:177-192)'
  - 'manifest script: report mode exits 0 ALWAYS (''the manifest is not a gate''); --strict turns bare_model|ambiguous non-empty into exit 1; usage errors exit 2 (scripts/check_champion_manifest.sh:11-13, :61-65, :19-31)'
  touched_by:
  - mlflow@3.15.1
  - pandas@2.3.3
  validated_by:
  - tests/test_mlflow_utils_extended.py::test_log_dataset_records_param_and_lineage
  - tests/test_mlflow_utils_extended.py::test_log_dataset_missing_source_warns_and_continues
  - tests/test_mlflow_utils_extended.py::test_logged_pipeline_reloads_and_predicts_raw_frame
  - tests/test_mlflow_utils_extended.py::test_log_metadata_records_metrics
  - tests/test_mlflow_utils_unreachable.py::test_unreachable_http_store_raises_clear_error
  - tests/test_training_contracts.py::test_setup_mlflow_logs_without_server
  - tests/test_champion_manifest.py::test_classify_champion_returns_buckets
  - tests/test_champion_manifest.py::test_manifest_lists_all_three_buckets
  - tests/test_champion_manifest.py::test_strict_exits_nonzero_when_bare_or_ambiguous
  - tests/test_champion_manifest.py::test_strict_exits_zero_when_only_pipeline_champions
  - tests/test_champion_manifest.py::test_unreadable_artifact_is_ambiguous_with_reason
  if_changed:
  - ModelPyFunc retirement condition (SKLEARN_PIPELINES.md Slice 4 decision 3; legacy loader kept only for old artifacts — src/broadway/training/models/pyfunc_wrapper.py:19)
  - GATE-TRAIN-37 promote_candidate call shape
  - champion manifest CI/consumers
  findings:
  - argv handling in check_champion_manifest.sh stops at position 3 — anything after '--strict' ($4+) is silently ignored (:26-31), so `check_champion_manifest.sh --tracking-uri <uri> --strict --json` reports success while dropping the trailing flag on the floor; no usage rejection, no test pins the extra-arg case.
  - log_dataset's StopIteration raise (:120-124) is the ONE untested raise site of this file — grep shows no test removing LocalArtifactDatasetSource from the registry; the warn-and-continue (:100-102) and duplicate-name-first (:125-129) branches ARE pinned/hermetically noted respectively.
- id: GATE-TRAIN-118
  phase: training-eval
  order: 42
  owner: 'src/broadway/baseline/module.py:75 run() (+ _git_commit() :20-27 subprocess-provenance leg)'
  inputs:
  - PipelineConfig baseline section + dataset frame
  outputs:
  - 'BaselineResult JSON saved :85 (save_result, out_path :84) + lineage node_id("baseline") with parents :87-91'
  transforms:
  - 'silent degrade CONFIRMED (packet G D4): the module HAS logger (:17) but the CalledProcessError/FileNotFoundError fallback returns "unknown" emitting NOTHING (:26-27) — artifacts carrying commit:"unknown" are indistinguishable from provenance-bearing ones'
  - 'CWD-dependent: rev-parse resolves against process CWD, never anchored to a repo root; the train-job image does not ship .git ⇒ deployed runs record "unknown" EVERY time, silently — provenance theater in precisely the lane the manifests provision'
  - wall-clock twin (created_at via _build_trace :30-36) is REPRO-TIME-FREEZE territory — cross-ref only, no duplicate custody claimed
  touched_by:
  - broadway.baseline.contracts save_result/load_result
  - broadway.lineage.records write_record
  validated_by: []
  if_changed:
  - ArtifactTrace.commit payload consumed downstream
  findings:
  - 'LAND with validated_by [] + FINDING/root (lawful combination, packet G D4). Fix shape ≤2 files when landed: fallback branch must logger.warning naming the exception class + record monkeypatched CalledProcessError→assert value AND warning test pins (FileNotFoundError twin). Raise-on-missing REJECTED: containers legitimately lack .git; baseline must not die for metadata. Deletion-first applied honestly: deleting _git_commit strips a contract field consumed via ArtifactTrace — rejected (B#14 keep-with-rationale class).'
- id: GATE-TRAIN-119
  phase: training-eval
  order: 43
  owner: 'src/broadway/causal/module.py:18 run()'
  inputs:
  - cfg.causal design params + canonical frame
  outputs:
  - 'causal design artifact save_design(:35) + lineage record node_id("causal") with parents [baseline, analysis] (:37-42)'
  transforms:
  - 'design-semantics step custody; anchors exact at HEAD (packet G D6): run() :18, save_design(design, out_path) :35, write_record :37-42'
  touched_by: []
  validated_by: []
  if_changed:
  - causal design artifact schema
  findings: []
- id: GATE-TRAIN-120
  phase: training-eval
  order: 44
  owner: 'src/broadway/training/mlflow_utils.py:47-51 _CONNECTION_REFUSED_MARKERS/_is_unreachable_http_store connectivity-marker net'
  inputs:
  - tracking URI
  outputs:
  - 'named RuntimeError with README hint (:63, :75-76)'
  transforms:
  - 'marker net at HEAD: {"Connection refused", "Failed to establish a new connection", "Max retries exceeded"} (:47-51) wrapped-refusal detection via _is_unreachable_http_store (:66-72); setup_mlflow except-branch raises RuntimeError(_unreachable_server_hint(...)) (:60-63)'
  - 'X1 AMENDMENT (binding law, packet H): widen the net with NAMED additions each landing as a NEW TEST PARAM — name-resolution failure, connection reset, read timeout; KEEP the negative path pinned (non-matching MlflowException re-raises RAW — over-broad matching would swallow genuine server errors as connectivity); worker preflight stays its own lane'
  touched_by:
  - mlflow.exceptions.MlflowException
  validated_by:
  - tests/test_mlflow_utils_unreachable.py::test_unreachable_http_store_raises_clear_error
  if_changed:
  - marker tuple membership (each addition must arrive with its test param)
  findings:
  - 'code-side widening is recorded here as the gate''s binding requirement; the marker tuple still carries the three HEAD literals until the widening batch lands.'
- id: GATE-STATS-40
  phase: stats
  order: 45
  owner: src/broadway/stats/module.py:24 run()
  inputs:
  - PipelineConfig (optional sections cfg.dataset, cfg.stats; required cfg.analysis)
  - AnalysisMode contract
  outputs:
  - ValueError "stats step requires dataset and stats config" (:24-25)
  - AnalysisMode.HYPOTHESIS enforcement via require_mode (:26)
  - ValueError "hypothesis mode requires a 'hypothesis' block (group_column, group_values)" (:28-29)
  transforms:
  - None-guard both optional config sections BEFORE any IO or dispatch
  - 'mode contract: stats runs only under hypothesis mode (require_mode)'
  - hypothesis-block presence re-checked inside the step because model_copy bypasses the contract validator (pin rationale tests/test_stats_module.py:230-233)
  touched_by:
  - logging
  - broadway.analysis.contracts.require_mode/AnalysisMode
  - broadway.config.schema.PipelineConfig
  validated_by:
  - tests/test_stats_module.py::test_stats_requires_dataset_and_stats_config
  - tests/test_stats_module.py::test_stats_hypothesis_block_required
  if_changed:
  - either guard message string (asserted via pytest.raises match=)
  - AnalysisMode.HYPOTHESIS requirement
  - CLI dispatch wiring src/broadway/cli.py:151-165
  findings: []
- id: GATE-STATS-41
  phase: stats
  order: 46
  owner: src/broadway/stats/module.py:31 run() data leg
  inputs:
  - canonical parquet at canonical_path(cfg.dataset, cfg.environment) when sample is None; else SampleSpec.path (+ sample.column_mapping logical→source remap of group_column :41-42)
  outputs:
  - 'FileNotFoundError "canonical dataset not found: … — run the etl step first" (:32-33)'
  - 'FileNotFoundError "sample dataset not found: …" (:37-38)'
  - ValueError "group column '<g>' not found in data" (:43-44)
  - ARTIFACT-STATS-PLAN written via save_plan (:58-61 → src/broadway/stats/plan.py:31-32 write_text)
  - lineage node_id("stats", analysis.name) parents [node_id("baseline",…), node_id("analysis",…)] (:66-73)
  transforms:
  - existence checked BEFORE pd.read_parquet on whichever leg was selected
  - out_dir = Path(cfg.stats.output_dir); mkdir(parents=True, exist_ok=True) before write (:58-59)
  - plan stamped with analysis_goal/sample_name/sample_role via model_copy (:51-57)
  touched_by:
  - pandas.read_parquet
  - pathlib.Path
  - broadway.data.loader.canonical_path
  - broadway.stats.plan.save_plan/load_plan
  - broadway.lineage.ids.node_id
  - broadway.lineage.records.write_record
  validated_by:
  - tests/test_stats_module.py::test_stats_missing_canonical_raises
  - tests/test_stats_module.py::test_stats_missing_sample_raises
  - tests/test_stats_module.py::test_stats_group_column_not_in_data_raises
  - tests/test_stats_module.py::test_stats_run_writes_plan_file
  - tests/test_plan.py::test_save_plan_writes_indented_json_with_null
  - tests/test_loud_failures.py::test_load_plan_malformed_raises
  if_changed:
  - ARTIFACT-STATS-PLAN name/location (configs/step/stats.yaml:2-3)
  - node_id("stats") kind or parents
  - guard message strings
  findings:
  - 'load_plan (src/broadway/stats/plan.py:35) has ZERO production callers — repo-wide src/ grep finds save_plan called only at module.py:61; the plan JSON is write-only in this band, so a reader-side schema drift would surface nowhere except tests (tests/test_loud_failures.py:31). Strict-purity note: this gate is a deliberate IO boundary (step entry), not a library violation.'
- id: GATE-STATS-42
  phase: stats
  order: 47
  owner: src/broadway/stats/groups.py:9 build_declared_groups()
  inputs:
  - df
  - source_group_column
  - declared ordered group_values
  - target column name
  outputs:
  - (dict[str, np.ndarray] containing EVERY declared value in declared order, sorted absent list); a value with no matching rows OR all-NaN target maps to size-0 array (:28-31)
  - absent = sorted size-0 names (:32)
  transforms:
  - per-value extraction df[col==g][target].dropna().to_numpy() — NaN targets demote a group to absent
  - 'NEVER RAISES: documented invariant (:22-26) is that a NON-EMPTY absent list MUST be treated as error by the caller'
  - 'enforcement lives at callers: stats entry raises standard vocabulary ''declared groups absent from data: {absent}'' at src/broadway/stats/module.py:48-49 (T-BUG-4 eee33d7 — was silent k-1 ANOVA); baseline raises partial absence at src/broadway/baseline/hypothesis.py:15-16 while total absence keeps dedicated ''no groups present'' raise (:17-18); walkthrough DELIBERATELY keeps size-0 arrays (src/broadway/timeline/runners.py:107 discards _absent_groups) relying on downstream validate_groups ''group X is empty'''
  touched_by:
  - numpy
  - pandas
  - broadway.stats.module (:45-47)
  - broadway.stats.describe (:50, :107)
  - broadway.baseline.hypothesis (:13)
  - broadway.timeline.runners (:107)
  validated_by:
  - tests/test_stats_module.py::test_stats_run_absent_declared_group_fails_loud
  - tests/test_stats_module.py::test_baseline_hypothesis_absent_declared_group_raises
  - tests/test_describe.py::test_describe_groups_and_absent
  - tests/test_walkthrough.py::test_run_describe_flags_imbalance_and_absent_groups
  if_changed:
  - standard vocabulary string "declared groups absent from data:" (shared stats/baseline, pinned by two test files)
  - absent-list sort order
  - never-raises contract
  - per-surface enforcement choice
  findings:
  - 'the "constructor ValueError on partial absence" is an INVARIANT ENFORCED ONLY BY CONVENTION — groups.py structurally cannot raise, so every future caller re-decides whether to fail loud; the walkthrough''s keep-arrays exception is sanctioned by docstring (:25-26) but nothing mechanical distinguishes it from a silent-skip regression like the one T-BUG-4 fixed. Second wobble: describe''s plot pass re-runs the builder and ignores absent AGAIN (src/broadway/stats/describe.py:107) — the known redundant-second-construction rider (agents/ledger/FIXES.md:111-112).'
- id: GATE-STATS-43
  phase: stats
  order: 48
  owner: src/broadway/stats/module.py:50 run() floor binding
  inputs:
  - groups from GATE-STATS-42
  - cfg.stats.min_rows_for_sampling
  outputs:
  - 'small_group_threshold argument into run_anova; surfaces as AnalysisPlan.threshold_context.any_small_group + imbalance_ratio (src/broadway/stats/anova.py:60, 68-71) and warning "underpowered: small group(s)" when ANY group < floor'
  transforms:
  - 'T-BUG-4 (eee33d7) binding AT ENTRY: run_anova(groups, small_group_threshold=cfg.stats.min_rows_for_sampling) — pre-fix the key was dead config (agents/ledger/DECISIONS.md:81-83)'
  - 'config chain: authored configs/step/stats.yaml:4 (=10000) → StatsStep.min_rows_for_sampling src/broadway/config/schema.py:253 (required int, no default) → bound here; second out-of-band consumer project/data.py:61 (MIN_ROWS_FOR_SAMPLING)'
  - alpha is NOT bound on this surface — stays library default 0.05 (anova.py:80)
  touched_by:
  - broadway.config.schema.StatsStep
  - configs/step/stats.yaml
  - project/data.py
  - broadway.stats.anova.run_anova
  validated_by:
  - tests/test_stats_module.py::test_stats_run_small_group_threshold_binds_to_config
  - tests/test_config.py::test_stats_config_fields
  if_changed:
  - key name min_rows_for_sampling
  - authored value 10000
  - threshold_context key names ("any_small_group"/"imbalance_ratio" asserted verbatim in tests/test_stats_module.py:191,197)
  findings:
  - 'FLOOR BINDS ON ONE SURFACE ONLY — walkthrough omnibus (src/broadway/timeline/runners.py:315-319) passes ONLY alpha (configs/step/walkthrough.yaml:5 significance_alpha=0.05 via _thresholds(), runners.py:53-54) and never small_group_threshold, so its underpowered warnings judge groups against library-default 30 (anova.py:80,122,165) while the stats step judges against 10000: same warning string, two effective floors (the recorded floor-kwarg semantic-tension rider, agents/ledger/FIXES.md:115-116).'
- id: GATE-STATS-44
  phase: stats
  order: 49
  owner: src/broadway/stats/guards.py:8 validate_groups()
  inputs:
  - dict[str, np.ndarray] group arrays
  outputs:
  - 'list[str] of per-group zero-variance warnings (non-fatal); ValueErrors: "at least two groups required, got N" (:9-10)'
  - '"group ''<name>'' is empty" (:15-16)'
  - '"group ''<name>'' contains non-finite values" (:17-18)'
  - '"group ''<name>'' has fewer than 2 observations" (:19-20)'
  - '"all groups have zero variance — no variation to compare" (:26-27)'
  transforms:
  - 'np.asarray(dtype=float) cast then per-group audit: finiteness, ddof=1 variance'
  - zero-variance is WARN-per-group (:21-23) but ALL-groups-zero escalates to raise (:26-27)
  - 'SHARED FLOOR: invoked by run_anova/run_welch/run_kruskal (anova.py:82,124,167) and run_levene/check_normality (assumptions.py:12,22); post_hoc and effect_size do NOT route through it (they degrade silently — size<2 → 0.0 fallbacks, see GATE-STATS-45/47)'
  touched_by:
  - numpy
  - broadway.stats.anova
  - broadway.stats.assumptions
  validated_by:
  - tests/test_anova.py::test_run_anova_requires_two_groups
  - tests/test_anova.py::test_anova_empty_group_raises
  - tests/test_anova.py::test_anova_zero_variance_group_warns
  - tests/test_anova.py::test_anova_all_zero_variance_raises
  - tests/test_anova.py::test_anova_non_finite_raises
  - tests/test_assumptions.py::test_check_normality_empty_group_raises
  if_changed:
  - any error/warning substring
  - ddof=1 variance convention
  - warn-vs-raise split for zero variance
  findings: []
- id: GATE-STATS-45
  phase: stats
  order: 50
  owner: src/broadway/stats/anova.py:79 run_anova() family (run_welch :121, run_kruskal :164)
  inputs:
  - groups
  - 'alpha (default 0.05 — callers: NONE bind it at stats step, walkthrough binds significance_alpha)'
  - small_group_threshold (default 30 — bound ONLY by module.py:50)
  - sizes via _group_sizes (:18-19)
  outputs:
  - AnalysisPlan{statistics.statistic/p_value, effect_sizes eta_squared+omega_squared (anova/welch) or epsilon_squared (kruskal), threshold_context.imbalance_ratio/any_small_group, passed=bool(p<alpha), next_step="posthoc" if passed} (_build_plan :45-76)
  transforms:
  - scipy stats.f_oneway (:87) / closed-form Welch (:22-42) / scipy kruskal (:173)
  - '''underpowered: small group(s)'' warning when any size < small_group_threshold (:97-98, :140-141, :183-184)'
  - HARDCODED imbalance ratio > 1.5 → 'n imbalance' warning (:99-100, :142-143) — a literal, not a parameter
  - Welch escalates validate_groups zero-variance warnings into ValueError 'Welch's ANOVA requires non-zero within-group variance in every group' (:125-126)
  - effect-size numeric guards SILENTLY return 0.0 on degenerate denominators (eta_squared :10-11, omega_squared :18-19, epsilon_squared :24-27 clamps to [0,1], cohens_d :33-34/:38-39) and group_imbalance returns 1.0 for <2 groups or a zero-size group (effect_size.py:53-58)
  touched_by:
  - scipy.stats
  - numpy
  - broadway.stats.guards
  - broadway.stats.effect_size
  - broadway.stats.plan.AnalysisPlan
  - broadway.timeline.runners.run_omnibus (:315-319)
  - src/broadway/stats/module.py:50
  validated_by:
  - tests/test_anova.py::test_run_anova_passes_and_has_effect_sizes
  - tests/test_anova.py::test_run_anova_not_passed_when_means_equal
  - tests/test_anova.py::test_run_welch_valid_plan
  - tests/test_anova.py::test_welch_zero_variance_raises
  - tests/test_anova.py::test_run_kruskal_valid_plan
  - tests/test_effect_size.py::test_eta_squared_hand_computed
  - tests/test_effect_size.py::test_epsilon_squared_clamped_negative_to_zero
  - tests/test_effect_size.py::test_group_imbalance_edge_cases
  if_changed:
  - default alpha/small_group_threshold signatures
  - the 1.5 imbalance literal
  - threshold_context key names
  - next_step routing string "posthoc"
  - effect-size silent-fallback policy
  findings:
  - 'GOLDEN-FLOAT STATUS — glob tests/*golden* matches NOTHING on this tree; every repo-wide "golden" hit belongs to other phases (tests/test_cross_validate.py:27 sklearn-KFold golden dict, tests/test_transformers.py:31/:87 encoding goldens, tests/test_decision5_columns.py:104, project/tests/test_ml_pipeline.py:153). Stats float assertions are pytest.approx RELATIVE hand-computed pins (tests/test_effect_size.py:17/:21/:34, tests/test_anova.py:29) — approx everywhere, exact nowhere, EXCEPT ONE bitwise pin: tests/test_stats_robust.py:118-121 np.testing.assert_array_equal on HC3 SE/CIs (T-BUG-1 idempotency rider). NO golden pin exists for any statistic/p_value/shapiro output, so scipy-version drift in this band''s headline numbers is undetected by design.'
- id: GATE-STATS-46
  phase: stats
  order: 51
  owner: src/broadway/stats/assumptions.py:19 check_normality() (companion run_levene :11)
  inputs:
  - groups; shapiro_max_n default 5000 (:20); run_levene(groups) plain
  outputs:
  - per-group {skew, kurtosis, shapiro_p} (:31-35); levene {statistic, p_value} (:15-16); ValueError "normality checks require non-constant groups" (:23-24); ValueError "Levene's test requires non-zero variance in every group" (:13-14)
  transforms:
  - validate_groups first on BOTH functions (:12, :22)
  - 'SEEDED SUBSAMPLE: when arr.size > shapiro_max_n → np.random.default_rng(0).choice(arr, size=shapiro_max_n, replace=False) (:28-30) — fixed seed literal 0, drawn ONCE per group before skew/kurtosis/shapiro so all three statistics see identical rows; result is deterministic given input but ORDER-SENSITIVE to row arrangement'
  - 'caller pinning: walkthrough run_normality/run_variance call check_normality(groups)/run_levene(groups) with NO overrides (src/broadway/timeline/runners.py:199, :270) — the 5000 cap is the operative threshold on every real path; no config key exists for it'
  touched_by:
  - scipy.stats (levene/skew/kurtosis/shapiro)
  - numpy.random.default_rng
  - broadway.stats.guards
  validated_by:
  - tests/test_assumptions.py::test_check_normality_caps_subsample
  - tests/test_assumptions.py::test_levene_zero_variance_raises
  - tests/test_assumptions.py::test_run_levene_returns_statistic_and_p_value
  - tests/test_assumptions.py::test_check_normality_returns_per_group_stats
  if_changed:
  - seed literal 0 (:29)
  - shapiro_max_n default 5000
  - subsample-vs-refuse policy for oversized groups
  - zero-variance raise strings
  findings:
  - 'threshold-with-no-caller-pinning — shapiro_max_n=5000 AND seed 0 are unconfigurable literals: neither the stats step nor the walkthrough passes them and no config key (StatsStep or WalkthroughConfig) exposes them, so tuning normality behavior for wide groups requires a code edit invisible to config-diff review. Seeding itself is PRESENT and deterministic (contract question answered: yes, default_rng(0)).'
- id: GATE-STATS-47
  phase: stats
  order: 52
  owner: src/broadway/stats/post_hoc.py:11 games_howell()
  inputs:
  - long df
  - dv
  - between
  - small_n default 30 (:11); SOLE production caller timeline/runners.py:396 passes dv/between only
  outputs:
  - pingouin pairwise_gameshowell frame augmented with cohens_d / hedges_g / effect_size_note columns (:29-31)
  transforms:
  - 'note switch per pair: ''use hedges_g'' when EITHER side n < small_n else ''use cohens_d'' (:24-27) — advisory strings only, pingouin p-values unaffected'
  - pairwise effect sizes from grouped raw arrays via cohens_d/hedges_g (effect_size.py:30-49, incl. their silent 0.0/degenerate fallbacks)
  touched_by:
  - pingouin
  - pandas
  - broadway.stats.effect_size.cohens_d/hedges_g
  - broadway.timeline.runners.run_posthoc (:396)
  validated_by:
  - tests/test_post_hoc.py::test_games_howell_columns
  - tests/test_post_hoc.py::test_games_howell_effect_size_note_small_n
  - tests/test_post_hoc.py::test_games_howell_effect_size_note_large_n
  if_changed:
  - '"use hedges_g"/"use cohens_d" note strings (rendered into reports/timeline evidence)'
  - small_n default
  findings: []
- id: GATE-STATS-48
  phase: stats
  order: 53
  owner: src/broadway/stats/robust.py:47 estimation_table() (regression/diagnostics/time_series/baseline cluster)
  inputs:
  - fitted statsmodels results object exposing get_robustcov_results
  - 'alpha default 0.05 (:34); siblings: outlier_mask(threshold) :17'
  - winsorize(cap_quantile) :25
  - modified_zscore MAD-guard :12-13
  - fit_ols/formula + fit_robust(cov_type="HC3") (regression.py:11-16)
  - bp_test/jb_test/durbin_watson wrappers (diagnostics.py:20-31)
  - durbin_watson_test/plot_acf(lags) (time_series.py:13-21)
  - train_lgbm(**params)/evaluate(tail_quantile) (baseline.py:11-23)
  outputs:
  - TypeError "estimation_table requires a fitted statsmodels regression results object exposing get_robustcov_results…" (:48-53) — the ONLY raising guard in this cluster; coef/HC3_SE/CI_low/CI_high table derived by INTERNAL get_robustcov_results("HC3") re-fit (:54-56) so labels are truthful regardless of how the input was fitted (T-BUG-1 d056164, DECISIONS.md context :72); DiagnosticResult model (diagnostic_models.py:6-11); PNG artifacts via savefig (diagnostics.py:49, :84; time_series.py:21)
  transforms:
  - HC3 covariance re-derived inside the function (:54) — idempotent for already-HC3 fits (bitwise pin)
  - modified z-score returns all-zeros when MAD == 0 (robust.py:12-13)
  - winsorize clips at per-column quantile on a copy (:25-31)
  - diagnostics wrappers are pure float casts over statsmodels het_breuschpagan/jarque_bera/durbin_watson
  - matplotlib.use('Agg') runs at IMPORT time (diagnostics.py:7, describe.py:8) — process-global backend mutation as an import side effect of pure-library modules
  touched_by:
  - statsmodels
  - matplotlib (Agg)
  - lightgbm
  - sklearn.metrics
  - numpy
  - pandas
  - broadway.stats.diagnostic_models
  - broadway.viz.palette_colors — INDIRECT CONFIG READ (see FINDING)
  validated_by:
  - tests/test_stats_robust.py::test_estimation_table_returns_four_columns
  - tests/test_stats_robust.py::test_estimation_table_already_hc3_input_is_idempotent
  - tests/test_stats_robust.py::test_estimation_table_plain_ols_still_yields_hc3_columns
  - tests/test_stats_robust.py::test_outlier_mask_flags_only_configured_column_extremes
  - tests/test_stats_robust.py::test_winsorize_caps_exactly_at_quantile_and_copies
  - tests/test_regression.py::test_fit_robust_returns_hc3_covariance
  - tests/test_regression.py::test_bp_jb_returns_six_keys
  - tests/test_diagnostics.py::test_mean_specification_diagnostic_returns_typed_result
  - tests/test_time_series.py::test_plot_acf_saves_png
  - tests/test_effect_size.py::test_cohens_d_empty_array_returns_zero
  if_changed:
  - TypeError message (match="get_robustcov_results")
  - '"HC3" cov_type literal'
  - Agg backend setup
  - DiagnosticResult field set
  findings:
  - 'PURITY + UNPINNED THRESHOLDS — (a) indirect config+IO read inside stats: diagnostics.py:37 calls viz.palette_colors(1) whose default-palette branch (src/broadway/viz.py:32-35) calls load_viz_config() which OPENS configs/step/viz.yaml from disk (src/broadway/config/viz.py:79-83); describe.py:112 has the same reach — the "pure library" thus depends on a config file through a plotting helper. (b) CORRECTED caller map (supersedes the earlier no-production-caller claim): production callers EXIST for bp_jb (project/scripts/08_ols_residuals_diagnostics.py:45, project/scripts/09_log_target_ols.py:35, project/scripts/11_interaction_ols.py:72), fit_ols (project/scripts/08_ols_residuals_diagnostics.py:82, project/scripts/09_log_target_ols.py:62+70, project/scripts/10_durbin_watson_time.py:41, project/scripts/11_interaction_ols.py:38+45), fit_robust (project/scripts/09_log_target_ols.py:74), durbin_watson_test (project/scripts/10_durbin_watson_time.py:43), plot_acf (project/scripts/10_durbin_watson_time.py:54), train_lgbm (project/scripts/12_lgbm_baseline.py:59), evaluate (project/scripts/12_lgbm_baseline.py:72); genuinely caller-less outside tests: outlier_mask, winsorize, estimation_table, standardized_coefs, scenario_dollars; whether project/scripts counts as a production surface awaits an explicit ruling (agents/ledger/arbitration/2026-08-24/00-resolutions.md §6), so threshold pins remain provisional. Dead-in-src status now applies only to the five caller-less functions above — flagged per contract, severity low (tests exercise every knob).'
- id: GATE-STATS-49
  phase: stats
  order: 54
  owner: src/broadway/stats/describe.py:121 run()
  inputs:
  - PipelineConfig (cfg.dataset/cfg.stats/cfg.analysis)
  - SampleSpec REQUIRED (no canonical leg, unlike GATE-STATS-41)
  - sample.column_mapping logical→source resolution (:132)
  - cfg.dataset.target
  outputs:
  - ValueError "stats describe requires dataset and stats config" (:122-123)
  - require_mode HYPOTHESIS (:124)
  - hypothesis-block ValueError (:125-126)
  - FileNotFoundError "sample dataset not found" (:127-129)
  - ValueError "group column '<g>' not found in sample data" (:134-135)
  - ARTIFACT-DESCRIBE-SUMMARY describe.json via model_dump_json (:137-140)
  - lineage node_id("describe", analysis.name) parents [node_id("etl",…), node_id("analysis",…)] (:143-147)
  - group figures PNG (:98-118, savefig :117)
  transforms:
  - 'EVIDENCE-ONLY CORE: describe() emits imbalance_ratio round-4dp (:62, :69) with an explicit NO balanced/unbalanced VERDICT — banned tokens pinned by test (GroupSummary.model_fields scan); n==0 declared groups retained as GroupStat(n=0, mean=None, std=None) (:54-55); n==1 std pinned to NaN mirroring pandas ddof=1 (:57-59); proportions guarded for total_n==0 (:63)'
  - absent (n=0) groups annotated red on the boxplot (_draw_group_distribution :81-83)
  - figure pass re-runs build_declared_groups ignoring absent (:107) — redundant second construction (FIXES.md rider)
  - 'IO confined to entry: read_parquet (:130), mkdir + write_text (:138-140), savefig (:117)'
  touched_by:
  - pydantic (extra=forbid models :25-45)
  - pandas
  - matplotlib Agg (:8)
  - broadway.stats.groups.build_declared_groups
  - broadway.viz.palette_colors (:112 — indirect viz.yaml disk read, see GATE-STATS-48 FINDING)
  - broadway.lineage.records.write_record
  - reports.describe.GroupSummary consumer
  - CLI dispatch src/broadway/cli.py:156-160
  validated_by:
  - tests/test_describe.py::test_describe_run_requires_dataset_or_stats_config
  - tests/test_describe.py::test_describe_run_requires_hypothesis_block
  - tests/test_describe.py::test_describe_run_missing_sample_raises
  - tests/test_describe.py::test_describe_run_missing_source_column_raises
  - tests/test_describe.py::test_describe_single_observation_group_std_is_nan
  - tests/test_describe.py::test_describe_no_verdict
  - tests/test_describe.py::test_describe_run_writes_artifacts
  if_changed:
  - describe.json filename (:139)
  - GroupSummary field set (extra=forbid breaks additive drift loudly)
  - node_id("describe") kind/parents
  - verdict-ban vocabulary
  - NaN-for-n=1 std pin
  findings: []
- id: GATE-STATS-113
  phase: stats
  order: 55
  owner: 'src/broadway/stats/guards.py:8 validate_groups() + sample-size floor config'
  inputs:
  - sample parquet + declared group_values incl Staten Island
  outputs:
  - loud unreachable-group verdict instead of vacuous comparison
  transforms:
  - 'small-group reachability floor at sample scale: diagnostic samples cannot reach declared groups (full-data Staten Island n=84 — agents/notes/trust.md, LEARN.md:28)'
  - walkthrough deliberately keeps size-0 arrays (STATS-42 finding) relying on these guards — the floor is what makes absence LOUD instead of vacuous
  - 'CO-TENANCY with GATE-STATS-44 DECLARED (same symbol guards.py:8 validate_groups, distinct aspects): STATS-44 owns per-group statistical validity law (variance/finiteness/min-2 for the anova library path); 113 owns the SAMPLE-SCALE REACHABILITY aspect (declared groups unreachable at diagnostic sample sizes) — scanner cannot express aspect splits, so the mechanical flag is known-accepted'
  touched_by:
  - broadway.stats.groups.build_declared_groups
  validated_by:
  - tests/test_walkthrough.py::test_run_describe_flags_imbalance_and_absent_groups
  if_changed:
  - validate_groups return contract consumed by describe/walkthrough
  findings:
  - 'source FINDING-D3 (courtesy-verified packet F #22: guards.py:8 exact, test at test_walkthrough.py:751 exists).'
- id: GATE-TLINE-50
  phase: timeline-lineage
  order: 56
  owner: src/broadway/timeline/models.py:31 AnalysisStep()
  inputs:
  - step_id/order/question/kind from configs/flow/hypothesis_walkthrough.yaml
  - runner-computed result_summary dicts
  - StepStatus enum (models.py:9)
  - FigureRef (models.py:24)
  - runners.now_iso() timestamp
  outputs:
  - one validated AnalysisStep record per analysis step; serialized by timeline/module.save_step to artifacts/timeline/<analysis>/steps/<step_id>.json; consumed by suggest.py
  - reports/results.py:244 write_results()
  - reports/timeline.py:16 render_timeline()
  - reports/index.py:61 render_dashboard()
  transforms:
  - 'pydantic v2 model, extra=forbid: raw runner kwargs -> typed step-of-record with status NOTE/WARNING/FAILED semantics carried in result_summary+ramification free dicts/strings (result_summary: dict is unvalidated payload)'
  - figures list[str]->list[FigureRef] with reports/-relative paths
  touched_by:
  - src/broadway/timeline/walkthrough.py:106 (_handle_failure builds FAILED step)
  - src/broadway/timeline/walkthrough.py:384 (save_step of executor result)
  - src/broadway/timeline/runners.py:158/:243/:285/:365/:416/:513 (six constructors)
  - src/broadway/timeline/suggest.py:295 (by_id lookup)
  - src/broadway/reports/results.py + reports/timeline.py + reports/index.py (renderers)
  validated_by:
  - tests/test_timeline.py::test_step_roundtrip
  - tests/test_timeline.py::test_figure_ref_roundtrip
  - tests/test_timeline.py::test_step_without_figures_defaults_to_empty
  - tests/test_timeline.py::test_step_figures_roundtrip
  - tests/test_timeline.py::test_step_status_has_note_and_failed
  if_changed:
  - rerun tests/test_timeline.py
  - tests/test_walkthrough.py
  - tests/test_lineage_scope.py consumers of step JSON shape
  - regenerate reports/ renderers against new field names
  findings: []
- id: GATE-TLINE-51
  phase: timeline-lineage
  order: 57
  owner: src/broadway/timeline/models.py:50 AnalysisDecision()
  inputs:
  - kind restricted to Literal["omnibus","posthoc"]
  - status pinned to Literal["resolved"]
  - method string from configs/step/walkthrough.yaml decisions.<kind>.methods
  - reason list
  - parents list from walkthrough.yaml
  - decided_at ISO stamp
  outputs:
  - AnalysisDecision record-of-record; persisted as artifacts/timeline/<analysis>/decisions/<id>.json via timeline/module.save_decision; read back by walkthrough._resolved_decision (walkthrough.py:64-68)
  - _warn_stale_decisions (walkthrough.py:192-201)
  - suggest.suggest_next (suggest.py:312 decided_ids)
  - all three report renderers
  transforms:
  - pydantic extra=forbid model freezing a human method-choice into an immutable 'resolved' decision whose id == kind (one live decision per kind; re-decide overwrites the same file)
  - reason str -> [reason] single-element list at construction (decide.py:41)
  touched_by:
  - src/broadway/timeline/decide.py:35 (sole constructor)
  - src/broadway/timeline/module.py:43 save_decision()
  - src/broadway/cli.py:183-184 (decide command build+save)
  - src/broadway/timeline/walkthrough.py:65/:194 (load side)
  validated_by:
  - tests/test_timeline.py::test_decision_roundtrip
  - tests/test_timeline.py::test_decision_persistence_roundtrip
  - tests/test_decide.py::test_record_writes_valid_decision
  if_changed:
  - rerun tests/test_decide.py
  - tests/test_timeline.py
  - tests/test_walkthrough.py (gate-pass/resume logic keys on status=="resolved")
  findings: []
- id: GATE-TLINE-52
  phase: timeline-lineage
  order: 58
  owner: src/broadway/timeline/decide.py:21 record()
  inputs:
  - analysis.name from AnalysisContract (hypothesis mode enforced upstream at cli.py:182 require_mode)
  - kind
  - method
  - reason from `ds-pipeline decide` args
  - allowed-method vocabulary from configs/step/walkthrough.yaml:7-13 via sequence.load_walkthrough_config()
  outputs:
  - validated AnalysisDecision ready for module.save_decision; ValueError on unknown kind (decide.py:28-29) or out-of-vocabulary method (decide.py:30-34)
  transforms:
  - 'ALLOWED_METHODS fork: user-supplied method checked against YAML allowlist spec.methods before any record exists'
  - kind -> question text resolved from flow yaml decide_<kind> step (decide.py:14-18 _question_for)
  - spec.parents copied verbatim as decision.parents (decide.py:43)
  touched_by:
  - src/broadway/cli.py:183 (only production caller)
  - tests/test_decide.py
  - indirectly walkthrough gates that consume the recorded decision
  validated_by:
  - tests/test_decide.py::test_record_rejects_invalid_method
  - tests/test_decide.py::test_record_rejects_unknown_kind
  - tests/test_decide.py::test_record_writes_valid_decision
  - tests/test_decide.py::test_decisions_config_and_step_actions
  if_changed:
  - rerun tests/test_decide.py; reconcile walkthrough.yaml decisions.*.methods with runners.py dispatch sets; update walkthrough.py:166/:182 indexing
  findings:
  - the method vocabulary is double-owned and never cross-validated — the record-time allowlist is configs/step/walkthrough.yaml:9/:12, but the execute-time forks dispatch on a SECOND, independent hardcoded set (runners.py:314-321 {"anova","welch","kruskal"}; runners.py:394-395 {"games_howell"}). Agreement holds only because tests pin the YAML content (tests/test_decide.py:56); adding a plausible method to walkthrough.yaml yields a cleanly "resolved" decision that later crashes run_omnibus with ValueError (runners.py:321) and lands as a FAILED step via walkthrough._handle_failure (walkthrough.py:94-123) — the gate validates admission, not executability.
  - the executor fork accepts ANY `decide_*` flow-step id as a kind without validating it against WalkthroughConfig.decisions (walkthrough.py:328 removeprefix), prints a `ds-pipeline decide --kind {kind}` prescription for it (walkthrough.py:353-355) that decide.record will then reject ("unknown decision kind", decide.py:28-29); meanwhile _print_decision_required/_print_posthoc_decision_required hard-index thresholds.decisions["omnibus"]/["posthoc"] (walkthrough.py:166, walkthrough.py:182) -> KeyError if the YAML drops a kind, whereas this gate tolerates a missing kind via .get (decide.py:27). The two ends of the same fork disagree on how a bad kind fails.
- id: GATE-TLINE-53
  phase: timeline-lineage
  order: 59
  owner: src/broadway/timeline/module.py:19 save_step()
  inputs:
  - validated AnalysisStep / AnalysisDecision objects
  - BROADWAY_TIMELINE_DIR env override (module.py:8, default artifacts/timeline)
  outputs:
  - artifacts/timeline/<analysis>/steps/<step_id>.json and .../decisions/<decision.id>.json (model_dump_json indent=2); read back by load_step/load_steps/load_decision/load_decisions which feed every gate check
  - suggestion pass
  - and renderer
  transforms:
  - typed record -> pretty JSON file-of-record, one file per step_id / decision.id (filename IS the primary key; overwrite-on-save versioning, no history)
  - load_steps re-sorts by order field (module.py:40); load_decisions sorts by filename (module.py:62)
  touched_by:
  - src/broadway/timeline/walkthrough.py:121 (failure step save)
  - walkthrough.py:384 (step save)
  - src/broadway/cli.py:127-128 (report subcommand reads)
  - cli.py:184 (decision save)
  - src/broadway/timeline/suggest.py (via loaded lists)
  validated_by:
  - tests/test_timeline.py::test_persistence_roundtrip
  - tests/test_timeline.py::test_decision_persistence_roundtrip
  - tests/test_walkthrough.py::test_walkthrough_resume_idempotent
  if_changed:
  - rerun tests/test_timeline.py
  - tests/test_walkthrough.py; audit every glob("*.json") reader for filename-coupling
  findings:
  - writers-of-record duplicated across modules — timeline/module.save_step/save_decision (module.py:19-22, module.py:43-46) and lineage/records.write_record (records.py:19-40) are two structurally identical mkdir+write_text(model_dump_json) writers-of-record over parallel env-overridable roots (BROADWAY_TIMELINE_DIR module.py:8 / BROADWAY_LINEAGE_DIR records.py:8), with different filename rules (raw step_id vs ":"->"_" sanitized node_id) and no shared persistence layer; a durability or atomic-write fix must be applied twice.
- id: GATE-TLINE-54
  phase: timeline-lineage
  order: 60
  owner: src/broadway/timeline/walkthrough.py:291 run()
  inputs:
  - PipelineConfig (stats step, hypothesis-mode enforced walkthrough.py:292-296)
  - WalkthroughSequence + WalkthroughConfig (sequence.py loads)
  - frame/groups from runners.load_frame_and_groups (runners.py:86)
  - persisted steps/decisions
  - --force flag
  outputs:
  - per-order executor dispatch through _STEP_RUNNERS (walkthrough.py:281-288); step JSONs saved (walkthrough.py:384); DECISION REQUIRED console panels (walkthrough.py:138-189); reports regenerated via _write_timeline (walkthrough.py:71-91 -> reports/timeline.md, reports/index.md, reports/results/*); stale-decision warnings (walkthrough.py:192-201)
  transforms:
  - 'executor-stop semantics: decision-kind step with unresolved decision -> _write_timeline + panel + RETURN (walkthrough.py:337-366): the loop halts BEFORE downstream analysis steps'
  - already-recorded decision -> gate passes silently (logger only, walkthrough.py:337-340); posthoc gate additionally skipped entirely when omnibus passed=False (walkthrough.py:329-336)
  - unknown step id -> 'not yet implemented' stop (walkthrough.py:367-370); existing step skipped unless --force (walkthrough.py:371-373)
  - 'executor exceptions: _StopWalkthrough -> silent return (walkthrough.py:377-378); any other Exception -> FAILED step + traceback log in artifacts/timeline/<analysis>/failures/<step_id>.log then return (walkthrough.py:379-381, _handle_failure walkthrough.py:94-123)'
  - _run_omnibus/_run_posthoc/_run_conclusion raise _StopWalkthrough when a prerequisite decision/evidence is missing after re-writing the timeline first (walkthrough.py:231-278)
  touched_by:
  - src/broadway/cli.py:166-173 (walkthrough subcommand)
  - src/broadway/timeline/runners.py (all executors)
  - src/broadway/timeline/suggest.py (post-step/post-gate suggestions)
  - src/broadway/reports/{timeline,index,results}.py
  validated_by:
  - tests/test_walkthrough.py::test_walkthrough_stops_at_gate
  - tests/test_walkthrough.py::test_walkthrough_resume_past_gate
  - tests/test_walkthrough.py::test_walkthrough_resume_idempotent
  - tests/test_walkthrough.py::test_walkthrough_force_reruns_but_preserves_decision
  - tests/test_walkthrough.py::test_walkthrough_failure_capture
  - tests/test_walkthrough.py::test_stale_decision_warning
  - tests/test_walkthrough.py::test_walkthrough_end_to_end
  - tests/test_walkthrough.py::test_walkthrough_posthoc_gated_on_insignificant_omnibus
  if_changed:
  - rerun tests/test_walkthrough.py in full (stop/resume/force/failure matrix); re-verify reports regeneration order in _write_timeline
  findings: []
- id: GATE-TLINE-55
  phase: timeline-lineage
  order: 61
  owner: src/broadway/timeline/runners.py:302 run_omnibus()
  inputs:
  - groups dict[str,np.ndarray] built by build_declared_groups (runners.py:107)
  - decision.method from the resolved AnalysisDecision
  - alpha from walkthrough.yaml significance_alpha
  - 'posthoc: full df + source_group_column + target'
  outputs:
  - omnibus/posthoc/conclusion AnalysisStep records; effect-size ramification text; artifacts/timeline/<analysis>/evidence/omnibus.json (runners.py:324) and posthoc.json (runners.py:413); conclusion consumes prior steps' result_summary (runners.py:462-512) rather than recomputing
  transforms:
  - 'method fork #1: anova|welch|kruskal -> run_anova/run_welch/run_kruskal plans, else ValueError ''unknown omnibus method'' (runners.py:314-321)'
  - 'method fork #2: posthoc admits only games_howell, else ValueError (runners.py:394-395)'
  - plan.passed -> decision_required flag on the omnibus step (runners.py:377) which downstream gates and suggest.py key on
  - 'effect-size branch: eta²/omega² for anova|welch vs ε² for kruskal duplicated across run_omnibus, run_conclusion, and suggest._suggest_omnibus (runners.py:333-353, runners.py:467-473, suggest.py:146-152)'
  touched_by:
  - src/broadway/timeline/walkthrough.py:231-278 (_run_omnibus/_run_posthoc/_run_conclusion wrappers enforcing prerequisites)
  - src/broadway/stats/anova.py
  - stats/post_hoc.py games_howell
  - tests/test_walkthrough.py
  validated_by:
  - tests/test_walkthrough.py::test_run_omnibus_welch_reports_effect_sizes
  - tests/test_walkthrough.py::test_run_omnibus_kruskal_epsilon_squared
  - tests/test_walkthrough.py::test_run_omnibus_unknown_method_raises
  - tests/test_walkthrough.py::test_run_omnibus_imbalanced_significant_is_note
  - tests/test_walkthrough.py::test_run_posthoc_significant_pairs
  - tests/test_walkthrough.py::test_run_conclusion_copies_effect_sizes
  - tests/test_walkthrough.py::test_run_conclusion_kruskal_epsilon_squared
  if_changed:
  - rerun tests/test_walkthrough.py
  - tests/test_anova.py
  - tests/test_effect_size.py; update walkthrough.yaml methods in lockstep with dispatch sets
  findings:
  - 'these execute-time forks are the unvalidated twin of GATE-TLINE-52''s allowlist — nothing static ties walkthrough.yaml methods to the dispatch sets here; the only guards are two content-pinning tests, and run_conclusion re-derives effect sizes from an `else` branch (runners.py:472-473) that will crash with KeyError(''epsilon_squared'') on any future non-anova/welch/kruskal method that forks #1 ever admits.'
- id: GATE-TLINE-56
  phase: timeline-lineage
  order: 62
  owner: src/broadway/timeline/runners.py:114 run_describe()
  inputs:
  - runner-local evidence models from timeline/evidence.py (NormalityEvidence evidence.py:14, VarianceEvidence evidence.py:22, PosthocEvidence evidence.py:40, ConclusionEvidence evidence.py:48)
  - GroupSummary from stats.describe
  - threshold flags
  outputs:
  - 'six evidence JSONs under artifacts/timeline/<analysis>/evidence/: describe.json (runners.py:138)'
  - normality.json (runners.py:219)
  - variance.json (runners.py:274)
  - omnibus.json (runners.py:324)
  - posthoc.json (runners.py:413)
  - conclusion.json (runners.py:499); plus figures under reports/figures/ referenced by FigureRef
  transforms:
  - typed evidence snapshot -> standalone pretty JSON beside the step record; step.evidence_refs stores bare filenames ('describe.json', 'normality.json', ...) as machine pointers (runners.py:167-170, :252, :294, :374, :425, :522)
  - flagging logic (imbalance/skew/kurtosis/shapiro/levene-alpha) decides NOTE/WARNING status and ramification copy independently of the written evidence file
  touched_by:
  - src/broadway/timeline/walkthrough.py executors
  - src/broadway/discover/qq.py:_plot_qq_joint (figure only)
  - tests reading the files back
  validated_by:
  - tests/test_walkthrough.py::test_walkthrough_stops_at_gate
  - tests/test_walkthrough.py::test_run_normality_writes_figures
  - tests/test_walkthrough.py::test_run_variance_warning_on_unequal_variances
  - tests/test_walkthrough.py::test_run_posthoc_significant_pairs
  - tests/test_walkthrough.py::test_run_conclusion_copies_effect_sizes
  - tests/test_qq.py::test_run_normality_produces_single_joint_qq
  if_changed:
  - rerun tests/test_walkthrough.py + tests/test_qq.py; decide whether evidence JSON becomes a read input or is demoted explicitly
  findings:
  - evidence nobody reads back — no production code anywhere opens these six files; every human/machine surface (render_timeline, render_dashboard, write_results, suggestions) renders the DUPLICATED result_summary copy embedded in steps/*.json instead, and evidence_refs are never dereferenced. Sole readers are tests (tests/test_walkthrough.py:78-83, :291, :368, :395; tests/test_qq.py:80-85), so a writer bug that desyncs evidence JSON from result_summary would ship undetected by the product.
  - 'duplicate writer-of-record for describe evidence — broadway/stats/describe.py:139-140 writes its own <stats.output_dir>/describe.json (the ONLY describe.json read back at runtime, reports/describe.py:11, and the artifact lineage node describe:<analysis> points at via stats/describe.py:143-147), while run_describe re-invokes describe() and re-writes a same-named describe.json in the timeline tree (runners.py:130-140): two modules own one artifact name with divergent freshness and only one is consumed.'
- id: GATE-TLINE-57
  phase: timeline-lineage
  order: 63
  owner: src/broadway/timeline/sequence.py:47 load_walkthrough_sequence()
  inputs:
  - configs/flow/hypothesis_walkthrough.yaml (step ids/orders/questions/kind/action)
  - configs/step/walkthrough.yaml (skew/kurtosis/shapiro/imbalance/significance thresholds, max_qq_groups, decisions.{omnibus,posthoc}.{methods,parents})
  outputs:
  - WalkthroughSequence (sequence.py:22) and WalkthroughConfig (sequence.py:35) pydantic models; the single source of gate order
  - gate questions
  - threshold flags
  - and the decision allowlists/parent sets used by GATE-TLINE-52/-54/-55
  transforms:
  - yaml.safe_load -> extra=forbid models; step.kind constrained to Literal['evidence','decision','analysis'] (sequence.py:18) which routes the executor between analysis runners and decision gates
  - DecisionSpec.methods/parents become the decision-fork vocabulary and provenance parent list verbatim
  touched_by:
  - src/broadway/timeline/walkthrough.py:297-298 (loads both per run)
  - src/broadway/timeline/decide.py:15/:27
  - src/broadway/timeline/runners.py:53-54 (_thresholds in every runner)
  - src/broadway/cli.py:132 (report path)
  - src/broadway/timeline/suggest.py (cfg threading)
  validated_by:
  - tests/test_timeline.py::test_load_walkthrough_sequence
  - tests/test_decide.py::test_decisions_config_and_step_actions
  - tests/test_walkthrough.py::test_walkthrough_stops_at_gate
  if_changed:
  - rerun tests/test_timeline.py + tests/test_decide.py; keep flow decide_* ids
  - walkthrough.yaml decisions keys
  - and cli.py:98 --kind choices in lockstep
  findings: []
- id: GATE-TLINE-58
  phase: timeline-lineage
  order: 64
  owner: src/broadway/lineage/records.py:19 write_record()
  inputs:
  - node_id strings from lineage/ids.py:1 node_id() (f"{kind}:{name}", no character validation)
  - TransformAudit (lineage/models.py:73) from etl/canonicalize accounting
  - sample_name/sample_role
  - parents list
  - BROADWAY_LINEAGE_DIR env (records.py:8, default artifacts/lineage)
  outputs:
  - artifacts/lineage/records/<sanitized_node_id>.json LineageRecords; consumed only by graph.build_graph (graph.py:105-123) -> LineageGraph -> reports/lineage/graph.json + graph.md (lineage/module.py:27/:38) and mermaid rendering (mermaid.py:6); enforce_drop_fraction (records.py:43) raises before any record is written when unexplained row loss exceeds max_drop_fraction
  transforms:
  - provenance event -> durable record keyed by sanitized node_id; artifact path stored as string and existence-probed at graph time to set produced vs ran_but_output_missing (graph.py:109-111)
  - 'coercion lineage kind: etl collects CoercionRecord events (cleaning/structural.py:21-46, collector threaded through cleaner.parse_numeric) and writes {dataset}_coercion_audit.json + write_record(node_id(''coercion'', dataset), ''coercion'', ...) with ingest-or-dataset parent selection (etl/module.py:81/:145-156/:139-144)'
  - dangling parents get placeholder referenenced_not_found nodes (graph.py:139-147); edges deduped (graph.py:52-60); scope closure per analysis/dataset (graph.py:162-228)
  touched_by:
  - src/broadway/etl/module.py:151/:162/:167/:171 (coercion/join/lookup_value/etl records)
  - src/project/etl/process.py (ingest record, process.py:218 log)
  - src/broadway/stats/describe.py:143 (describe record)
  - src/broadway/lineage/{graph,state,module,mermaid}.py (read/render side)
  validated_by:
  - tests/test_lineage_models.py::test_transform_audit_and_lineage_record_round_trip
  - tests/test_lineage_graph.py::test_build_graph_links_nodes_and_records
  - tests/test_lineage_graph.py::test_dangling_parent_gets_placeholder_node
  - tests/test_process.py::test_process_data_writes_ingest_lineage
  - project/tests/test_ingest_audit.py::test_ingest_record_carries_balanced_audit
  - tests/test_lineage_state.py::test_non_lineage_kinds_never_listed
  if_changed:
  - rerun tests/test_lineage_*.py
  - tests/test_ingest_audit.py
  - tests/test_process.py; re-audit filename sanitization and KIND_LABELS/LINEAGE_STEPS kind coverage together
  findings:
  - 'lineage decision records have a reader but no writer — records.decisions_dir() (records.py:15-16) is never written by any src module: graph.build_graph reads artifacts/lineage/decisions/*.json into decision nodes with ''raises'' edges (graph.py:125-137) and load_decisions feeds RunState.open/resolved_decisions (graph.py:231-238, state.py:25-26), yet DecisionRecord (models.py:63) is instantiated nowhere in production; only a test fixture authors those files (tests/test_lineage_graph.py:83-93). The published open_decisions/resolved_decisions summary is fed by hand-maintained data.'
  - node_id->filename collision silently overwrites records-of-record — ids.node_id emits f"{kind}:{name}" unvalidated (ids.py:1-2) and write_record maps ':'->'_' for the filename (records.py:39), so distinct ids such as node_id("a","b:c") and node_id("a:b","c") both serialize to a_b_c.json with last-writer-wins and no uniqueness check; the graph then faithfully renders whichever record survived.
  - KIND_LABELS (graph.py:19-36) is referenced by nothing and already drifted — it omits the 'coercion' kind that etl actually writes (etl/module.py:153); state.LINEAGE_STEPS (state.py:5-9) also lacks coercion, so coercion nodes never advance stage/not_yet_run accounting. Dead label vocabulary coexisting with a live unwritten kind.
- id: GATE-TLINE-59
  phase: timeline-lineage
  order: 65
  owner: src/broadway/cli.py:105 main()
  inputs:
  - 'argv parsed by _build_parser (cli.py:26-102): walkthrough --analysis/--dataset/--sample/--force (cli.py:87-92)'
  - decide --analysis/--method/--reason/--kind choices={omnibus,posthoc} (cli.py:94-100)
  - lineage --analysis/--dataset (cli.py:40-42)
  - report (cli.py:44-46)
  - stats run|describe --sample (cli.py:80-84); console-script entry ds-pipeline = broadway.cli:main (pyproject.toml:53-54)
  outputs:
  - dispatch into timeline.walkthrough.run (cli.py:166-173)
  - decide_module.record + timeline_module.save_decision (cli.py:174-189)
  - lineage.module.run (cli.py:118-121)
  - report path re-rendering results from persisted steps/decisions with 'run the walkthrough first' short-circuit (cli.py:122-134)
  transforms:
  - 'argparse-level validation only at the CLI boundary: --kind choices gate the fork vocabulary once, everything else (method spelling, dataset/analysis existence, sample config) deferred to downstream ValueErrors/FileNotFoundError raised by decide.record / load_sample / load_config'
  - lazy imports per subcommand keep unrelated heavy deps unloaded
  touched_by:
  - uv run ds-pipeline (installed entry point)
  - python -m broadway.cli equivalent
  - tests/test_cli.py TestWalkthroughCLI
  - tests/test_cli_dispatch.py spies
  validated_by:
  - tests/test_cli.py::TestWalkthroughCLI::test_walkthrough_requires_dataset
  - tests/test_cli_dispatch.py::test_cli_lineage_dispatches_analysis_and_dataset
  - tests/test_cli_dispatch.py::test_cli_report_without_history_prints_hint
  - tests/test_cli_dispatch.py::test_cli_report_writes_results
  - tests/test_cli_dispatch.py::test_cli_stats_describe_dispatches_with_sample
  if_changed:
  - rerun tests/test_cli*.py; re-pin decide-branch dispatch; decide whether project/scripts gains a record-writing shim or is declared record-free by contract
  findings:
  - 'the project/scripts/* teaching entry points bypass this gate entirely — all eleven scripts (project/scripts/01_load_data.py .. 12_lgbm_baseline.py, `python project/scripts/<n>.py` / `python -m project.scripts.<n>` via __main__ guards, e.g. 04_anova_boroughs.py:55) call broadway.stats primitives directly (04_anova_boroughs.py runs anova.run_anova with no groups-empty-vs-absent gating beyond its own len filter) and write NO AnalysisStep, NO AnalysisDecision, and NO lineage record: analyses executed through them are invisible to the timeline gates, the decision ledger, and the lineage graph — two sanctioned front doors, one of which keeps no records.'
  - '`ds-pipeline decide` — the only command that mints decision records — has no CLI-level test: tests exercise decide.record directly (tests/test_decide.py) and walkthrough arg validation (tests/test_cli.py:157-160 asserts exit 2 for missing --dataset), but no test drives main() through the decide branch (cli.py:174-189), so the argparse choices + record + save wiring is unpinned end-to-end.'
- id: GATE-TLINE-114
  phase: timeline-lineage
  order: 66
  owner: 'src/broadway/lineage/state.py:5 LINEAGE_STEPS + src/broadway/baseline/module.py:87-91 parents'
  inputs:
  - flow step sequence
  outputs:
  - loud graph incompleteness when a declared parent kind never ran
  transforms:
  - 'LINEAGE_STEPS sequences all lead with "profile": prediction/hypothesis/causal = ["profile", …] (:6-8, verbatim at HEAD) — slate''s EXPECTED symbol name repaired per packet F #23'
  - 'baseline declares the profile parent REGARDLESS of whether profile ran: parents = ([node_id("analysis",…), node_id("profile", cfg.dataset.name)] if cfg.dataset is not None else […]) baseline/module.py:87-91'
  - e2e record evidence: flows ran ingest→etl→baseline without ever invoking the profile step (profile ran separately 22:45 vs baseline 22:38) — declared parent kind missing from the executed graph, silently
  touched_by: []
  validated_by: []
  if_changed:
  - LINEAGE_STEPS sequences
  - baseline parent declaration feeding the completeness check
  findings:
  - 'source FINDING-D2 root; validated_by [] with FINDING conforms to house law (packet F #23 amendment).'
- id: GATE-SURF-60
  phase: surfaces
  order: 67
  owner: 'src/broadway/timeline/walkthrough.py:87 _write_timeline()  # renders via src/broadway/reports/index.py:61 render_dashboard()'
  inputs:
  - persisted AnalysisStep list (timeline/module.load_steps)
  - persisted AnalysisDecision list
  - WalkthroughSequence (configs/flow walkthrough sequence)
  - Suggestion from timeline/suggest.suggest_next
  outputs:
  - reports/index.md
  transforms:
  - derive per-step resolved/blocked status from persisted steps+decisions (reports/index.py:55 _resolved())
  - pick frontier step -> COMPLETE / DECISION REQUIRED / IN PROGRESS
  - format next-action headline+command
  - render markdown dashboard
  touched_by:
  - ds-pipeline walkthrough (cli.py:87 parser -> timeline/walkthrough.py:291 run())
  validated_by:
  - tests/test_suggest.py::test_render_dashboard_sections
  - tests/test_suggest.py::test_render_dashboard_complete
  - tests/test_walkthrough.py::test_walkthrough_end_to_end
  if_changed:
  - rerun tests/test_walkthrough.py end-to-end
  - re-run walkthrough and assert nav links resolve (tests/test_surface_integrity.py::test_report_markdown_links_resolve)
  findings: []
- id: GATE-SURF-61
  phase: surfaces
  order: 68
  owner: 'src/broadway/timeline/walkthrough.py:85 _write_timeline()  # renders via src/broadway/reports/timeline.py:16 render_timeline()'
  inputs:
  - persisted AnalysisStep list
  - AnalysisDecision list
  - WalkthroughSequence
  - Suggestion appended as '## Suggested next action' (walkthrough.py:78-83)
  outputs:
  - reports/timeline.md
  transforms:
  - ordered step/status table
  - per-step ramification + humanized result_summary bullets
  - figure links emitted as figures/<name> relative to reports/ (timeline.py:61)
  touched_by:
  - ds-pipeline walkthrough
  validated_by:
  - tests/test_timeline.py::test_render_includes_details_for_completed
  - tests/test_timeline.py::test_render_timeline_figure_link_has_no_parent_prefix
  - tests/test_walkthrough.py::test_walkthrough_end_to_end
  if_changed:
  - rerun tests/test_timeline.py render block
  - assert every figure link target exists under reports/
  findings: []
- id: GATE-SURF-62
  phase: surfaces
  order: 69
  owner: 'src/broadway/reports/results.py:244 write_results()   # sole writer of reports/results/*.md; index via _render_index results.py:146; save loop results.py:258-259'
  inputs:
  - persisted AnalysisStep list
  - AnalysisDecision list
  - WalkthroughSequence (labels slugified via slugify results.py:54)
  outputs:
  - reports/results/index.md
  - reports/results/describe-groups.md
  - reports/results/normality-diagnostics.md
  - reports/results/variance-homogeneity.md
  - reports/results/post-hoc-comparisons.md
  - reports/results/principal-analysis.md
  - reports/results/conclusion.md
  transforms:
  - render persisted evidence ONLY (humanize_summary/humanize_value, no recomputation)
  - status derivation derive_status (results.py:125)
  - orphan-page deletion of stale *.md not in completed set (results.py:253-257)
  - figure links ../figures/<name> relative to reports/results/ (results.py:222)
  touched_by:
  - ds-pipeline walkthrough (walkthrough.py:91)
  - ds-pipeline report (cli.py:122-135 re-render from persisted evidence)
  validated_by:
  - tests/test_results.py::test_render_results_index_and_pages
  - tests/test_results.py::test_write_results_orphan_deletion
  - tests/test_results.py::test_write_results_failed_step_has_no_page
  - tests/test_figures.py::test_write_results_figures_relative_to_results
  if_changed:
  - rerun tests/test_results.py + tests/test_cli_dispatch.py report branch
  - confirm orphan deletion still prunes stale pages
  findings: []
- id: GATE-SURF-63
  phase: surfaces
  order: 70
  owner: 'src/broadway/reports/audit.py:684 run()               # writes all five audit pages at audit.py:694-706'
  inputs:
  - artifacts <dataset>_clean.json (StructuralCleanResult)
  - <dataset>_join_audit.json
  - <dataset>_lookup_value_audit.json
  - artifacts/discover/profile.json (DatasetProfile)
  - artifacts/discover/qq_overview.json (QqOverview)
  outputs:
  - reports/audit/index.md
  - reports/audit/profile.md
  - reports/audit/transform.md
  - reports/audit/join.md
  - reports/audit/lookup_values.md
  transforms:
  - 'deserialize typed Pydantic evidence and render Markdown only (module docstring audit.py:1-6: never re-runs ingest/etl/stats/profile, never reads parquet)'
  - PASS/WARNING/INCOMPLETE dataset status rollup (_dataset_status audit.py:472)
  - figure blocks reference ../figures/<name> from qq_overview.json paths (_render_figure_block audit.py:80-96)
  touched_by:
  - ds-pipeline audit (cli.py elif 'audit' -> audit.run)
  validated_by:
  - tests/test_audit_render.py::test_run_writes_five_markdown_files
  - tests/test_audit_render.py::test_render_join_distinguishes_rows_from_events
  - tests/test_audit_render.py::test_render_profile_high_identifier
  - tests/test_audit_render.py::test_join_counts_raises_on_divergent_row_counts
  if_changed:
  - rerun tests/test_audit_render.py
  - re-run ds-pipeline audit and confirm five tracked files unchanged in structure
  findings: []
- id: GATE-SURF-64
  phase: surfaces
  order: 71
  owner: 'src/broadway/lineage/module.py:15 run()               # graph.json at :27; graph.md at :38-40'
  inputs:
  - configs/ tree
  - artifacts/lineage records + decisions (lineage/records.py LINEAGE_DIR)
  - LineageGraph from lineage/graph.build_graph + scope_graph
  - Mermaid text from lineage/mermaid.to_mermaid
  outputs:
  - reports/lineage/graph.json
  - reports/lineage/graph.md
  transforms:
  - build full graph then scope to analysis/dataset
  - dump scoped LineageGraph JSON
  - wrap to_mermaid flowchart LR in fenced block with fixed reading preamble
  touched_by:
  - ds-pipeline lineage (cli.py elif 'lineage' -> lineage/module.run)
  validated_by:
  - tests/test_lineage_graph.py::test_build_graph_links_nodes_and_records
  - tests/test_lineage_mermaid.py::test_to_mermaid_renders_nodes_and_edges
  - tests/test_lineage_mermaid.py::test_to_mermaid_renders_sample_suffix
  if_changed:
  - rerun tests/test_lineage_graph.py + tests/test_lineage_mermaid.py + tests/test_lineage_scope.py
  - regenerate graph.md and check link resolution from other reports
  findings: []
- id: GATE-SURF-65
  phase: surfaces
  order: 72
  owner: 'src/broadway/stats/describe.py:117 plot_describe_figures() [savefig describe.png] + src/broadway/discover/qq.py:478 _plot_qq_joint() [savefig normality_qq.png]  # invoked solely by timeline/runners.py:143-146 and :210 inside run_describe()/run_normality()'
  inputs:
  - raw canonical/sample frame groups loaded by runners.load_frame_and_groups (runners.py:86)
  - GroupSummary from stats.describe
  - viz config configs/step/viz.yaml (describe_figure :16, normality_figure :17)
  outputs:
  - reports/figures/describe.png
  - reports/figures/normality_qq.png
  transforms:
  - boxplot+group-sizes panel draw (stats/describe.py)
  - per-group z-standardization INSIDE plot module (qq.py:436) + scipy.stats.probplot fit (:437) — see FINDING F-SURF-1
  - optional raw-vs-log layout chosen by pooled-skew computed at render time (runners.py:204-208) — see FINDING F-SURF-1b
  - FigureRef(path='figures/<name>') recorded into AnalysisStep evidence_refs/figures
  touched_by:
  - ds-pipeline walkthrough steps describe_groups / normality
  validated_by:
  - tests/test_describe.py::test_plot_functions_write_files
  - tests/test_figures.py::test_run_describe_sets_figures
  - tests/test_figures.py::test_run_normality_sets_figures
  - tests/test_walkthrough.py::test_run_normality_writes_figures
  if_changed:
  - rerun tests/test_figures.py + tests/test_qq.py joint-plot block
  - confirm PNG stays under cap (tests/test_surface_integrity.py::test_report_png_under_size_cap)
  findings: []
- id: GATE-SURF-66
  phase: surfaces
  order: 73
  owner: 'src/broadway/discover/module.py:74 _write_qq_overview() -> src/broadway/discover/qq.py:566 plot_numeric_qq()  # savefig sites qq.py:297/:356/:514/:562; called from module.py:78 (run() :105 and profile() :127)'
  inputs:
  - raw CSV/parquet frame
  - configs/step/viz.yaml knobs (qq_figure :14, qq_log_figure :15, dist_figure :15, diagnostics.figure :31, dpi, chunk sizes, zones/markers toggles)
  - contract exclude_from_profiling list
  outputs:
  - reports/figures/numeric_qq_{n}.png
  - reports/figures/numeric_dist_{n}.png
  - reports/figures/numeric_qq_log_{n}.png
  - reports/figures/numeric_diagnostics.png
  - artifacts/discover/qq_overview.json (figure-name registry consumed by reports/audit)
  transforms:
  - compute skew/kurtosis/zero_rate/QQ points while plotting (evidence producer, by design)
  - chunk features per figure by NAME not position (qq.py:718-746)
  - zone/marker overlays behind viz.yaml enabled flags (config/viz.py:11-38); NOTE no toggle suppresses emitting a figure — FINDING F-SURF-5
  touched_by:
  - ds-pipeline discover
  - ds-pipeline profile
  validated_by:
  - tests/test_qq.py::test_plot_numeric_qq_excludes_and_splits
  - tests/test_qq.py::test_plot_numeric_qq_log_figure_generated
  - tests/test_qq.py::test_plot_numeric_qq_discrete_feature
  - tests/test_viz.py::test_viz_config_loads_values
  if_changed:
  - rerun tests/test_qq.py + tests/test_viz.py
  - regenerate figures then rerun ds-pipeline audit so ../figures links match renamed files
  findings: []
- id: GATE-SURF-67
  phase: surfaces
  order: 74
  owner: tests/test_surface_integrity.py:41 test_report_markdown_links_resolve()
  inputs:
  - git ls-files reports/ '*.md' (tracked surface inventory, :26-38)
  - markdown link regex :23
  - size caps HTML_CAP_BYTES=5MiB :20 / PNG_CAP_BYTES=2MiB :21
  outputs: []
  transforms:
  - resolve every non-http/mailto/anchor relative link against md.parent (:53)
  - flag unresolvable links
  - enforce .html and .png size caps (:59-74)
  touched_by:
  - root pytest gate (D4 uv run pytest -q)
  - no dedicated CI job wiring found in scripts/ or pyproject
  validated_by:
  - tests/test_surface_integrity.py::test_report_markdown_links_resolve
  - tests/test_surface_integrity.py::test_report_html_under_size_cap
  - tests/test_surface_integrity.py::test_report_png_under_size_cap
  if_changed:
  - any new artifact type under reports/ requires a matching cap — .svg currently UNCOVERED, see FINDING F-SURF-4
  findings: []
- id: GATE-SURF-68
  phase: surfaces
  order: 75
  owner: '.gitignore:17-19  # experiments/results tracking convention (negation triad), pinned-sample negations :21-22'
  inputs:
  - experiment script outputs written ad hoc, e.g. experiments/fare_prediction/02_filtered_profile.py:46 desc.to_csv(CSV_OUT)
  - experiments/mlflow/_common.py:51 RESULTS = experiments/results/mlflow convention
  outputs:
  - experiments/results/**/*.csv (tracked)
  - experiments/results/univariate/fare_amount_trip_distance/ratecode1_sample.parquet|.json (pinned, tracked)
  transforms:
  - ignore everything under experiments/results/** (:17)
  - negate directories so traversal works (!…/**/ :18)
  - negate CSVs only (!…/**/*.csv :19)
  - global *.png ignore (:23) keeps experiment PNGs (e.g. experiments/results/univariate/**/01_*.png …) UNTRACKED; only !reports/figures/*.png (:24) is exempted
  touched_by:
  - ad-hoc experiment scripts only; NO broadway/src writer, NO test
  validated_by:
  - tests/test_governance_probes.py::test_probe_e_new_surface_tripwire_live_and_falsifiable
  if_changed:
  - loosening :23 or :19 silently changes which result surfaces are committed; any new figure format under reports/figures needs its own negation
  findings: []
- id: GATE-SURF-69
  phase: surfaces
  order: 76
  owner: 'src/broadway/reports/__init__.py  # renderer-purity contract over the package (markdown.py, results.py, timeline.py, index.py, audit.py, registry.py)'
  inputs:
  - persisted typed evidence JSON artifacts
  - persisted AnalysisStep/AnalysisDecision models
  - QqOverview/DatasetProfile/JoinAuditReport/LookupValueAuditReport/StructuralCleanResult models
  outputs:
  - (cross-cutting) all reports/**/*.md listed in GATE-SURF-60..64
  transforms:
  - 'formatting only: humanize_float/humanize_pvalue (formatting.py) via shared helpers results.py:27-77'
  - display-status derivation
  - NO statistical recomputation permitted in a renderer — violations flagged as findings
  touched_by:
  - 'every command that renders: walkthrough, report, audit, lineage'
  validated_by:
  - tests/test_results.py::test_humanize_helpers_live_in_shared_module
  - tests/test_results.py::test_rendered_output_plain_text_no_glyphs_or_literals
  - tests/test_audit_render.py::test_join_counts_raises_on_divergent_row_counts
  if_changed:
  - violations = FINDINGS F-SURF-1/1b/2 below; refusal condition if a second live writer ever lands on one artifact path
  findings: []
- id: GATE-SURF-100
  phase: surfaces
  order: 77
  owner: 'src/broadway/inference/api.py:1 FastAPI app stub (module docstring only — NO app symbol at HEAD; gate demands its creation) + k8s/api-deployment.yaml:18 uvicorn command'
  inputs:
  - uvicorn ASGI target import (inference.api:app)
  - models:/taxi@champion via MLflow registry
  outputs:
  - HTTP GET /health POST /predict GET /metrics bound 0.0.0.0:8000 — or loud CrashLoopBackOff with named cause on every replica
  transforms:
  - 'FINDING-rooted demand: api.py is a ONE-LINE docstring stub — hasattr(module, ''app'') is False at HEAD (interpreter probe, packet G D1)'
  - 'k8s/api-deployment.yaml:18-24 commands uvicorn inference.api:app --host 0.0.0.0 --port 8000 under replicas: 2 (:6) with HPA minReplicas: 2 (:48) ⇒ every apply CrashLoopBackOffs ALL replicas'
  - 'failure class: uvicorn fails AT STARTUP binding the ASGI target (attribute-not-found on inference.api:app), container exits non-zero, BackOff on both replicas — NOT a per-request predict failure'
  touched_by: []
  validated_by: []
  if_changed:
  - 'FIRST if_changed entry (packet G D1 AMEND 3): the moment an app symbol exists this gate MUST gain an import-contract pin (target importable; /health /predict /metrics declared) BEFORE DEPLOY-F1-API-LANE unblocks'
  findings:
  - 'CrashLoopBackOff today: the serving manifest demands a symbol that does not exist — deploy BLOCKED. Disposition = packet G D1 option (a): documented aspiration whose deploy stays blocked until implemented; refusal condition — if DEPLOY-F1-API-LANE is not executed by the next arbitration cycle the manifests die instead (drop Deployment+Service+HPA set demanding the unbuilt app); that decision belongs to the MERGE-WITH-DEPLOY-F1-API-LANE row (INFRA-130 lane, id burned), no duplicate custody minted.'
- id: GATE-SURF-101
  phase: surfaces
  order: 78
  owner: src/broadway/discover/columns.py:10 run()
  inputs:
  - raw CSV path (ds-pipeline columns subparser argv)
  outputs:
  - stdout per-column dtype report (read-only probe, zero artifact writes)
  transforms:
  - 'read-only column listing: pd.read_csv/read_parquet then prints name: normalize_dtype(dtype) per source column (:11-14)'
  touched_by:
  - pandas
  - broadway.config.schema.normalize_dtype
  validated_by: []
  if_changed:
  - CLI columns subparser surface
  findings: []
- id: GATE-SURF-102
  phase: surfaces
  order: 79
  owner: 'experiments_ui.py:56 FastAPI app custody (dashboard series endpoints)'
  inputs:
  - experiments results CSVs
  outputs:
  - dashboard series endpoints served by the FastAPI app object
  transforms:
  - 'app = FastAPI() module-level custody (:56); app-body aspect of the dashboard surface'
  - 'co-tenancy with GATE-INFRA-123 declared LEGAL (packet H #123): entry (__main__ uvicorn.run) vs app-body aspects of one file'
  touched_by: []
  validated_by: []
  if_changed:
  - dashboard endpoint contract consumed by GATE-INFRA-123 server entry
  findings: []
- id: GATE-CFG-70
  phase: config-schema
  order: 80
  owner: src/broadway/config/loader.py:91 _load_yaml()
  inputs:
  - configs/** relative path via CONFIGS_DIR (loader.py:34, overridable by env BROADWAY_CONFIGS_DIR)
  - yaml.safe_load stream
  outputs:
  - raw dict[Any, Any] section payload
  - or raised gate failure
  transforms:
  - 'existence gate: FileNotFoundError naming absolute path (:94)'
  - 'empty-document gate: ValueError ''config file is empty'' (:98)'
  - 'shape gate: non-mapping document rejected ''must be a mapping'' (:100)'
  - utf-8 strict open (:95)
  touched_by:
  - _merge_section (loader.py:109)
  - _missing_step_sections (loader.py:119)
  - _build_config sibling/full-step loads (loader.py:143,149)
  - resolve_full_steps flow load (loader.py:167)
  - scripts/run_local_ci.sh:47-51 configs gate
  - scripts/check_e2e_determinism.sh:174-187
  validated_by:
  - tests/test_config.py::test_missing_dataset_raises
  - tests/test_config.py::test_missing_experiment_raises
  - tests/test_full_dispatch.py::test_full_dispatch_missing_flow_file_raises
  - tests/test_reports.py::test_stats_sequence_bad_yaml_raises
  if_changed:
  - src/broadway/config/loader.py
  - pyproject dependency yaml
  findings:
  - 'the "YAML = single source of truth: no get(key, default), no hardcoded values" rule (agents/contracts/WORKER_CONTRACT.md:21, README.md:355) has ZERO enforcement tests — grep census over tests/ finds no probe for `.get(` defaults in config consumption paths; the only hygiene probe (tests/test_platform_hygiene.py::test_platform_test_has_no_project_coupling) checks project-coupling, not the get-default rule. Violations exist in-tree (src/broadway/samples/loader.py:38,41 `entry.get("nullable")` / `entry.get("checks", [])`; src/broadway/features/builders.py:39-55 kw.get defaults) — declared doctrine, unenforced gate.'
  - CONFIGS_DIR silently honors BROADWAY_CONFIGS_DIR (loader.py:34) — an inherited/stale value repoints EVERY load at a foreign tree with no warning; only tests/scripts set it deliberately.
- id: GATE-CFG-71
  phase: config-schema
  order: 81
  owner: src/broadway/config/resolver.py:9 _resolve_string()
  inputs:
  - every string scalar in the merged config dict
  outputs:
  - strings with ~/$VAR expanded
  - or the ORIGINAL string when the variable is unset
  transforms:
  - os.path.expandvars then os.path.expanduser (resolver.py:10)
  - recursive walk over dicts/lists via resolve_values (resolver.py:13-20)
  - applied post-merge pre-pydantic at loader.py:126, and again per raw step payload at loader.py:144,150
  touched_by:
  - _build_config (loader.py:126)
  - step-section attachment (loader.py:144)
  - full sub-step resolution (loader.py:150)
  - configs/environment/staging.yaml:2-11 (${DATA_DIR}, ${DATABASE_PORT}…)
  - configs/environment/production.yaml:2-11
  validated_by: []
  if_changed:
  - src/broadway/config/resolver.py
  - src/broadway/config/loader.py
  - configs/environment/production.yaml
  - configs/environment/staging.yaml
  findings:
  - 'unset-variable LITERAL PASSTHROUGH — os.path.expandvars leaves `${VAR}` verbatim when VAR is unset, and NOTHING scans the resolved tree for residual `${`. For str fields the literal sails through pydantic silently: data_dir becomes a directory literally named `${DATA_DIR}` (writes land in a bogus path, etl/module.py:92 mkdir creates it), database_password becomes the literal string `${DATABASE_PASSWORD}` (production.yaml:8), mlflow_tracking_uri likewise (production.yaml:6). Only ACCIDENT of int coercion makes it loud: `${DATABASE_PORT}` (production.yaml:11) dies in pydantic at EnvironmentConfig.database_port (schema.py:73) — fail-loud by type collision, not by design.'
  - resolver module has ZERO test coverage — grep census of tests/ for expandvars|expanduser|resolve_values|${ returns no hits; interpolation, passthrough behavior, and ordering vs merge are all unpinned.
- id: GATE-CFG-72
  phase: config-schema
  order: 82
  owner: src/broadway/config/loader.py:104 _merge_section()
  inputs:
  - environment (required name, loader.py:193)
  - dataset/experiment/analysis (optional CLI names, loader.py:194-196)
  - step/<name>.yaml (required, loader.py:197)
  outputs:
  - single merged dict keyed by section
  transforms:
  - 'required-name-missing gate: ValueError ''section X is required but name is None'' (:108)'
  - recursive deep-merge override within a section via _deep_merge (loader.py:81-88, applied :110)
  - later sections never overwrite other sections (update scoped per key, :110)
  touched_by:
  - load_config (loader.py:192-197)
  - _TOP_LEVEL_SECTIONS fence (loader.py:66)
  - _STEP_SECTION_REQUIREMENTS table (loader.py:72-78)
  validated_by:
  - tests/test_loader.py::test_standalone_step_loads_declared_sections
  - tests/test_loader.py::test_missing_required_step_section_raises
  - tests/test_analysis_contract.py::test_loader_wires_analysis
  if_changed:
  - src/broadway/config/loader.py
  findings:
  - deep-merge precedence (override wins, loader.py:83-87) and list-replacement semantics are UNTESTED — grep census of tests/ shows no _deep_merge/_merge_section exercise; a future merge-semantics change lands silent. Merge happens BEFORE env-var resolution (merge at :110, resolve at :126), so a partial `${VAR}` inside an overridden subtree still resolves once, merged — correct today, unpinned tomorrow.
- id: GATE-CFG-73
  phase: config-schema
  order: 83
  owner: src/broadway/config/schema.py:62 EnvironmentConfig
  inputs:
  - merged environment section (post GATE-CFG-71)
  outputs:
  - typed EnvironmentConfig instance embedded in PipelineConfig (loader.py:135)
  transforms:
  - 'field-coercion gates: database_port:int (:72) — staging/prod ${DATABASE_PORT} literal fails here when var unset'
  - 'all 15 fields required, none defaulted (:62-77); download_chunk_size:int REMOVED this cycle per G D5 dead-code-delete-referral (field + yaml keys + download.py moved together under the extra-key hazard)'
  - NO extra='forbid' — pydantic-v2 default ignores unknown keys
  - 'unknown step name gate upstream: ValueError ''unknown step'' (loader.py:186-187); step model dispatch via STEP_MODULES (loader.py:37-48)'
  - 'PipelineConfig assembly gate (loader.py:132-138): AnalysisContract/DatasetContract/ExperimentConfig constructed iff present'
  touched_by:
  - load_config→_build_config (loader.py:132-138)
  - every step module consuming cfg.environment (etl/module.py:92)
  - scripts/run_local_ci.sh:47-51 (loads all configs/experiment/*.yaml through this gate)
  - tests/test_loader.py synthetic environments (tests/test_loader.py:14-31)
  validated_by:
  - tests/test_config.py::test_invalid_step_raises
  - tests/test_config.py::test_load_train_config
  - tests/test_pyfunc_inference.py::test_champion_predicts_on_raw_frame
  - tests/test_recipe.py::test_cross_check_fires_at_config_load
  if_changed:
  - src/broadway/config/schema.py
  - src/broadway/config/loader.py
  - configs/environment/development.yaml
  - configs/environment/staging.yaml
  - configs/environment/production.yaml
  findings: []
- id: GATE-CFG-74
  phase: config-schema
  order: 84
  owner: src/broadway/config/schema.py:51 DatasetContract
  inputs:
  - 'configs/dataset/*.yaml (column pins: dtype/null_count/role per column, e.g. configs/dataset/taxi.yaml:1-29; lookup_tables with value_policies/sentinel_values taxi.yaml:31-59)'
  outputs:
  - DatasetContract with dict[str, ColumnSchema]
  - consumed by pandera_dtype/build_raw_schema and every loader
  transforms:
  - 'role vocabulary gate: ColumnRole str-Enum (schema.py:17-21) — bad role fails coercion'
  - 'dtype normalization gate: _normalize_dtype field_validator mode=before (schema.py:33-36) folds datetime64[ns]/[us]→datetime64 via normalize_dtype (schema.py:24-25)'
  - LookupValuePolicy sentinel gate extra='forbid' (schema.py:39-40); LookupSpec parse (schema.py:44-48)
  - null_count observed-not-invariant (documented contracts/pandera.py:52-53)
  touched_by:
  - _build_config (loader.py:134)
  - build_raw_schema (contracts/pandera.py:58)
  - build_generic_feature_specs nullable=null_count>0 (features/generic.py:19)
  - lookup value audit (data/lookup_value_audit.py:48)
  - joined_schema_columns (schemas/joined.py:27-30)
  validated_by:
  - tests/test_boundary_contracts.py::test_raw_canonical_boundary_rejects_dtype_mutation
  - tests/test_config.py::test_lookup_spec_value_policies_parse
  - tests/test_config.py::test_lookup_spec_na_values_parse
  - tests/test_contracts.py::test_valid_dataframe_passes_all_checks
  - tests/test_engineered_order_parity.py::test_specs_order_matches_producer_write_order
  if_changed:
  - src/broadway/config/schema.py
  - src/broadway/contracts/pandera.py
  - configs/dataset/taxi.yaml
  - configs/dataset/test.yaml
  findings:
  - schema-without-strictness asymmetry — DatasetContract (schema.py:51) and EnvironmentConfig (schema.py:62) do NOT set ConfigDict(extra="forbid"), while AnalysisContract (analysis/contracts.py:20), DataSourceRef (schema.py:146), ModelHPOSpec (schema.py:120), PreprocessingStepConfig (schema.py:175) and SampleSpec (lineage/models.py:38) DO. A typo'd key in a dataset/environment yaml (e.g. `datbase_host`) is SILENTLY IGNORED by pydantic-v2 default — config drift invisible at the exact layer whose job is to pin truth.
  - dead EnvironmentConfig fields with zero consumers — grep census of src/ finds NO reader of sample_size_ci / sample_size_stats / api_replicas_min / api_replicas_max / api_hpa_cpu_threshold (schema.py:74-78; only the declaration matches). production.yaml:12-13 hardcodes both sample sizes to 0 and nothing rejects 0 (no ge constraint, contrast EtlStep.ci_sample_size Field(ge=0) schema.py:227). D11 (DECISIONS.md:60-62) already rules dead EnvironmentConfig fields = straight delete.
- id: GATE-CFG-75
  phase: config-schema
  order: 85
  owner: src/broadway/contracts/pandera.py:46 build_raw_schema()
  inputs:
  - DatasetContract.columns (post GATE-CFG-74)
  outputs:
  - pa.DataFrameSchema
  - one pa.Column per contract entry
  - coerce=False strict dtypes
  - nullable=True (pandera.py:55-59)
  transforms:
  - 'pandera_dtype derivation (pandera.py:34-43): int8/16/32/64→pa.Int*, float32/64→pa.Float*, datetime64 family→pa.DateTime, object/str/string→pa.String'
  - is_numeric_dtype selector basis (pandera.py:30-31, consumed by contracts/selectors.py:6)
  - 'validation consumers: etl canonical write guard (etl/module.py:95), joined-loader unique-label SchemaError context (data/loader.py:100)'
  touched_by:
  - etl/module.py:95
  - data/loader.py:99-109
  - samples/loader.py:44
  - features/generic.py:52
  - features/schema.py:32
  validated_by:
  - tests/test_boundary_contracts.py::test_raw_canonical_boundary_rejects_dtype_mutation
  - tests/test_boundary_contracts.py::test_gap_raw_boundary_int_float_coercion_mask
  - tests/test_boundary_contracts.py::test_gap_engineered_boundary_extra_column
  - tests/test_boundary_contracts.py::test_joined_loader_rejects_pre_existing_lookup_suffix_collision
  - tests/test_boundary_contracts.py::test_joined_loader_duplicate_label_error_names_all_collisions
  if_changed:
  - src/broadway/contracts/pandera.py
  - src/broadway/etl/module.py
  - src/broadway/data/loader.py
  findings:
  - 'silent-degrade tail of the dtype map — pandera_dtype returns pa.Object for ANY unrecognized dtype string (pandera.py:43) and ColumnSchema.dtype is free-form str (only the datetime64 prefix is normalized, schema.py:24-25): a misspelled pin (`flaot64`, `int`) builds a schema whose column validates almost anything — the raw boundary gate quietly loses its teeth for that column instead of failing at derivation.'
- id: GATE-CFG-76
  phase: config-schema
  order: 86
  owner: src/broadway/config/schema.py:187 ExperimentConfig
  inputs:
  - configs/experiment/*.yaml (data_source/features/model/split/hpo/preprocessing)
  - allowed_params registry (training/models/registry.py)
  outputs:
  - validated ExperimentConfig; HPO search spaces registry-checked; flow-mode map checked
  transforms:
  - 'DataSourceRef._validate_version_rule (schema.py:151-163): named_sample⇔version exclusivity, both directions'
  - 'HPO param registry gate _validate_hpo_search_space (schema.py:197-215): non-pre__ params must ⊆ allowed_params(spec.name); PRE_PARAM_PREFIX carve-out (schema.py:181-184)'
  - 'FullStep._validate_flow_modes (schema.py:308-316): flow keys ⊆ AnalysisMode values'
  - PreprocessingStepConfig Literal kind gate (schema.py:176) + extra='forbid' (:175)
  - SplitConfig Literal['time','random','stratified'] + validation_size∈[0,1] (schema.py:114-116)
  touched_by:
  - _build_config (loader.py:136)
  - training HPO engine
  - full dispatch (loader.py:146)
  - tests/test_config.py::_experiment_config builder
  validated_by:
  - tests/test_config.py::test_hpo_search_space_valid_for_lgbm
  - tests/test_config.py::test_hpo_search_space_invalid_for_linear_raises
  - tests/test_config.py::test_hpo_search_space_validated_per_model
  - tests/test_config.py::test_named_sample_requires_version
  - tests/test_config.py::test_version_rejected_for_non_named_sample_loader
  - tests/test_config.py::test_data_source_rejects_invalid_loader
  - tests/test_config.py::test_preprocessing_step_unknown_type_rejected
  - tests/test_config.py::test_preprocessing_step_extra_fields_forbidden
  - tests/test_full_dispatch.py::test_full_step_rejects_invalid_flow_mode
  - tests/test_config.py::test_train_step_cv_kind_invalid_raises
  if_changed:
  - src/broadway/config/schema.py
  - src/broadway/training/models/registry.py
  - configs/experiment/*.yaml
  findings:
  - B1 include-validation NOT LANDED (slate v4 row QUEUED — agents/ledger/FIXES.md:38; DECISIONS.md:12-13, absorbed into Tier2 by D13 DECISIONS.md:67-68). Today build_generic_feature_specs SILENTLY SKIPS any FeatureConfig.include name absent from contract.columns (features/generic.py:16-19, `if col in contract.columns:` with no else) — a typo'd include shrinks the feature surface invisibly, violating the very "YAML declares everything" doctrine; no tripwire test pins the silence either (tests/test_generic_features.py::test_build_generic_feature_specs exercises only resolvable includes).
- id: GATE-CFG-77
  phase: config-schema
  order: 87
  owner: src/broadway/features/recipe.py:102 validate_preprocessing_columns()
  inputs:
  - PipelineConfig with experiment.preprocessing + data_source.schema_contract + dataset
  outputs:
  - config-load pass
  - or ValueError naming offending step/columns/schema_contract
  transforms:
  - bound-surface resolution via schema_columns (recipe.py:129-133 → schemas/__init__.py:41-58; unknown contract raises 'unknown schema_contract' :52-57)
  - '''engineered'' requires features config (schemas/__init__.py:26-30)'
  - joined surface = contract ∪ lookup-derived renames, read live from lookup file headers (schemas/joined.py:23-31; missing lookup file FileNotFoundError :19)
  - strict-subset column gate per step (recipe.py:134-141)
  - one_hot sole-step gate (recipe.py:119-128)
  - fires AT CONFIG LOAD from loader.py:139 — bad recipes never reach a run
  - 'downstream recipe-param gates: _validate_step_params (recipe.py:33-46), bool coercion _coerce_bool_param (recipe.py:49-58), smoothing float() (recipe.py:67)'
  touched_by:
  - _build_config (loader.py:139)
  - build_pipeline consumers (training)
  - eligible_feature_columns (utils.py, pinned by Decision-5 tests)
  validated_by:
  - tests/test_recipe.py::test_cross_check_rejects_column_outside_bound_schema
  - tests/test_recipe.py::test_lookup_derived_column_passes_joined_fails_raw
  - tests/test_recipe.py::test_joined_schema_matches_loader_output
  - tests/test_recipe.py::test_unknown_schema_contract_fails_loud
  - tests/test_recipe.py::test_cross_check_fires_at_config_load
  - tests/test_recipe.py::test_one_hot_mixed_with_other_steps_rejected_at_load
  - tests/test_recipe.py::test_invalid_step_param_fails_loud
  - tests/test_recipe.py::test_target_encoding_requires_smoothing
  - tests/test_recipe.py::test_normalize_accepts_string_bools
  - tests/test_recipe.py::test_normalize_rejects_non_bool_values
  - tests/test_decision5_columns.py::test_engineered_requires_features_config
  - tests/test_decision5_columns.py::test_raw_and_joined_ignore_features_argument
  if_changed:
  - src/broadway/features/recipe.py
  - src/broadway/schemas/__init__.py
  - src/broadway/schemas/joined.py
  - src/broadway/config/loader.py
  findings: []
- id: GATE-CFG-78
  phase: config-schema
  order: 88
  owner: src/broadway/config/loader.py:155 resolve_full_steps()
  inputs:
  - PipelineConfig(full+analysis)
  - configs/flow/<name>.yaml (FlowConfig steps list, schema.py:319-320)
  outputs:
  - ordered list[str] of concrete step names for the analysis mode
  transforms:
  - analysis-present gate (:156-157)
  - full-block-present gate (:158-159)
  - 'mode→flow mapping gate: ''no flow defined for analysis mode'' listing valid modes (:160-164)'
  - flow-file existence gate FileNotFoundError→ValueError (:166-169)
  - unknown-step gate against STEP_MODELS (:170-175)
  - sub-step payload reload+revalidate per step in _build_config (:145-151)
  touched_by:
  - load_config('full') (loader.py:197)
  - orchestration entrypoints dispatching flows
  - configs/flow/*.yaml (prediction/causal/hypothesis/stats_sequence/walkthrough)
  validated_by:
  - tests/test_full_dispatch.py::test_flows_dispatch_per_mode
  - tests/test_full_dispatch.py::test_full_requires_analysis
  - tests/test_full_dispatch.py::test_full_dispatch_unknown_mode_raises
  - tests/test_full_dispatch.py::test_full_dispatch_unknown_step_raises
  - tests/test_full_dispatch.py::test_full_dispatch_missing_flow_file_raises
  if_changed:
  - src/broadway/config/loader.py
  - configs/flow/
  - src/broadway/config/schema.py
  findings: []
- id: GATE-CFG-79
  phase: config-schema
  order: 89
  owner: src/broadway/samples/loader.py:34 _build_schema()
  inputs:
  - SampleSpec.schema block from configs/sample/*.yaml (e.g. configs/sample/fare_prediction_1m.yaml:29-36 dtype/nullable/checks)
  - provenance JSON sidecar
  - parquet artifact
  outputs:
  - pa.DataFrameSchema with op-derived pa.Checks (_CHECK_BUILDERS map loader.py:24-31); validated Sample(df, spec, provenance)
  transforms:
  - 'spec resolution gate: load_sample FileNotFoundError/mapping checks (lineage/sample.py:9-16) over SampleSpec extra=''forbid'' (lineage/models.py:37-52; role Literal diagnostic|estimation :7, FilterSpec op Literal :17-21)'
  - artifact+provenance existence gates (loader.py:66,68)
  - 'integrity gates: artifact sha256 (:71-72), definition digest drift → ''bump version and regenerate'' (:73-77), row count (:80-84)'
  - schema block optional — absent ⇒ digest/row-count only (boundary-map B5, tests/test_boundary_contracts.py:41-42); validate lazy=True raising SchemaErrors (:85-87)
  touched_by:
  - read_named_sample consumers (stats/timeline runners)
  - sample_generate.generate_sample writer side
  - tests/test_samples.py fixtures writing configs/sample/*.yaml
  validated_by:
  - tests/test_samples.py::test_schema_violation_raises
  - tests/test_samples.py::test_corrupted_artifact_raises
  - tests/test_samples.py::test_definition_digest_mismatch_raises
  - tests/test_samples.py::test_row_count_mismatch_raises
  - tests/test_samples.py::test_version_resolution
  - tests/test_samples.py::test_existing_config_still_parses
  - tests/test_sample.py::test_sample_spec_validation
  - tests/test_sample.py::test_load_sample_column_mapping_round_trip
  - tests/test_boundary_contracts.py::test_named_sample_boundary_rejects_source_dtype_mutation
  if_changed:
  - src/broadway/samples/loader.py
  - src/broadway/lineage/models.py
  - src/broadway/lineage/sample.py
  - configs/sample/fare_prediction_1m.yaml
  findings:
  - C1 _SAMPLE_SCHEMA DERIVATION STILL QUEUED (agents/ledger/FIXES.md:40 "queued"; audit note agents/audits/B-TRUTH-ENFORCEMENT.md:32). Production derivation EXISTS (samples/loader.py:34 builds the schema FROM the sample config's schema block), but the boundary tripwire hand-maintains its own `_SAMPLE_SCHEMA` literal at tests/test_boundary_contracts.py:302-307 instead of deriving from configs/sample/fare_prediction_1m.yaml:29-36 — a second declaration surface that can drift from the shipped config with nothing red.
- id: GATE-CFG-103
  phase: config-schema
  order: 90
  owner: 'src/broadway/config/loader.py:50 STEP_MODULES'
  inputs:
  - step name argv
  outputs:
  - module binding or loud unknown-step error
  transforms:
  - 'dispatch-map custody — THE registry deciding which module each step name may load (STEP_MODULES dict literal at :50)'
  - 'PRECISE-SPLIT vs GATE-CFG-78 (packet F #12): CFG-78 keeps resolve_full_steps() merge/orchestration law (:155); 103 owns the dispatch map itself — distinct symbols, split is real'
  touched_by:
  - load_config→_build_config dispatch (loader.py:132-138)
  validated_by:
  - tests/test_gate_registry.py::test_step_modules_dispatch_owned_by_cfg_103
  if_changed:
  - STEP_MODULES membership
  - unknown-step error grammar consumed by GATE-CFG-78
  findings:
  - 'lands WITH the same-commit flip of the former UNOWNED-surface tripwire (was test_has_step_modules_stays_a_drift_proof_miss @ tests/test_gate_registry.py:689-695 pinning arbitration PROPOSE-GH1-01) — rewritten into an ownership assertion so CI stays green (packet F #12 mandatory correction b: teeth live TRACKED in this batch, no self-attestation).'
- id: GATE-CFG-104
  phase: config-schema
  order: 91
  owner: 'project/data.py:31-47 configs-root resolution block'
  inputs:
  - 'configs/{dataset,project}/ trees'
  outputs:
  - seven module-level config singletons resolved at import (_contract/_project/_etl/_features and siblings)
  transforms:
  - 'seven configs resolved by CWD-relative Path literals: _DATASET_YAML = Path("configs/dataset/taxi.yaml") (:31) … _FEATURES_YAML = Path("configs/step/features.yaml") (:46-47)'
  - 'platform dialect is env-aware CONFIGS_DIR = Path(os.getenv("BROADWAY_CONFIGS_DIR") or "configs") (src/broadway/config/loader.py:34) — already imported by project/etl/process_config.py:6,10 while data.py hardcodes relative Paths: the split-brain, verified worse than stated (packet F #13)'
  - 'file-shared/symbol-disjoint CO-TENANCY declared with GATE-TRAIN-32 (owner read_training_sample() project/data.py:166) — closes the PROPOSE-GH3-02 OVERLAP-PATH flag'
  touched_by: []
  validated_by: []
  if_changed:
  - configs-root dialect shared with project/etl/process_config.py (GATE-ETL-117 rider)
  findings:
  - 'FINDING root (packet F #13): single configs-root owner demanded to kill the project-plane split-brain; validated_by [] lawful under the FINDING/root rule.'
- id: GATE-CFG-105
  phase: config-schema
  order: 92
  owner: 'src/broadway/onboard/module.py:215 init() (_write_configs :178-198)'
  inputs:
  - stdin prompts + 13 argv flags
  outputs:
  - 'configs dataset/analysis/experiment YAMLs + profile JSON + 1 lineage record (write call :302)'
  transforms:
  - 'scaffolder custody: the ONLY entry that MUTATES the contract SSOT tree (_write_configs :178-198 invoked from init :215)'
  touched_by: []
  validated_by: []
  if_changed:
  - scaffolded configs shape consumed by every loader gate
  findings: []
- id: GATE-CFG-107
  phase: config-schema
  order: 93
  owner: 'src/broadway/data/loader.py:134 lookup pre-read existence admission (declared-lookup bootstrap check)'
  inputs:
  - 'DatasetContract.lookup_tables paths — configs/dataset/taxi.yaml:31-34 declares out-of-repo symlink data/raw/taxi_zone_lookup.csv → /home/opc/ONE/learning/data/raw/taxi_zone_lookup.csv'
  outputs:
  - dangling-symlink-aware loud pre-merge error NAMING the bootstrap step
  transforms:
  - 'AMENDED per packet H X2: the gate lives as a PRE-READ existence assertion at load_with_audit — Path.exists() follows links so a DANGLING symlink passes naive checks; the error must name the bootstrap step'
  - 'co-tenancy with GATE-ETL-11/GATE-INGEST-02 on load_with_audit DECLARED, line-anchored aspect: pre-read admission check (O5 precedent); artifact declaration side remains configs/dataset/taxi.yaml:31-34'
  touched_by: []
  validated_by: []
  if_changed:
  - lookup_tables declaration shape (GATE-CFG-74)
  findings:
  - 'FINDING root (packet H X2): dependency on a path OUTSIDE the repo, checked by nobody today; complements D#7''s README bootstrap edit (docs side).'
- id: GATE-CFG-108
  phase: config-schema
  order: 94
  owner: 'src/broadway/onboard/infer.py:10 _IDENTIFIER_THRESHOLD'
  inputs:
  - BROADWAY_IDENTIFIER_THRESHOLD env var
  outputs:
  - typed float threshold or named parse error
  transforms:
  - 'exactly ONE numeric env-parse site exists at HEAD: float(os.getenv("BROADWAY_IDENTIFIER_THRESHOLD", "0.95")) at import time with a bare ValueError on bad floats (:10; packet F #17 courtesy finding — the slate''s plural threshold-vars/dedup framing overstated the surface; single-var law landed accordingly)'
  touched_by: []
  validated_by: []
  if_changed:
  - identifier-threshold semantics consumed at infer.py:44
  findings: []
- id: GATE-CFG-111
  phase: config-schema
  order: 95
  owner: 'experiments/mlflow/_common.py:60 CONFIG_PATH BROADWAY_MLFLOW_CONFIG resolution + experiments/mlflow/03_optuna_worker.py:46-49 HPO_CONFIG_PATH env branch'
  inputs:
  - BROADWAY_MLFLOW_CONFIG env var
  outputs:
  - single resolution dialect for the MLflow/HPO config path
  transforms:
  - '_common.py falls back to repo default configs/experiments/mlflow.yaml when env unset (:58-61); worker branches on env presence with parents[2] repo fallback (:46-49) — TWO styles today, one dialect demanded'
  - k8s render_worker_jobs.py:48 injects the env into worker pods (injection site rides the same dialect)
  touched_by: []
  validated_by: []
  if_changed:
  - worker-image config mounting contract
  findings:
  - 'slate attributed the owner to training/mlflow_utils.py — corrected during landing to the REAL resolution sites after existence verification (packet F #20 routed note); BROADWAY_MLFLOW_CONFIG never appears in mlflow_utils.'
- id: GATE-CFG-112
  phase: config-schema
  order: 96
  owner: 'src/broadway/lineage/models.py:37 SampleSpec.column_mapping + configs/sample/taxi_diagnostic.yaml:5 (+ consumers stats/module.py:42, stats/describe.py:132, timeline/runners.py:100)'
  inputs:
  - 'column_mapping block + analysis group_column (configs/analysis/taxi_hypothesis.yaml:13 group_column: Borough)'
  outputs:
  - 'validated mapping direction (logical→source); load-time existence validation of mapped values against the sample artifact'
  transforms:
  - 'DIRECTION LAW logical→source, pinned verbatim at HEAD: group_col = sample.column_mapping.get(analysis.hypothesis.group_column, …) stats/module.py:42 · source_group_column = sample.column_mapping.get(group_column, group_column) stats/describe.py:132 and timeline/runners.py:100'
  - 'shipped yaml writes {Borough: pickup_borough}; analysis declares group_column: Borough so .get("Borough") HITS — the E2E-D1 headline ("inverted yaml, remap silently never fires") is DISPROVEN at HEAD (packet F #21 parquet probe: joined_sample_live.parquet carries pickup_borough 200130 non-null, NO Borough column; packet G D2 concurs on the corrected owner path)'
  - 'LOAD-TIME EXISTENCE CUSTODY (the real residual gap): nothing anywhere validates that mapped values exist in the sample artifact — a bad value today surfaces only downstream as group-column-not-found ValueError, and only if that path runs'
  - 'SampleSpec definition fact: src/broadway/lineage/models.py:37 class SampleSpec, field column_mapping dict[str,str] = {} at :43, extra="forbid" at :38 — ZERO column_mapping occurrences in src/broadway/samples/'
  touched_by:
  - broadway.lineage.sample load path (lineage/sample.py)
  validated_by:
  - tests/test_sample.py::test_load_sample_column_mapping_round_trip
  - tests/test_stats_module.py::test_stats_run_with_sample_column_mapping
  - tests/test_describe.py::test_describe_run_column_mapping
  if_changed:
  - SampleSpec.column_mapping field semantics
  - taxi_diagnostic.yaml mapping block
  findings:
  - 'FINDING-D1 correction carried per packets F/G: the three cited TRACKED tests pin the consumers'' correct direction (describe test builds source_{group} physical columns); the "fixture fix / inverted yaml" framing is struck — what legitimately remains is direction-law documentation plus the unpinned load-time existence check.'
- id: GATE-HPO-80
  phase: hpo-optuna
  order: 97
  owner: src/broadway/training/hpo.py:120 run_model_study()
  inputs:
  - ModelHPOSpec search space via _trial_objective (hpo.py:99)
  - HPOConfig.direction + storage_url (CFG-HPO-SPEC / CFG-HPO-EXPERIMENT)
  - random_state seed int
  outputs:
  - ARTIFACT-OPTUNA-STUDY (optuna.Study — in-memory object or RDB-backed shared study)
  transforms:
  - study_name convention f'hpo-{model.name}' (:139); k8s twin f'hpo-{key}' (experiments/mlflow/03_optuna_worker.py:186)
  - TPESampler seeded AT CONSTRUCTION — Optuna 4.x reads the seed only there (:132-140), so the same (model, random_state) reproduces the trajectory
  - storage_url None → optuna.create_study(direction, sampler) IN-MEMORY; study_name unused on this branch (:141-142)
  - else RDBStorage(url, heartbeat_interval=60, grace_period=300) (:144-148) + create_study(load_if_exists=True) reopen (:149-155) — heartbeat pattern from GATE-HPO-86
  - direction passed through VERBATIM, default 'minimize' (:126; callers pass hpo.direction)
  - mlflow_tracking → callbacks=[_mlflow_callback(study_name, tags)] (:156-158)
  touched_by:
  - optuna@4.4.0
  - sqlalchemy@2.0.40
  validated_by:
  - tests/test_hpo.py::test_run_model_study_sqlite_reopens_and_accumulates
  - tests/test_optuna_extended.py::test_run_study_rdb_sqlite
  - tests/test_optuna_extended.py::test_run_study_rdb_load_if_exists_reopens
  - tests/test_hpo.py::test_hyperopt_smoke_through_pipeline_objective
  if_changed:
  - ARTIFACT-OPTUNA-STUDY resume/reopen semantics
  - mlflow run-name/tag schema of GATE-HPO-83
  - GATE-HPO-82/-86
  findings:
  - 'distributed phase-1 points N pods at ONE shared study with IDENTICALLY seeded samplers — every pod constructs TPESampler(seed) from the same config seed (experiments/mlflow/03_optuna_worker.py:190-194) and no TPESampler(constant_liar=True) exists anywhere in the repo, so concurrent pods reading the same trial history can propose identical params and burn duplicated budget; nothing detects or dedupes collisions. root: src/broadway/training/hpo.py:140 (+ experiments/mlflow/03_optuna_worker.py:194)'
- id: GATE-HPO-81
  phase: hpo-optuna
  order: 98
  owner: src/broadway/training/hpo.py:30 make_objective()
  inputs:
  - PipelineConfig experiment block
  - ModelHPOSpec.name registry key
  - CFG-HPO-SPEC.target_metric
  - X/y train+val frames
  outputs:
  - Objective(params[, trial]) -> float closure; per-trial 'broadway_metrics' user attr
  transforms:
  - closure builds the pipeline PER CALL via build_model_pipeline(cfg, model_type, params) (:54) — the single composition shared with the trainer per GATE-TRAIN-34, so preprocessing cannot drift between fit and search
  - preprocessing REFITS on train every trial = the COMPLETE leakage guard because HPO folds nothing internally (docstring :39-43; pinned by tests/test_hpo.py::test_hpo_trial_refits_preprocessing_each_trial)
  - fit on train only, predict val only (:55-56) — no internal CV anywhere
  - 'metric extraction: compute_metrics(y_val.to_numpy(), pipeline.predict(X_val)) → the full rounded 7-key regression dict (:56; vocabulary owned by GATE-TRAIN-36)'
  - 'broadway_metrics user attr {name: float(v)} attached ONLY when a trial is passed (:57-60) — the exact dict shape the per-trial callback logs (GATE-HPO-83)'
  - returns metrics[target_metric] (:61)
  - 'random_state threading: estimator seeds arrive ONLY via build_model_pipeline''s registry-gated injection (GATE-TRAIN-34), never via this closure'
  touched_by:
  - scikit-learn@1.7.2
  - pandas@2.3.3
  - numpy@2.5.2
  validated_by:
  - tests/test_hpo.py::test_make_objective_returns_target_metric
  - tests/test_hpo.py::test_make_objective_returns_float
  - tests/test_hpo.py::test_hpo_trial_refits_preprocessing_each_trial
  - tests/test_hpo.py::test_hyperopt_smoke_through_pipeline_objective
  - tests/test_hpo.py::test_pre_params_reach_pipeline_params
  if_changed:
  - every trial objective value and hence leaderboard ordering
  - broadway_metrics schema consumed by GATE-HPO-83
  - pre__<step>__<param> addressing contract
  findings:
  - 'metrics[target_metric] KeyError is UNPINNED — no test drives make_objective with a target_metric outside the 7-key vocabulary, so a YAML typo (e.g. ''MAE'') surfaces mid-study as a bare KeyError AFTER trials have burned, never as a config-validation error. root: src/broadway/training/hpo.py:61'
- id: GATE-HPO-82
  phase: hpo-optuna
  order: 99
  owner: 'src/broadway/training/hpo.py:159+:177 study.optimize call sites (run_model_study leg + _optimize_study bandit continuation)'
  inputs:
  - _trial_objective wrapper (hpo.py:99)
  - n_trials budget (initial_trials_per_model or bandit allocation)
  - callbacks list from GATE-HPO-83
  outputs:
  - COMPLETE/PRUNED/FAIL trials appended to ARTIFACT-OPTUNA-STUDY
  transforms:
  - 'suggest dispatch: suggest_int iff BOTH bounds are ints, else suggest_float (:107-111; legacy twin optuna.py:95-99)'
  - non-finite objective → raise optuna.TrialPruned (:112-114; twin optuna.py:100-102) — prune records no value
  - optimize(n_trials=n_trials, callbacks=…) with NO timeout, NO n_jobs (intra-study sequential), NO catch= tuple, gc_after_trial/show_progress_bar at library defaults (:159); bandit continuation appends via a second optimize call (_optimize_study :163-177)
  - 'budget arithmetic: remaining = total_trials − initial_trials_per_model × len(models) (:302), allocated at :303 and executed across threads (:232-257)'
  touched_by:
  - optuna@4.4.0
  validated_by:
  - tests/test_hpo.py::test_run_model_study_sqlite_reopens_and_accumulates
  - tests/test_hpo.py::test_run_hpo_end_to_end
  - tests/test_hpo.py::test_run_hpo_no_bandit_when_budget_exhausted
  - tests/test_hpo.py::test_mlflow_tracking_logs_trials
  if_changed:
  - trial-state semantics under exceptions/pruning
  - round-abort behavior
  - budget math feeding GATE-HPO-85
  findings:
  - 'one raising trial aborts the WHOLE optimize call (no catch=, :159) and the exception propagates out of future.result() during result collection (:229, :256-257) — sibling studies'' completed work is discarded mid-round and run_hpo dies with no leaderboard/summary; no test pins the partial-failure semantics. root: src/broadway/training/hpo.py:159'
  - 'bool params are UNTUNABLE END-TO-END (cross-ref E2E-D6, e2e-lane defect confirmed at clean HEAD 5016e93 and reproduced identically in this working tree) — three layers conspire: (a) the registry vocabulary is NAME-only (allowed_params frozenset, src/broadway/training/models/registry.py:28 advertises fit_intercept/positive/copy_X with no dtype dimension); (b) the suggest dispatch knows only int-vs-float from bound types (:107-111) — there is no suggest_categorical/bool branch; (c) ModelHPOSpec.search_space dict[str, list[float | int]] (src/broadway/config/schema.py:122) SILENTLY COERCES native bool bounds — verified live: [true, false] validates to [1.0, 0.0]. Int-encoded [0, 1] passes schema AND registry validation, then sklearn strict validation raises InvalidParameterError (''fit_intercept … must be an instance of bool … Got 0 instead.'') on EVERY trial sampling those params; a only-bool space aborts optimize() on trial 0 via the no-catch= mechanics above; a mixed space decimates the leaderboard probabilistically. root: src/broadway/training/hpo.py:107-111 (+ src/broadway/config/schema.py:122 silent bool→float coercion, src/broadway/training/models/registry.py:28 dtype-blind vocabulary)'
- id: GATE-HPO-83
  phase: hpo-optuna
  order: 100
  owner: src/broadway/training/hpo.py:66 _mlflow_callback()
  inputs:
  - FrozenTrial states/values/user attrs from the optimize callbacks hook
  - ambient MLFLOW_TRACKING_URI
  - caller mlflow_tags
  outputs:
  - one nested ARTIFACT-MLFLOW-RUN per COMPLETE trial (params + numeric metrics + tags)
  transforms:
  - 'COMPLETE-only guard: non-COMPLETE state or value=None returns BEFORE any logging (:78-79) — pruned/failed trials emit NOTHING'
  - nested run per COMPLETE trial named f'{study_name} trial {n}' with tags {**caller_tags, trial, study} (:80-84)
  - logs trial.params (:85); numeric-filtered broadway_metrics user attr (:86-93); target_metric re-logged explicitly (:94)
  - tracking URI stays AMBIENT (MLFLOW_TRACKING_URI set by caller — docstring :74; k8s caller setup_mlflow at experiments/mlflow/03_optuna_worker.py:189)
  - NO trial.report and NO intermediate_values readers anywhere in the repo (grep census) — pruning is raise-only, no intermediate pruning decisions are ever read back
  touched_by:
  - mlflow@3.15.1
  - optuna@4.4.0
  validated_by:
  - tests/test_hpo.py::test_mlflow_tracking_logs_trials
  - tests/test_hpo.py::test_run_hpo_mlflow_tags
  if_changed:
  - per-trial MLflow run volume/schema
  - nested-run grouping under the study's best run
  - columns of experiments/results/mlflow/01_model_battle_optuna.csv
  findings:
  - 'pruned and failed trials are invisible in BOTH telemetry planes — the callback skips them (:78) and _leaderboard silently DROPS their studies via bare except ValueError (:260-268) — so an all-pruned study leaves no MLflow trace and vanishes from result[''models''] with no warning; the consumer-side silent-empty .get failure mode is ALREADY RULED (cross-ref arbitration board row B-HPO-METRICS-MAPPING, verdicts #23 — not re-ruled here). root: src/broadway/training/hpo.py:78'
- id: GATE-HPO-84
  phase: hpo-optuna
  order: 101
  owner: src/broadway/training/hpo.py:317 best_model min() selection
  inputs:
  - leaderboard from _leaderboard (:260-268)
  - studies dict
  - cfg.experiment.model.type (trainer side)
  outputs:
  - 'result dict {''models'': {name: {best_params, best_value, n_trials}}, ''best_model'', ''best_params'', ''best_value''}'
  transforms:
  - per-model result dicts built ONLY for leaderboard members — invalid models are OMITTED from 'models' silently (:308-316)
  - '''best_model'' = min(leaderboard, key=value) — minimize HARDCODED regardless of configured direction (:317)'
  - return shape fixed at :318-323
  - 'trainer consumption: training/module.py:76-85 models.get(cfg.experiment.model.type) → None → ValueError ''hpo produced no valid trial for model …'' — OWNED BY GATE-TRAIN-33, cross-ref only, not re-ruled'
  - 'battle consumption: experiments/mlflow/01_model_battle_mlflow.py:226-229 run_hpo(battle_pipeline_config(), hpo_cfg, encoded frames, SEED, mlflow_tracking=True); best_params/best_value/best_model logged into the ''hpo_bandit'' run (:230-236); per-model CSV experiments/results/mlflow/01_model_battle_optuna.csv (:245-250)'
  - 'worker consumption: experiments/mlflow/03_optuna_worker.py:195-200 study.best_trial → log_to_mlflow (mae := best.value)'
  touched_by:
  - pandas@2.3.3
  - mlflow@3.15.1
  validated_by:
  - tests/test_hpo.py::test_run_hpo_end_to_end
  - tests/test_hpo.py::test_run_hpo_no_valid_trial_raises
  if_changed:
  - params reaching TrainingResult via GATE-TRAIN-33/34
  - 01_model_battle_optuna.csv schema
  - hpo_bandit MLflow run fields
  findings:
  - 'direction inversion — HPOConfig.direction is an UNCONSTRAINED str (schema.py:127, no Literal), flows verbatim into create_study (:142, :153), yet BOTH ranking planes assume minimize: bandit_allocate sorts ascending (hpo.py:195) and best_model takes min() (:317); setting direction=''maximize'' hands the remaining budget to the WORST models and crowns the worst model best — silently, no guard, no test. root: src/broadway/training/hpo.py:317 (+ src/broadway/config/schema.py:127)'
- id: GATE-HPO-85
  phase: hpo-optuna
  order: 102
  owner: src/broadway/training/hpo.py:180 bandit_allocate()
  inputs:
  - 'leaderboard {model: best objective} (lower=better)'
  - remaining trial budget (:302)
  - top_k (CFG-HPO-SPEC)
  outputs:
  - '{model: n_trials} allocation dict fed to _bandit_round (:304-305)'
  transforms:
  - 'guards: remaining <= 0 OR empty leaderboard OR top_k <= 0 → {} (:193-194)'
  - ranked = names sorted by ASCENDING best value — minimize assumed (:195); selected = ranked[:min(top_k, len)] (:196)
  - divmod even split across selected; remainder +1 goes to the FIRST k selected entries (= best-first) (:197-200)
  - 'pure function: no RNG, no optuna objects — fully table-testable'
  - top_k >= len selects everyone; models outside top-k get zero allocation
  touched_by: []
  validated_by:
  - tests/test_hpo.py::test_bandit_allocate_top_k_selection
  - tests/test_hpo.py::test_bandit_allocate_even_split
  - tests/test_hpo.py::test_bandit_allocate_remainder_to_best
  - tests/test_hpo.py::test_bandit_allocate_remaining_less_than_k
  - tests/test_hpo.py::test_bandit_allocate_top_k_beyond_models
  - tests/test_hpo.py::test_bandit_allocate_empty_leaderboard
  - tests/test_hpo.py::test_bandit_allocate_no_remaining
  - tests/test_hpo.py::test_bandit_allocate_zero_top_k
  if_changed:
  - budget distribution across models
  - tie behavior feeding GATE-HPO-84 selection
  findings: []
- id: GATE-HPO-86
  phase: hpo-optuna
  order: 103
  owner: src/broadway/training/optuna.py:48 run_study_rdb()
  inputs:
  - storage_url string (sqlite:/// locally; postgresql://…:5432/optuna in k8s via compose_db_url)
  - study_name
  - direction
  - optional random_state
  outputs:
  - durable Optuna schema in the RDB (ARTIFACT-OPTUNA-STUDY at rest); stale RUNNING trials flipped FAIL on resume
  - ARTIFACT-OPTUNA-SNAPSHOT dumps under gitignored data/optuna-backup/
  transforms:
  - HEARTBEAT_INTERVAL=60.0 / GRACE_PERIOD=300.0 constants (:17-18) wired into EVERY RDBStorage (optuna.py:63-67 AND hpo.py:144-148) — an interrupted worker's stale RUNNING trial recovers to FAIL on resume (docstring :3-6)
  - 'create-race retry: 8 attempts × 5 s sleep on sqlalchemy IntegrityError/OperationalError (:20-21, :32-45) — mitigates concurrent create_study races'
  - load_if_exists=True ALWAYS (:37, :152) — reopen ACCUMULATES trials, never resets
  - 'snapshot plane (k8s/optuna/lifecycle.sh): dump_state pg_dump -Fc optuna | gzip → $BACKUP_DIR/optuna.sql.gz (:149-165); restore via pg_restore --clean --if-exists (:119-120); view_local restores into a throwaway docker postgres (:195-198); cluster is otherwise EPHEMERAL — down deletes everything (:167-170) — ARTIFACT-OPTUNA-SNAPSHOT is the ONLY durable store'
  - 'naming: ''hpo-{model}'' carries NO dataset/experiment scope (hpo.py:139, experiments/mlflow/03_optuna_worker.py:186) — see FINDING'
  - 'legacy in-memory twin run_study (:76-109): generic search_space dict → suggest dispatch, TrialPruned non-finite (:100-102), empty → ValueError ''HPO study produced no valid trial (N trials)'' (:106-109); ZERO production callers (grep census — imported by tests only)'
  - compose_db_url interpolates driver/user/password/host/port/name into a plain URL string (src/broadway/training/optuna_worker.py:19-21; run_worker :24-36 wraps run_study_rdb and raises 'worker study … produced no valid trial')
  touched_by:
  - optuna@4.4.0
  - sqlalchemy@2.0.40
  - psycopg2-binary@2.9.12
  validated_by:
  - tests/test_optuna_extended.py::test_run_study_rdb_recovers_interrupted_trial
  - tests/test_optuna_extended.py::test_run_worker_no_valid_trial_raises
  - tests/test_training_contracts.py::test_run_study_finds_quadratic_optimum
  - tests/test_loud_failures.py::test_optuna_all_invalid_raises
  - tests/test_optuna_extended.py::test_compose_db_url
  if_changed:
  - resume/zombie-recovery semantics
  - data/optuna-backup snapshot contract
  - study-name collision behavior
  - compose_db_url URL shape (also asserted by experiments.py:447)
  findings:
  - 'study_name COLLIDES across datasets/experiments — ''hpo-lgbm''-style names are globally unique keys in one storage, and load_if_exists makes re-use SILENT; pointing any second experiment at a persistent storage_url appends foreign trials to the existing studies, mixing datasets inside one leaderboard. root: src/broadway/training/hpo.py:139'
  - 'DB credentials reach pod LOGS — compose_db_url bakes the raw password into the URL (optuna_worker.py:21) and the worker prints the composed db_url to stdout as a deliberate ''visibility requirement'' (experiments/mlflow/03_optuna_worker.py:87); the 2026-08-24 secret-audit factsheet covered manifests/templates only, not this runtime print. root: experiments/mlflow/03_optuna_worker.py:87'
- id: GATE-HPO-87
  phase: hpo-optuna
  order: 104
  owner: k8s/optuna/render_worker_jobs.py:26 render_jobs()
  inputs:
  - CFG-HPO-SPEC hpo.models (ONE Job per model)
  - CFG-K8S-OPTUNA-INFRA configmap inline config.yaml
  - optuna-db Secret
  outputs:
  - Job manifests optuna-{display_name} applied by lifecycle.sh run_hpo (k8s/optuna/lifecycle.sh:136-138); optuna-init Job pre-creating studies
  transforms:
  - the HPO spec is the SINGLE SOURCE for which Jobs exist (docstring :3-5; static worker-jobs.yaml removed); renderer is the ONLY generator
  - env BROADWAY_MLFLOW_CONFIG=/app/configs/experiments/mlflow.yaml injected (:47-50); the worker branches on env presence with a parents[2] repo fallback (experiments/mlflow/03_optuna_worker.py:46-49)
  - 'mounts: configMap optuna-config → /etc/broadway/config.yaml subPath readOnly (:52-53); Secret optuna-db → /etc/broadway/secret readOnly (:54-55) — FILE-based credentials, NEVER env vars (module docstring :3-5); load_secret reads DB_USER/DB_PASSWORD/DB_NAME files (03_optuna_worker.py:52-56)'
  - 'Job semantics: restartPolicy Never, backoffLimit 2, ttlSecondsAfterFinished 120 (:36-40); init-Job twin ttl 600 with args [--init-only] materializing empty studies via _dummy_objective returning 0.0 (k8s/optuna/optuna-init.yaml:10-20; 03_optuna_worker.py:74-76, :165-171)'
  - image tag injectable --image for CD pinning (:71; consumed via OPTUNA_WORKER_IMAGE at lifecycle.sh:135-137)
  - 'image plane: Dockerfile.worker mirrors the repo layout and sets PYTHONPATH=/app/src:/app (:13-16), BAKES the dataset parquet (:11-12) plus configs/experiments/{mlflow,working}.yaml (:9-10); base image uv sync --frozen (Dockerfile.base:10)'
  - 'preflight loud-exit: SQLAlchemy connect + MlflowClient().search_experiments BEFORE any trial, sys.exit(1) with FATAL banner on either failure (03_optuna_worker.py:118-138)'
  - 'orchestration: lifecycle.sh pipes renderer | kubectl apply (:136-138), waits condition=complete per job with 1200 s timeout, FAILED → job-log tail + exit 1 (:140-145); separate optuna/mlflow databases mandated because both use Alembic''s alembic_version (configmap.yaml:3-4, postgres.yaml:1-3)'
  - kubeconform scope hole (k8s/optuna/-only) is GATE-INFRA-99's existing finding — referenced, NOT re-found
  touched_by:
  - pyyaml@6.0.2
  - scikit-learn@1.7.2
  - mlflow@3.15.1
  validated_by: []
  if_changed:
  - Job manifest shape/env/mounts
  - image layout contract (PYTHONPATH, baked parquet)
  - preflight exit codes
  - CI docker-only verdicts (scripts/run_local_ci.sh:57-104)
  findings:
  - 'the ENTIRE worker SCRIPT plane has zero pytest coverage — render_worker_jobs.py, 03_optuna_worker.py main/preflight/load_dataset/log_to_mlflow, and lifecycle.sh are exercised only by CI shell syntax checks + kubeconform shape validation; the LIBRARY functions beneath them (compose_db_url, run_worker, run_model_study, run_study_rdb) ARE pinned, so regressions in script wiring (arg names, env var names, mount paths, endpoint printing) land green. root: k8s/optuna/render_worker_jobs.py:69 main()'
  - 'worker pods hold FULL MLflow write power — configmap tracking_uri http://mlflow:5000 plus allowed_hosts ''*'' (k8s/optuna/configmap.yaml:27, :29) hands every optuna worker ambient write access to the whole tracking store INCLUDING the champion alias, so any compromised/buggy pod can move production custody; single-writer custody for the champion alias is ALREADY PROPOSED upstream (cross-ref GH-5 / PROPOSE-GH5-01, agents/ledger/arbitration/2026-08-24/gap-cd-mlflow.md:108 — cited, not re-found). root: k8s/optuna/configmap.yaml:27'
- id: GATE-HPO-88
  phase: hpo-optuna
  order: 105
  owner: src/broadway/training/hpo.py:204 _initial_round()
  inputs:
  - random_state base seed
  - hpo.models list ORDER
  - CFG-HPO-SPEC / CFG-HPO-EXPERIMENT seed literals upstream
  outputs:
  - deterministic per-model sampler seeds; reproducible trajectories across runs/processes
  transforms:
  - per-model sampler seed = random_state + model_index over specs INSERTION ORDER (:212-228, offset at :223) — identical search spaces diverge deterministically (docstring :5-7)
  - parallel==sequential pinned by swapping ThreadPoolExecutor for an eager executor (tests/test_hpo.py::test_run_hpo_parallel_equals_sequential); intra-study execution sequential EVERYWHERE (no optimize n_jobs>1 call site)
  - 'seed literals feeding the HPO data path: configs/experiments/mlflow.yaml seed:42 (:11) → experiments/mlflow/03_optuna_worker.py:156 + train_test_split(random_state=seed) (:176-177); configs/experiment/hyperopt.yaml random_state:42 (:21) → training/module.py:73 passes experiment.random_state into run_hpo; battle sample/split SEED=42 (experiments/mlflow/_common.py:69, :113, :120)'
  - sampler reproducibility contract stated as Optuna-4.x-constructor-only (src/broadway/training/optuna.py:56-62); reopened-trajectory pins carry OBSERVED magic numbers ('x == 3.943281 observed over 10 runs', tests/test_optuna_extended.py:32-35 and :74-75) with deliberately tight bands
  - 'hazards NOT guarded: lgbm/xgb boosting randomness relies solely on registry random_state injection (GATE-TRAIN-34 pin); TPE without constant_liar under threaded RDB sharing (GATE-HPO-80 finding); host BLAS thread counts untouched'
  touched_by:
  - numpy@2.5.2
  - optuna@4.4.0
  validated_by:
  - tests/test_hpo.py::test_run_hpo_deterministic
  - tests/test_hpo.py::test_run_hpo_parallel_equals_sequential
  - tests/test_optuna_extended.py::test_run_study_rdb_load_if_exists_reopens
  if_changed:
  - every reproducibility claim (hpo.py docstring :5-7, optuna.py :56-62)
  - resumed-study trajectories
  - determinism ledger RNG-family rows
  findings:
  - 'the seed OFFSET couples determinism to MODEL LIST ORDER — inserting, removing, or reordering hpo.models shifts EVERY subsequent model''s sampler seed (:212-228), silently changing all trajectories while every determinism test stays green (they freeze the whole config, not the offset scheme); no test pins the offset convention itself. root: src/broadway/training/hpo.py:223'
- id: GATE-HPO-89
  phase: hpo-optuna
  order: 106
  owner: src/broadway/config/schema.py:125 HPOConfig
  inputs:
  - 'configs/experiments/mlflow.yaml hpo: block (:14-44) = CFG-HPO-SPEC'
  - ExperimentConfig.hpo (experiment-embedded twin, e.g. configs/experiment/hyperopt.yaml:23-36) = CFG-HPO-EXPERIMENT
  - k8s configmap inline config.yaml = CFG-K8S-OPTUNA-INFRA (infra keys only)
  outputs:
  - typed HPOConfig{engine, direction, total_trials, initial_trials_per_model, top_k, target_metric, models[].search_space, storage_url}
  transforms:
  - 'keys consumed: engine (:126 — see FINDING), direction (:127 → create_study), total_trials (:128 → remaining arithmetic hpo.py:302), initial_trials_per_model (:129), top_k (:130), target_metric (:131 → metrics[target_metric] hpo.py:61), models[].name+search_space (ModelHPOSpec :119-122, extra=''forbid''), storage_url (:133 → in-memory vs RDB switch hpo.py:141)'
  - 'search-space validation: per-model against registry allowed_params with the pre__ prefix exempt (ExperimentConfig._validate_hpo_search_space :197-215; PRE_PARAM_PREFIX :181-184)'
  - 'committed instance YAMLs: configs/experiments/mlflow.yaml carries lgbm/xgb spaces (:22-42) and a DELIBERATELY EMPTY linear space (:43-44 — constant-objective trials, accepted design); storage_url absent from BOTH YAMLs → local runs are in-memory, k8s injects the RDB URL at runtime only (header :5-8)'
  - 'verify-command census does NOT cover the hpo block: validate_configs checks mlflow.yaml sample_size/test_fraction/seed/features keys (experiments.py:381-385) and CFG-K8S-OPTUNA-INFRA keys dataset/databases/mlflow (experiments.py:46-47, :373-380) — a malformed or dropped hpo block passes `experiments.py verify`'
  touched_by:
  - pydantic@2.11.7
  - pyyaml@6.0.2
  validated_by:
  - tests/test_config.py::test_hpo_search_space_valid_for_lgbm
  - tests/test_config.py::test_hpo_search_space_invalid_for_linear_raises
  - tests/test_config.py::test_hpo_search_space_validated_per_model
  if_changed:
  - CFG-HPO-SPEC / CFG-HPO-EXPERIMENT schemas
  - budgets and spaces reaching GATE-HPO-80..85
  - registry allowed_params coupling (GATE-TRAIN-34)
  findings:
  - 'HPOConfig schema laxity — no extra=''forbid'' (:125-133, unlike siblings ModelHPOSpec :120 / DataSourceRef :146 / PreprocessingStepConfig :175) AND no Field bounds (contrast SplitConfig.validation_size ge/le :116): unknown keys are swallowed silently, and negative/zero total_trials / initial_trials_per_model / top_k pass pydantic and detonate later in run_hpo arithmetic (:302) — only top_k<=0 / remaining<=0 have functional guards (hpo.py:193). root: src/broadway/config/schema.py:125'
  - 'engine is DEAD CONFIG — declared REQUIRED (:126), set in both YAMLs (configs/experiments/mlflow.yaml:15, configs/experiment/hyperopt.yaml:24), echoed in fixtures (tests/test_config.py:138, :172), and READ BY NOTHING (repo-wide grep census finds no consumer of HPOConfig.engine); it implies a pluggable-engine surface that does not exist and would mask a future engine-vs-implementation mismatch. root: src/broadway/config/schema.py:126'
- id: GATE-INFRA-90
  phase: infra-meta
  order: 107
  owner: scripts/run_local_ci.sh:16 case-dispatch STATIC/TIER/CLEAN_LINT (+ run() aggregator :26-31, banner law :23/:100)
  inputs:
  - argv[1] ∈ {unset, --static, --tier=fast, --tier=full, --clean-lint} (else usage exit 2)
  - sub-gate verdicts via run() <name> <cmd...>
  outputs:
  - banner FAST-GREEN | LOCAL-CI GREEN | LOCAL-CI RED
  - exit 0 green / 1 red / 2 usage
  transforms:
  - --static OR --tier=fast suppresses the pytest+cov AND project-tests block (guard :94 `$STATIC -eq 0 && $TIER == "full"`)
  - '--clean-lint opt-in mode (:20, :32-61; ratified teeth ⑤ SNAPSHOT-LINT): ruff+mypy dispatch through a throwaway detached-HEAD worktree snapshot OUTSIDE the repo (ensure_clean_snapshot :38-45, built eagerly at :61, EXIT-trap teardown :42) so shared-tree WIP cannot false-red/green lint; every other gate stays tree-bound, pytest deliberately so (:35)'
  - run() never stops at first failure — aggregates fail=1 across all tiers (:25-31)
  - banner vocabularies deliberately distinct per tier (:23)
  touched_by:
  - .github/workflows/ci.yml:47 (invokes bare → full tier)
  - .git/hooks/pre-push:5
  - scripts/ship.sh:24
  - agents/contracts/MAIN_AGENT_CONTRACT.md §Ship-path law
  - 'agents/ledger/DECISIONS.md D16a/D26 (gate-list SSOT law: edit HERE, never YAML)'
  validated_by: []
  if_changed:
  - scripts/run_local_ci.sh
  - pyproject.toml
  - uv.lock
  findings:
  - no test executes this script end-to-end — tier parsing (:15-21), --static/--tier suppression logic (:94), --clean-lint snapshot machinery (:36-61), run() aggregation/banner emission (:26-31,:99-103) have zero coverage; tests only read its TEXT (see GATE-INFRA-91/92).
  - --static alone skips pytest+cov on a nominally full tier; scope discipline ("static is for iteration, not landing") is contract prose, not enforced by code.
- id: GATE-INFRA-91
  phase: infra-meta
  order: 108
  owner: scripts/run_local_ci.sh:30 gate_parity() (F1b pin-guard, wired at :43)
  inputs:
  - refs/remotes/origin/sklearn:scripts/check_branch_parity.sh via `git show` (:33)
  outputs:
  - parity sub-verdict to run() aggregator
  - 'FAIL parity (F1b): origin/sklearn unavailable | legacy pre-D16 checker on track ref'
  transforms:
  - pins execution to the TRACK REF copy, not the tree-local checker (D21)
  - rejects any fetched checker lacking inline `^PARITY_ERA=` marker (:36) — a stale checkout of main cannot gate CI with the pre-D16 legacy script
  - executes pinned copy under bash and propagates its rc (:39-41)
  touched_by:
  - scripts/check_branch_parity.sh:111 (the marker line itself)
  - agents/ledger/DECISIONS.md D21 (F1b closure)
  validated_by:
  - tests/test_branch_parity_scripts.py::test_f1b_guard_rejects_legacy_checker_without_era_marker[legacy]
  - tests/test_branch_parity_scripts.py::test_f1b_guard_rejects_legacy_checker_without_era_marker[post-D21]
  - tests/test_branch_parity_scripts.py::test_scripts_diff_empty_vs_main (wiring assert 'run parity gate_parity' via _gate_parity_source :122-138)
  if_changed:
  - scripts/check_branch_parity.sh
  - refs/remotes/origin/sklearn
  findings:
  - validation extracts-and-executes the real function body against a PATH-shimmed git — the true `git show refs/remotes/origin/sklearn:...` network path is never exercised in CI.
- id: GATE-INFRA-92
  phase: infra-meta
  order: 109
  owner: scripts/run_local_ci.sh:81 gate battery (ruff :81-84 incl. scripts/, mypy :85, configs :86-90, shell-scripts :91-93 both dirs, pytest+cov floor=95 :95-96, project-tests :97)
  inputs:
  - src/** tests/** experiments/mlflow experiments/fare_prediction experiments.py experiments_ui.py project/working.py project/data.py scripts/ (ruff)
  - src/broadway/** (mypy)
  - configs/experiment/*.yaml via load_config(dataset='test') (configs)
  - k8s/optuna/*.sh + scripts/*.sh via sh -n + shellcheck (shell-scripts)
  - tests/** (pytest -n 4 --dist worksteal, --cov-fail-under=95)
  - 'project/tests/** (project-tests: full tier only, -q --dist worksteal, NO coverage flags)'
  outputs:
  - PASS/FAIL ruff|mypy|configs|shell-scripts|pytest|project-tests banners + 40-line tails
  - cov floor breach ⇒ FAIL pytest
  transforms:
  - ruff/mypy/configs/shell-scripts run in EVERY tier incl. fast/static
  - 'shell-scripts widened to BOTH glob dirs (:93) — sh -n + shellcheck over k8s/optuna/*.sh AND scripts/*.sh, command-identical to ci.yml ''Shell scripts'' step (gate-divergence law comment :91)'
  - 'project-tests step (:97, full-tier-only): uv run pytest project/tests -q --dist worksteal with NO coverage flags — the >=95 floor deliberately stays on tests/ (header :7-8); closes the data-gate hole (human ruling): 26 tests / 6 files collected+passing at HEAD eb9ea18'
  - coverage floor value 95 owned here; docs must quote it identically
  - configs gate asserts non-empty glob then loads every experiment config
  touched_by:
  - agents/ledger/DECISIONS.md D26 (senior adoption makes gate-list edits executable)
  - tests/test_governance_probes.py:287 probe_coverage_floor (floor-quote SSOT check)
  validated_by:
  - tests/test_governance_probes.py::test_probe_d_live_floor_quotes_match_gate_script
  - tests/test_governance_probes.py::test_probe_d_red_stale_floor_quote
  if_changed:
  - src/
  - tests/
  - project/working.py
  - project/data.py
  - experiments.py
  - experiments_ui.py
  - experiments/mlflow/
  - experiments/fare_prediction/
  - configs/experiment/
  - configs/dataset/test.yaml
  - scripts/
  findings:
  - mypy covers src/broadway ONLY; ruff covers just two project/ files — project/etl/process.py etc. lint/type-check in NO tier.
  - data-gate hole CLOSED at this landing: project/tests executes via project-tests in every FULL tier incl. CI (ci.yml:47 invokes the bare SSOT script); fast/static tiers still skip it by design.
  - probes validate the floor NUMBER as text, not enforcement behavior — no test proves a sub-95 cov run actually fails.
- id: GATE-INFRA-93
  phase: infra-meta
  order: 110
  owner: scripts/check_branch_parity.sh:71 check() (SHARED lockstep, list at :43-69) + sync_to_main() :89
  inputs:
  - '25-entry SHARED surface: src/ tests/ demo/ configs/dataset/test.yaml configs/experiment/{baseline,engineered,hyperopt}.yaml configs/analysis/{test,test_hypothesis,test_causal}.yaml configs/step/{causal,etl}.yaml configs/environment/ configs/flow/ k8s/ docker/ .github/workflows/ pyproject.toml Dockerfile docker-compose.yml .gitignore .dockerignore README.md scripts/ experiments/more_modeling/'
  - origin/main vs origin/taxi tips
  outputs:
  - 'PARITY OK | DRIFT: <path> differs … PARITY FAILED — run $0 --sync'
  - 'sync mode: taxi→main checkout + deletion mirror (:89-103)'
  transforms:
  - byte-identical git diff --exit-code per SHARED path, deletions included (:77)
  - anything NOT listed is deliberately single-branch (:39-41)
  - --sync refused outside main era (dev dispatch :202-205)
  - 'RGC-1 custody flip (gap-foreign ratification checklist): experiments/more_modeling/ JOINED the declared surface — the {16..22} batch landed by human ruling, so its directory is no longer foreign-by-omission; gate met: checker passes WITH the entry (dev-era custody anchor-diff green; origin/main sits at PARITY_MAIN_ANCHOR)'
  touched_by:
  - scripts/run_local_ci.sh:30 (consumes pinned copy of THIS file)
  - tests/test_branch_parity_scripts.py:66 (parses SHARED array)
  - agents/contracts/MAIN_AGENT_CONTRACT.md §2 (branch model)
  validated_by:
  - tests/test_branch_parity_scripts.py::test_parity_surface_includes_scripts
  - tests/test_branch_parity_scripts.py::test_scripts_diff_empty_vs_main
  if_changed:
  - any SHARED[] path
  - refs origin/main, origin/taxi
  findings:
  - test_scripts_diff_empty_vs_main is era-gated and SKIPS during dev era — the stock check() comparison path never executes in dev-era CI; only the SHARED-array text shape is tested.
  - sync_to_main() (checkout/rm/deletion-mirror machinery) has zero test coverage anywhere.
- id: GATE-INFRA-94
  phase: infra-meta
  order: 111
  owner: scripts/check_branch_parity.sh:111 inline era declaration PARITY_ERA/PARITY_TRACK_BRANCH/PARITY_ALLOWLIST/PARITY_MAIN_ANCHOR (:111-114) + anchor guards :121-132 + dev-era dispatch :192-226
  inputs:
  - inline constants (no env dialect, D21)
  - GITHUB_REF_NAME else `git rev-parse --abbrev-ref HEAD` (:187)
  - origin refs
  outputs:
  - PARITY OK (era=… branch=…) | FATAL anchor shape/resolution errors | TAXI DRIFT | FORK | REFUSED (--sync off-era)
  transforms:
  - anchor must be full 40-hex AND resolve to a commit — garbled pin fails as CONFIG ERROR up front (:121-128)
  - era vocabulary restricted to {dev,main} (:129-132); editing these lines IS the main-day flip act (D16c)
  - 'dev era: custody FIRST on every event/branch (:206), then branch-aware pass-along — push-to-taxi requires byte-identical fast-forward (:212), push-to-track requires origin/taxi ancestor (never fork, :218), empty/main/other refs custody-only'
  touched_by:
  - scripts/run_local_ci.sh:36 (marker grep keys on ^PARITY_ERA=)
  - agents/ledger/DECISIONS.md D16/D21 (design + relocation)
  validated_by:
  - tests/test_branch_parity_scripts.py::test_parity_surface_includes_scripts (asserts parity-era.env NOT in SHARED)
  - tests/test_branch_parity_scripts.py::_declared_era via test_scripts_diff_empty_vs_main (parses ^PARITY_ERA= line)
  if_changed:
  - scripts/check_branch_parity.sh
  - refs/remotes/origin/taxi
  - refs/remotes/origin/sklearn
  - GITHUB_REF_NAME
  findings:
  - anchor shape/resolution guards (:121-128) and both dev-era pass-along guards (:212,:218) are asserted nowhere — no test feeds a non-ancestor taxi or a drifted taxi tip through the dispatch.
- id: GATE-INFRA-95
  phase: infra-meta
  order: 112
  owner: scripts/check_branch_parity.sh:134 custody() — layer 1 anchor-drift diff :146, freeze-intact shortcut :162, layer 2 blob-provenance comm -23 :166
  inputs:
  - PARITY_MAIN_ANCHOR=18607091ddbb2602ad4475341ad377bafee5ec4b (:114)
  - origin/main SHARED subtree
  - origin/sklearn object universe (rev-list --objects :178)
  - PARITY_ALLOWLIST=() prefix skips (:168-177)
  outputs:
  - 'ROGUE MAIN WRITE: frozen main changed since anchor … (adds/deletes/mods)'
  - 'ROGUE MAIN WRITE: novel blob(s) absent from track universe (head -10)'
  - silent return 0 via shortcut when main==anchor
  transforms:
  - 'layer 1: diff anchor..origin/main over SHARED catches adds/deletes/mods/smuggling (:146-149)'
  - 'shortcut: main==anchor ⇒ return before layer 2 — disjoint histories make the universe test false-positive forever otherwise (:155-164)'
  - 'layer 2: any blob on moved-off-anchor main that exists NOWHERE in sklearn''s universe IS rogue (novel-content adds)'
  touched_by:
  - scripts/run_local_ci.sh:30 (runs pinned custody on every CI event)
  - agents/ledger/DECISIONS.md D16b/F2-revised
  validated_by: []
  if_changed:
  - origin/main (any move off the anchor re-arms layer 2)
  - PARITY_MAIN_ANCHOR constant
  - origin/sklearn universe
  findings:
  - custody() has ZERO test coverage — neither alarm is exercised by any node; verified live that origin/main == anchor today, so BOTH layers are shortcut-bypassed on every current run (correct-by-construction but unproven).
  - stale-pin failure direction is documented safe (false-red AFTER an unanchored ratified change), yet no test pins that behavior either.
- id: GATE-INFRA-96
  phase: infra-meta
  order: 113
  owner: 'scripts/ship.sh:24 ship gate (`if ! bash scripts/run_local_ci.sh` → refuse :24-27; single push later) + WAVE-A teeth insertions (:15-19 tier_gate sourcing, :31-40 L1 hook guard, :42-53 TIER-GATE batch) mirrored by .git/hooks/pre-push:5'
  inputs:
  - argv remote/refspec (default origin sklearn:sklearn :14)
  - full-tier verdict of scripts/run_local_ci.sh
  outputs:
  - 'SHIP OK | SHIP REFUSED: LOCAL-CI RED … exit 1'
  - single git push invocation → one hook gate (:25)
  transforms:
  - push lines reachable ONLY on run_local_ci.sh exit 0 — keyword-grep gating and newline-chained pushes structurally impossible (born of two 2026-08-24 push-on-red recurrences)
  - set -euo pipefail (:8); no override flag exists by design
  - hook re-runs the SAME full tier fail-closed server-side-of-your-fingers; sole bypass is visible `git push --no-verify`
  - 'WAVE-A teeth ① insertions: sources scripts/tier_gate.sh (:17); L1 pre-push hook guard VERIFIES (never auto-installs) that .git/hooks/pre-push execs run_local_ci.sh, refusing with manual-install remediation naming agents/contracts/hooks-pre-push.template (:31-40); TIER-GATE runs tg_run over the origin/$branch..HEAD unpushed batch — atomic all-or-nothing, skipped with a SHIP NOTICE when no remote baseline exists (:42-53)'
  - 'TIER-GATE grammar custody lands HERE as FILE-law co-aspect (distinct symbols, one ship-path artifact): scripts/tier_gate.sh tg_* library (_TG_TIERS :20, tg_check_message :64, tg_run :96) implements the D35(2) two-key rule — Tier ∈ {FULL,FAST,STATIC,DOCS} case-sensitive; FULL resolves Reviewer: against the EVENTS section AT THAT SHA plus the event-id echoed in the message. RESOLUTION SEMANTICS (fix-forward commit after the gate refused its own maiden batch): tg_events_resolves accepts the token as a row id OR as an exact word occurrence inside the EVENTS body — i.e. via an authority-class registration row whose stated scope is "valid Reviewer:-trailer resolution target" (row 39de4245 registers 2d9ab1a1 exactly so); unregistered ids still fail closed'
  - 'POSTURE RIDER (mint deferred): tier_gate.sh has zero automated validation (no test executes any tg_*) and scripts/deadcode_census.py (teeth ⑥, advisory-never-gate by ratified law) is wired to NO tier — a dedicated registry row is OWED but ids are EXHAUSTED through 153 (burned incl.) under the suite''s BAND_RANGES law; minting awaits the band-range extension owned outside ledger custody'
  touched_by:
  - agents/contracts/MAIN_AGENT_CONTRACT.md §Ship-path law (:148-155)
  - agents/ledger/DECISIONS.md D24/D25 (ship semantics + PUSH-STANDING-GO compensating control i)
  validated_by: []
  if_changed:
  - scripts/run_local_ci.sh
  - scripts/ship.sh
  - scripts/tier_gate.sh
  - .git/hooks/pre-push
  - agents/contracts/hooks-pre-push.template
  - REFSPEC arguments
  findings:
  - ship.sh has zero automated validation — no test imports or executes it; its exit-code law lives entirely in contract prose + DECISIONS.
  - '.git/hooks/pre-push remains UNTRACKED machine-local (under .git/, invisible to git status; core.hooksPath unset) — every fresh clone ships with NO local gate; WAVE-A adds the tracked SSOT template (agents/contracts/hooks-pre-push.template) plus a ship-path REFUSAL when the live hook is missing/stale (:31-40), so the gap is now detected-and-named instead of procedural-only.'
- id: GATE-INFRA-97
  phase: infra-meta
  order: 114
  owner: scripts/check_e2e_determinism.sh:106 compare_trees() (json_diff whitelist :35-46, compare_file :89, --run chain run_e2e :163)
  inputs:
  - two artifact trees (positional) or --run
  - JSON leaves after canonical sorted-keys re-dump
  - whitelist EXACT={trace.created_at, artifact_path, train_time_seconds, promote, reason, warnings} :45 + PATTERN comparison.metrics.<m>.{champion,delta,delta_pct} :46
  outputs:
  - 'DETERMINISM OK + exit 0 | one `<file>: <field-path>` line per offender + exit 1 | `<file>: missing counterpart` | usage exit 2'
  transforms:
  - non-JSON files byte-compared via cmp (:91-96)
  - missing counterpart files on EITHER side fail (comm -23/-13 :113-120)
  - champion-derived delta fields exempt — identical invocations vs stale registry differ there by design
  - --run executes ds-pipeline full --dataset test --experiment baseline --analysis test twice against an ephemeral scratch MLflow server, configs copied+tracking-uri sed'd, never writing repo tree (:163-195)
  touched_by:
  - SKLEARN_PIPELINES.md §End-to-end verification criteria (structural rendering target)
  - champion registry state (feeds exempt delta fields)
  validated_by:
  - tests/test_check_e2e_determinism.py::test_identical_trees_report_ok
  - tests/test_check_e2e_determinism.py::test_differing_numerics_fail_with_field_path
  - tests/test_check_e2e_determinism.py::test_only_whitelisted_fields_may_differ
  - tests/test_check_e2e_determinism.py::test_champion_derived_delta_fields_may_differ
  - tests/test_check_e2e_determinism.py::test_missing_counterpart_file_is_reported
  - tests/test_check_e2e_determinism.py::test_train_time_seconds_whitelisted
  if_changed:
  - scripts/check_e2e_determinism.sh
  - artifacts trees
  - configs/environment/development.yaml
  - champion registry state
  findings:
  - the comparator core is well-tested but the --run runner (ephemeral MLflow boot, port pick, sed rewrite) has NO automated coverage; also this script is wired into NO tier of run_local_ci.sh nor ci.yml — determinism is checked only when someone remembers to run it.
- id: GATE-INFRA-98
  phase: infra-meta
  order: 115
  owner: scripts/tier_classifier.py:104 classify() (triggers :51-64, parse_diff_payload :138, CLI main :162)
  inputs:
  - git-diff payload on stdin (files + added lines)
  - governance basenames {CONTRACT_TEMPLATE,WORKER_CONTRACT,MAIN_AGENT_CONTRACT,DECISIONS,FIXES}.md + agents/ledger/STATE.md (:34-37)
  - behavior prefixes src/ tests/ project/ experiments/ scripts/ .github/ k8s/ + pyproject.toml uv.lock *.sh docker* + configs/*.yaml (:40-43)
  outputs:
  - '{"tier": FULL|CHECKLIST, "reasons": [...]} JSON'
  transforms:
  - 'defaults UP to FULL on ANY of: governance file, behavior surface, content trigger (sha-like-token, gate-number, backticked-path, shell-ish-line, fenced-code), unclassifiable path, empty payload, mixed signals (:127-135)'
  - CHECKLIST only when ALL files are classifiable prose (.md/.txt/.rst) and zero triggers fired
  - deliberately simple trigger regexes — hex English words like deadbeef match; false positives point UP (:53-55)
  touched_by:
  - tests/test_governance_probes.py (mechanical probe battery consumer)
  - 'agents/contracts/MAIN_AGENT_CONTRACT.md §6 (MAIN-AGENT-only Tier: trailer at staging)'
  validated_by:
  - tests/test_governance_probes.py::test_classifier_each_trigger_fires
  - tests/test_governance_probes.py::test_classifier_governance_file_forces_full
  - tests/test_governance_probes.py::test_classifier_behavior_surface_full
  - tests/test_governance_probes.py::test_classifier_descriptive_prose_is_checklist
  - tests/test_governance_probes.py::test_classifier_unknown_and_empty_default_up
  - tests/test_governance_probes.py::test_classifier_mixed_signals_aggregate_to_full
  - tests/test_governance_probes.py::test_classifier_cli_end_to_end
  if_changed:
  - any diff payload content
  - NEVER_PROSE/BEHAVIOR tables (:34-43)
  findings:
  - 'nothing enforces that committed commits actually CARRY a computed Tier: trailer — the classifier classifies when invoked, but no gate invokes it automatically on staging.'
- id: GATE-INFRA-99
  phase: infra-meta
  order: 116
  owner: '.github/workflows/ci.yml:47 platform job step "Platform gates (SSOT)" (delegation law :42-46; docker-only checks :51-66; build-and-boot :151; CD job :310; concurrency :11-13)'
  inputs:
  - push/PR to main|taxi|sklearn (:3-7)
  - fetch-depth 0 for parity tips (:28)
  - uv sync --all-extras --frozen (:40)
  - bash scripts/run_local_ci.sh (no args ⇒ full tier)
  outputs:
  - platform job verdict (parity+ruff+mypy+configs+shell-scripts+pytest+cov≥95+project-tests via SSOT script)
  - 'docker-only verdicts: sh -n + shellcheck k8s/optuna/*.sh AND scripts/*.sh (:57-66), kubeconform -strict k8s/optuna/ minus kind-config.yaml (:79), orchestrator dry-run render+kubeconform (:104)'
  - sha-tagged images built/boot-tested; CD publishes bit-for-bit verified tarball to GHCR on main/taxi pushes only (:290-376)
  transforms:
  - gate list lives in the SCRIPT — editing YAML alone reopens two-source drift (D16d C7 rejection, header :3-4)
  - 'Shell-scripts step (:57-66) mirrors the SSOT script''s shell-scripts gate COMMAND-IDENTICALLY (for-loop sh -n + shellcheck over BOTH glob dirs; gate-divergence-law comment :55-56) — edit the SCRIPT, never YAML (D16a/D16d)'
  - cancel-in-progress:true keyed workflow-ref cancels superseded runs (:11-13); CD job separately cancel-in-progress:false serialized per ref (:255-257)
  - only broadway-base cached, key hashes deps AND src/project code (:148) — prevents stale-code base + mislabeled-provenance ship
  touched_by:
  - scripts/run_local_ci.sh header (CI-only roster naming)
  - agents/ledger/DECISIONS.md:327 D25 ADDENDUM (supersedes 'last CI-on-tip green' conjunct)
  - DEPLOY-DIFF lane (kubeconform scope ruling)
  validated_by: []
  if_changed:
  - .github/workflows/ci.yml
  - scripts/run_local_ci.sh
  - k8s/optuna/**
  - Dockerfile.base
  - src/**
  - project/**
  - github.ref/event inputs
  findings:
  - kubeconform scope hardcoded to k8s/optuna/ (:80) — a future sibling directory under k8s/ is silently unscanned; kind-config.yaml excluded via ignore-pattern (:79).
  - 'cancel-in-progress × D25 push-always (DECISIONS.md:320-333): rapid successive pushes cancel the in-flight run, so a superseded tip''s CI verdict is structurally unobservable — D25 formally supersedes the "last CI-on-tip green" push conjunct; compensation (ship.sh local GREEN + monitored post-push run + fix-forward/revert recorded in FIXES.md) is procedural, not technical.'
  - no test reads or validates ci.yml itself — delegation, action-pinned SHAs, and job wiring are entirely unguarded by the suite.
# --- SENIOR ARBITRATION CYCLE #2 LANDING (2026-08-24) --------------------
# Appended region: ids continue the global dense order at 100..153 (slate §0).
# Burned ids NEVER reused: 121, 130, 132, 134, 135, 136, 140, 153. Not landed
# as standalone gates either: 106 MERGE-WITH-CFG-75 · 109 demoted to rider on
# GATE-INGEST-03 (packet F #18) · 110 OVERRULE→NOT-GATE (packet F #19) ·
# 115 OVERRULE→dead-code-DELETE-referral, executed this commit (packet G D5).
# BAND 81 HEADER LAW (packet H, verbatim): "From 81 onward, fragment numbers
# are ordinal band identifiers decoupled from id decades; 80-hpo-optuna is the
# last decade-aligned band."
# BAND 81 LAW-FIRST TEXT (CUST-153 refused AS MINTED — packet H #153): every
# external-store object creation (MLflow constructors, optuna create_study,
# git-tag mints, cache-root forks) must route through a NAMED chokepoint, and
# a created object earns exactly one append-only ledger row; re-mint at >=154
# when tracked_run exists with real owners + a named ledger consumer.
- id: GATE-INFRA-122
  phase: infra-meta
  order: 117
  owner: 'experiments.py:495 main argparse dispatcher (ols|diagnostics|qq_legend|verify)'
  inputs:
  - argv subcommand
  outputs:
  - plots/results CSVs/verification JSON (self-auditing verify subcommand)
  transforms:
  - 'root experiments CLI dispatcher custody: add_parser ols/:495 diagnostics/:497 qq_legend/:499 verify/:501 (verified live, packet H #122) — real write-surface, zero coverage elsewhere'
  touched_by: []
  validated_by: []
  if_changed:
  - subcommand result-file layout under experiments/results/
  findings: []
- id: GATE-INFRA-123
  phase: infra-meta
  order: 118
  owner: 'experiments_ui.py:893 __main__ uvicorn.run entry'
  inputs:
  - host/port argv
  outputs:
  - 'dashboard server (uvicorn.run(app, host="127.0.0.1", port=8000) :894)'
  transforms:
  - 'server-entry custody verified (packet H #123); co-tenancy with GATE-SURF-102 (FastAPI app custody) legal: entry vs app-body aspects'
  touched_by: []
  validated_by: []
  if_changed:
  - bind host/port defaults consumed by dashboard users
  findings: []
- id: GATE-INFRA-124
  phase: infra-meta
  order: 119
  owner: 'k8s/optuna/lifecycle.sh:221 train|up|view|dump|down case dispatch'
  inputs:
  - subcommand
  outputs:
  - cluster/state/view lifecycle actions (cluster_up/restore_state/run_hpo/dump_state/cluster_down/view_local)
  transforms:
  - 'command-surface gate incl SUBCOMMAND SYMMETRY LAW (packet H #124 AMENDMENT, binding): every host-object-creating verb must own its teardown pair — up↔down pairing exists; view↔stop pairing MISSING'
  - 'view_local backgrounds mlflow server ( cd "$ROOT" && uv run mlflow server … & ) :205-208 with NO stop verb in the dispatch table; cleanup is an echoed hint only'
  - 'AMENDMENT: the stop subcommand is a BINDING part of this gate, not a rider — capture child PID to $BACKUP_DIR/view.pid, keep pgrep fallback, and fix :215 where a doubled echo prints the hint literally'
  touched_by: []
  validated_by: []
  if_changed:
  - BACKUP_DIR generation layout (co-owned by GATE-CUST-151 keep-N cap)
  findings:
  - 'FINDING(:215): doubled-echo bug prints the stop hint literally instead of executing it. The orphaned broadway-view-pg container (gone by ruling time) was this function''s own product — the pairing gap is PROVEN, not hypothetical.'
- id: GATE-INFRA-125
  phase: infra-meta
  order: 120
  owner: 'k8s/optuna/optuna-init.yaml:19 init-job container command'
  inputs:
  - init-job manifest apply
  outputs:
  - 'python /app/worker.py --init-only execution (:19-20)'
  transforms:
  - 'container command custody: structurally kubeconform-valid today (inside k8s/optuna scope), semantically owned by nobody before this row (packet H #125)'
  touched_by: []
  validated_by: []
  if_changed:
  - worker.py --init-only contract
  findings: []
- id: GATE-INFRA-126
  phase: infra-meta
  order: 121
  owner: 'k8s/optuna/Dockerfile.worker:16 CMD'
  inputs:
  - published worker image
  outputs:
  - 'CMD ["python", "/app/experiments/mlflow/03_optuna_worker.py"] — what EVERY Job runs'
  transforms:
  - 'entrypoint custody: CI builds worker at $sha (ci.yml:178-179) and CD pushes it; the CMD decides what every Job runs yet had no owner (packet H #126)'
  touched_by: []
  validated_by: []
  if_changed:
  - worker entrypoint consumed by k8s/train-job template
  findings: []
- id: GATE-INFRA-127
  phase: infra-meta
  order: 122
  owner: './Dockerfile:19 CMD bare deployable-image default entrypoint'
  inputs:
  - deployable image build
  outputs:
  - 'CMD ["ds-pipeline"] default entrypoint (:19)'
  transforms:
  - 'CMD semantics custody (packet H #127): the IMAGE economy is the burned INFRA-136 lane (CI-BUILD-BROADWAY-LATEST); THIS gate owns CMD meaning so building and meaning do not land as one tangled change'
  touched_by: []
  validated_by: []
  if_changed:
  - default ds-pipeline invocation arguments
  findings: []
- id: GATE-INFRA-128
  phase: infra-meta
  order: 123
  owner: 'docker-compose.yml:3 mlflow/postgres service command custody'
  inputs:
  - compose stack definition
  outputs:
  - 'mlflow server command (:8 block) over built contexts ./docker/mlflow (:3) + ./docker/postgres (:16) with named volumes (:26)'
  transforms:
  - 'single file, single compose project ⇒ single-writer artifact, no split (packet H #128); census observed the stack NEVER materialized'
  - FILE-law co-aspect with GATE-INFRA-137 (contexts/volumes) declared — distinct symbols of one artifact
  touched_by: []
  validated_by: []
  if_changed:
  - compose command surface
  findings:
  - 'D#17 broken-context fact rides as FINDING root (postgres context lacks Dockerfile).'
- id: GATE-INFRA-129
  phase: infra-meta
  order: 124
  owner: 'experiments/mlflow/03_optuna_worker.py:106 optuna-worker __main__ sublane exemplar (three named sublanes enumerated in transforms)'
  inputs:
  - any experiments/** script invoked as __main__
  outputs:
  - per-sublane classification records owed per resolution #6
  transforms:
  - 'SPLIT-OWNER ratified WITH mandatory named sublanes (packet H #129 — a gate whose owner is "~60 runnable scripts" violates precise-owners): teaching-surface = experiments/{univariate,multivariate,polynomial_regression_et_all,fare_prediction,more_modeling}/ __main__ scripts · model-battle = experiments/mlflow/01_model_battle_mlflow.py + 02_explainability.py · optuna-worker = experiments/mlflow/03_optuna_worker.py'
  - 'resolution #6 forces the intentional-record-free classification to be WRITTEN DOWN per sublane at attach time — this row IS the attach record; per-sublane intentional notes remain owed'
  touched_by: []
  validated_by: []
  if_changed:
  - sublane membership (new experiment dirs must pick a lane or found one)
  findings:
  - 'flagged honestly: the per-sublane intentional-classification texts are required by resolution #6 but their prose lives outside this registry row; attach-time enumeration done here from the live tree.'
- id: GATE-INFRA-131
  phase: infra-meta
  order: 125
  owner: 'k8s/api-deployment.yaml:40 HorizontalPodAutoscaler bounds'
  inputs:
  - api workload metrics
  outputs:
  - 'autoscaling bounds minReplicas: 2 / maxReplicas: 10 / averageUtilization: 70 (:48-56)'
  transforms:
  - 'post-D#14 the manifest IS the declared SSOT and SSOTs get custody (packet H #131); leaving the newly-crowned SSOT unguarded would repeat the two-owner defect in mirror form'
  touched_by: []
  validated_by: []
  if_changed:
  - HPA bounds vs serving capacity assumptions
  findings: []
- id: GATE-INFRA-133
  phase: infra-meta
  order: 126
  owner: 'k8s/postgres-deployment.yaml:2 StatefulSet durable-state workload'
  inputs:
  - postgres manifest apply
  outputs:
  - 'durable-state workload custody (kind: StatefulSet :2)'
  transforms:
  - 'top-level manifests sit OUTSIDE kubeconform scope (hardcoded k8s/optuna/, see GATE-INFRA-99 finding) so nothing even parses it today (packet H #133)'
  touched_by: []
  validated_by: []
  if_changed:
  - postgres storage class/volume templates
  findings: []
- id: GATE-INFRA-137
  phase: infra-meta
  order: 127
  owner: 'docker-compose.yml:26 compose-stack build/volume FILE law'
  inputs:
  - compose stack definition
  outputs:
  - 'FILE law: build contexts resolve, volumes declared (:3/:16/:26)'
  transforms:
  - 'landed AMENDED (packet H #137): fold D#17 (postgres context lacks Dockerfile) as FINDING root; declare the never-materialized stack "definition-aspiration" until first runtime use'
  - 'gate owns FILE law, NOT runtime objects that do not exist; command-surface aspect co-owned by GATE-INFRA-128 (distinct symbols, one artifact)'
  touched_by: []
  validated_by: []
  if_changed:
  - compose context/volume declarations
  findings: []
- id: GATE-INFRA-138
  phase: infra-meta
  order: 128
  owner: 'tests/conftest.py:8 _SNAPSHOT_DIRS snapshot hygiene'
  inputs:
  - pytest session lifecycle
  outputs:
  - working-tree snapshot/restore of artifacts/ + reports/ around test runs
  transforms:
  - '_SNAPSHOT_DIRS = ["artifacts", "reports"] (:8, verified) — test-infra writes into the working tree and this fixture owns the hygiene'
  touched_by: []
  validated_by: []
  if_changed:
  - snapshot dir membership (new artifact roots must join or be excluded deliberately)
  findings: []
- id: GATE-INFRA-139
  phase: infra-meta
  order: 129
  owner: 'tests/test_uv_probe_guard.py:27 package-discovery probe guard suite'
  inputs:
  - uv editable-install discovery behavior
  outputs:
  - 'package-discovery guard contract (excludes the harness checkout, walks the repo root)'
  transforms:
  - 'formalization = naming the guard''s contract, NOT writing new machinery (packet H #139); existing suite attaches as validated_by'
  touched_by: []
  validated_by:
  - tests/test_uv_probe_guard.py::test_package_discovery_excludes_deepseek_harness
  - tests/test_uv_probe_guard.py::test_package_discovery_still_walks_repo_root
  if_changed:
  - package-discovery exclusion list
  findings: []
- id: GATE-INFRA-141
  phase: infra-meta
  order: 130
  owner: 'scripts/ship.sh push-path creator site for annotated tags (law: manual git tag -a mints ONLY via a named ledger-row procedure; NO scripted minter exists at HEAD)'
  inputs:
  - release/tag minting intent
  outputs:
  - published annotated tag with a ledger row naming its procedure
  transforms:
  - 'CREATION-TIME LAW (packet H #141): creator sites persist (manual git tag -a + the ship.sh push path) even while individual tags come and go; zero scripted minter exists (repo-wide grep: none) and no custody law names tags, yet tier-1-complete is PUBLISHED and pins history permanently'
  touched_by: []
  validated_by: []
  if_changed:
  - tag naming/procedure vocabulary
  findings:
  - 'Evidence CORRECTED (packet H DRIFT #4): the tag is ON ORIGIN, not local-only (ls-remote: tag object 015c5049, peeled 0d383125) — makes minting custody MORE urgent (published, unminted-law object). Credential rationale INADMISSIBLE: HC-1 VOIDED (00-resolutions.md §1); grounds survive on custody-hygiene law only.'
- id: GATE-INFRA-142
  phase: infra-meta
  order: 131
  owner: 'scripts/check_branch_parity.sh:89 sync_to_main() ref-management site (stale-ref retirement law; objects: LOCAL refs refs/heads/pr-1 + pr-2, closed-unmerged snapshots)'
  inputs:
  - named local refs (pr-1/pr-2 and future strays)
  outputs:
  - retire-or-own verdict per named ref; detect-and-retire loop execution record
  transforms:
  - 'refs verified local-only (remote-tracking remains main/sklearn/taxi/taxi_work); census danger #1 sp_probe found DELETED before ruling — proof the detect-and-retire loop works when someone executes it (packet H DRIFT #3)'
  - 'grounds hygiene-only: closed-unmerged snapshots, ownerless named refs; HC-1 void bars credential framing; reflog-only mass decays by default policy — accepted-local, out of gate scope'
  - 'CREATION-TIME LAW YES on the fetch-of-refs/pull/N/head creator pattern; anchored to the repo''s only scripted ref manager as the persisting management site (objects themselves are untracked .git state)'
  touched_by: []
  validated_by: []
  if_changed:
  - parity allowlist/branch vocabulary touching named refs
  findings:
  - 'owner-anchor judgment recorded: the gated objects are untracked .git-side refs with no in-repo declaring file; the law binds the scripted ref-management site plus the fetch/pull creator pattern rather than the ephemeral objects.'
- id: GATE-INFRA-143
  phase: infra-meta
  order: 132
  owner: '.github/workflows/ci.yml:145 actions/cache@v4 cache retention/dedupe law'
  inputs:
  - CI cache key family (broadway-base hashFiles key)
  outputs:
  - one save-site per key family; size-budget watch; purge as invoked workflow step
  transforms:
  - 'restores the broadway-base tarball keyed on pyproject/uv.lock/Dockerfile.base/src/project hash (:145-149, locally verified structure)'
  - 'GitHub-side census numbers (19 keys ≈10.39 GiB at LRU ceiling; setup-uv key saved twice = restore/save asymmetry proving two paths saved one key) accepted as recorded measurements, honestly marked NOT re-verifiable under zero-gh-ops (packet H #143)'
  - 'LAW: one save-site per key family · size-budget watch at lane level · purge = invoked workflow step, not folklore; no runtime teeth exist GitHub-side beyond workflow edits — the gate is workflow-custody law, stated as such'
  touched_by: []
  validated_by: []
  if_changed:
  - cache key family shape
  findings: []
- id: GATE-INFRA-144
  phase: infra-meta
  order: 133
  owner: '.github/workflows/ci.yml:355 Push verified images to GHCR (CD single writer)'
  inputs:
  - verified image builds from the CD job
  outputs:
  - 'registry writes: broadway-optuna-worker + mlflow-server ONLY, $sha always; :latest main-only; :taxi taxi-only'
  transforms:
  - 'writer identity defined and serialized (packet H #144): CD job needs:[platform, build-and-boot], concurrency-serialized; push/tag block verified at ci.yml:355-376'
  - 'VERSION-CAP LAW lands as creation-time law ON THE PUSH SITE (site persists while versions may or may not): mutable-tag writes are append-by-design; capping/pruning requires registry-side operations OUTSIDE repo reach — declare the cap and the unverifiability; do NOT mint teeth the repo cannot execute'
  touched_by: []
  validated_by: []
  if_changed:
  - pushed image vocabulary ($sha/latest/taxi tag law)
  findings:
  - 'inventory honestly UNVERIFIABLE: package listing 403/404 (token lacks read:packages) — recorded, not invented. OPEN QUESTION carried: who executes the registry-side version cap?'
- id: GATE-INFRA-145
  phase: infra-meta
  order: 134
  owner: 'k8s/optuna/teardown.sh teardown zero-footprint creation-time law (HEAD has ZERO rmi/residual lines; codified shape = WIP worktree :63-73)'
  inputs:
  - teardown invocation after cluster/image-producing lanes
  outputs:
  - host returned to ZERO project containers/images/volumes, FAILING LOUD otherwise
  transforms:
  - 'LAW (packet H #145): teardown leaves ZERO project containers/images/volumes and FAILS LOUD otherwise — rmi across BOTH vocabularies (CI-minted broadway-base*/broadway-optuna-worker*/mlflow-server* + manifest-demanded broadway*/broadway-mlflow*/broadway-postgres* + judged postgres:16-alpine), volume prune, loud residual assertion exiting 1'
  - 'at HEAD teardown.sh contains ZERO rmi/residual lines (grep count 0 verified) — census premise holds for the registry commit; the worktree WIP already implements exactly this shape and lands through normal tier gates'
  - 'object deleted @ hygiene-exec, creator site persists ⇒ CREATION-TIME LAW: YES — six project images + broadway-view-pg destroyed between census and ruling (packet H DRIFT #1); creator sites: ci.yml builds (:164/:178/:185), lifecycle.sh:100 kind load, view_local docker run :187'
  touched_by: []
  validated_by: []
  if_changed:
  - image vocabulary coverage of the rmi sweep
  findings:
  - 'kindest/node:<none> remnant is machine-local tooling (accepted-local) — do NOT widen the grep pattern to chase it (explicit rider, packet H #145).'
- id: GATE-INFRA-146
  phase: infra-meta
  order: 135
  owner: 'scripts/ship.sh:13 MPLCONFIGDIR cache-root export — sole surviving export; the UV_CACHE_DIR half is DELETED per the single-sanctioned-root law below'
  inputs:
  - ship-path shell session environment
  outputs:
  - ONE sanctioned uv cache root; deterministic matplotlib font-cache location
  transforms:
  - 'HISTORY (fork site of the 11.45 G second cache root, packet H #146): export UV_CACHE_DIR="${UV_CACHE_DIR:-$PWD/.uv-cache}" MPLCONFIGDIR=… stood at :10 until the cache-root law landed — HEAD carries NO UV_CACHE_DIR export anywhere in ship.sh (prohibition prose :10-12)'
  - 'LAW LANDED: one sanctioned uv cache root ($HOME/.cache/uv), a repo-local UV_CACHE_DIR export PROHIBITED (MAIN_AGENT_CONTRACT ledger & artifact hygiene); the fix WAS the deletion of the UV_CACHE_DIR export (deletion-first satisfied); MPLCONFIGDIR STAYS — export MPLCONFIGDIR="${MPLCONFIGDIR:-$PWD/.mplconfig}" at :13 (HEAD-verified), matplotlib font-cache determinism; /tmp variant documented run_local_ci.sh:18; §14 EXCEPTION (2026-08-25, eight-packet batch ruling #7): repo-root .mplconfig is the SANCTIONED MPLCONFIGDIR target — the SOLE permitted repo-local cache, per the contract sentence'
  - 'object deleted @ hygiene-exec, creator site persists ⇒ CREATION-TIME LAW: YES — repo-root .uv-cache/.mplconfig absent at ruling time (packet H DRIFT #2/#3); home-side ~/.cache/uv persists'
  touched_by: []
  validated_by: []
  if_changed:
  - cache-root export vocabulary
  findings: []
- id: GATE-INFRA-147
  phase: infra-meta
  order: 136
  owner: 'scripts/deadcode_census.py:1 module — teeth ⑥ DEADCODE-CENSUS advisory engine'
  inputs:
  - tracked *.py corpus via git ls-files (src/project/experiments/tests/scripts)
  - pyproject [project.scripts] entrypoint table
  outputs:
  - data/processed/deadcode_census.md suspicion report (gitignored sink); stdout default
  transforms:
  - 'ADVISORY-ONLY LAW (D34 tripwire 1 adopted verbatim, wave-A dispatch): census files findings to the backlog; NEVER a red gate — measures suspicion, not guilt; exclusion classes derived, never hardcoded'
  - 'engine: AST exact-token positional reference graph, substring-free; recursion/alias-safe after two scanner bug classes were fixed AGAINST live greps during delivery (ImportFrom alias invisibility; recursion-masked same-file refs) — true zero-reference count at HEAD eb9ea18 = 0; ledger-known same-module-only clusters confirmed (reports/audit.py render branch ×17, discover/qq.py ×6)'
  - 'ruff gate scope covers scripts/ dir (GATE-INFRA-92); census itself stays OUTSIDE every red gate by this row''s law'
  touched_by: []
  validated_by: []
  if_changed:
  - deadcode census vocabulary
  findings: []
# --- BAND 81-OBJECT-CUSTODY (minted per slate §0; header law above) ------
- id: GATE-CUST-147
  phase: object-custody
  order: 137
  owner: 'src/broadway/training/mlflow_utils.py:162 promote_candidate()'
  inputs:
  - 'model_uri + alias="champion"'
  outputs:
  - registered version + alias move; elsewhere loud refusal
  transforms:
  - 'champion-alias SINGLE-WRITER custody: alias writes originate SOLELY from the promotion chokepoint — register_model/set_registered_model_alias exist ONLY at :163-164; sole external caller evaluate/module.py:186'
  - 'enforcement vehicle: import-lint forbidding set_registered_model_alias/MlflowClient alias-write symbols outside training.mlflow_utils + registry audit trail'
  - 'context (packet H #147): worker pods hold full tracking reachability (configmap allowed_hosts: "*"); check_champion_manifest.sh declines to be a gate — codify the currently-true invariant BEFORE it erodes'
  touched_by:
  - mlflow.tracking.MlflowClient
  validated_by:
  - tests/test_evaluate_contracts.py::test_module_run_promotes_only_after_persistence
  if_changed:
  - champion alias vocabulary consumed by get_champion/list_champions (GATE-TRAIN-39)
  findings: []
- id: GATE-CUST-148
  phase: object-custody
  order: 138
  owner: 'src/broadway/training/mlflow_utils.py:163 register_model call sites'
  inputs:
  - candidate model_uri + dataset name
  outputs:
  - registered model name pinned to cfg.dataset.name (= configmap dataset.name)
  transforms:
  - 'registered-model-name PIN: ad-hoc names would silently fork the champion namespace scanned by get_champion/list_champions'
  - 'evidence CORRECTED (packet H #148): ONE live caller — evaluate/module.py:186 fed by dataset_name (:182), setup pairing setup_mlflow(cfg.environment.mlflow_tracking_uri, cfg.dataset.name) at :88; pin keeps a one-site accident true as sites multiply'
  touched_by: []
  validated_by: []
  if_changed:
  - dataset-name plumbing into promotion
  findings: []
- id: GATE-CUST-149
  phase: object-custody
  order: 139
  owner: 'experiments/mlflow/03_optuna_worker.py:111 set_tag trio (model/study/seed) + hpo callback tags + battle writers'
  inputs:
  - every mlflow.set_tag call across the independent writer sites
  outputs:
  - 'documented CLOSED VOCABULARY {model, study, seed}'
  transforms:
  - 'writer scatter verified (packet H #149): worker trio set_tag model/study/seed (:111-113); battle set_tag model (01_model_battle_mlflow.py:99/:233); hpo tags payload sites (hpo.py :217/:246 feeding _mlflow_callback :66)'
  - 'AMENDED enforcement vehicle: documented closed vocabulary + the SAME import-lint/test net as GATE-CUST-147 over the writer sites — NOT runtime rejection, because mlflow.set_tag accepts arbitrary keys server-side and wrapping every call buys machinery, not safety; say what the vocabulary is, lint who writes it'
  - 'W6 namespace join declared (packet G R4): TRAIN-36 metric dicts land in training/evaluate RUNS while HPO-83 nests trial runs — disjoint namespaces joined by THIS vocabulary'
  touched_by: []
  validated_by: []
  if_changed:
  - tag vocabulary additions (must land with lint-net updates)
  findings: []
- id: GATE-CUST-150
  phase: object-custody
  order: 140
  owner: 'src/broadway/training/mlflow_utils.py:59 set_experiment auto-create chokepoint (inside setup_mlflow :54-63)'
  inputs:
  - experiment_name argument at setup time
  outputs:
  - 'experiment created/reopened ONLY within lawful namespaces: ratecode1_model_battle family ∪ dataset-name experiments; orphans refused loudly'
  transforms:
  - 'setup_mlflow → mlflow.set_experiment(experiment_name) (:59) is the ONLY auto-creation path (packet H #150); refusing anything outside the two lawful namespaces turns silent orphan-farming into a loud error at the chokepoint'
  touched_by: []
  validated_by: []
  if_changed:
  - lawful-namespace vocabulary (battle family / dataset-name pattern)
  findings: []
- id: GATE-CUST-151
  phase: object-custody
  order: 141
  owner: 'k8s/optuna/lifecycle.sh:149 dump_state() snapshot generations (keep-N cap) + training-plane registry-prune policy (non-champion versions)'
  inputs:
  - registry versions + $BACKUP_DIR snapshot generations
  outputs:
  - retention as INVOKED operations with logged outcomes (no accidental append-only)
  transforms:
  - 'append-only by accident across TWO stores (packet H #151): zero deletion calls repo-wide (census B1); dump_state appends unbounded $BACKUP_DIR tarballs (:149-165)'
  - 'AMENDED: ONE gate with EXPLICIT CO-TENANCY declared per house law — (a) registry-prune policy owned at the training plane (non-champion version pruning), (b) snapshot-generation cap owned HERE (keep-N)'
  - 'deletion-first governs the POLICY (smallest lawful keep-N), not the data; "accidental append-only" is the defect being outlawed'
  touched_by: []
  validated_by: []
  if_changed:
  - keep-N budget vs restore needs (view_local reads generations)
  findings: []
- id: GATE-CUST-152
  phase: object-custody
  order: 142
  owner: 'experiments/mlflow/03_optuna_worker.py:106 optuna_{model} run-name convention (+ {study} trial {n}, battle names)'
  inputs:
  - every run-name emission across optuna/battle/callback writers
  outputs:
  - convention TEXT owned here so determinism-diff and champion manifest can rely on run identity
  transforms:
  - 'conventions verified de-facto (packet H #152): optuna_{model_name} (worker :106), {study} trial {n} (hpo callback), battle names (01_model_battle_mlflow.py:86/:230 start_run blocks)'
  - 'AMENDED: gate owns the CONVENTION TEXT; enforcement rides the GATE-CUST-147/CUST-149 lint net — no third mechanism; consumers of record: check_e2e_determinism.sh + champion manifest'
  touched_by: []
  validated_by: []
  if_changed:
  - run-name grammar consumed by determinism diffing
  findings: []
vocabulary:
- ARTIFACT-CANONICAL-FRAME
- ARTIFACT-CANONICAL-PARQUET
- ARTIFACT-CHAMPION-REGISTRY
- ARTIFACT-CLEAN-EVIDENCE
- ARTIFACT-COERCION-AUDIT
- ARTIFACT-DESCRIBE-SUMMARY
- ARTIFACT-EVALUATION-RESULT
- ARTIFACT-JOIN-AUDIT
- ARTIFACT-LOOKUP-VALUE-AUDIT
- ARTIFACT-MLFLOW-RUN
- ARTIFACT-RAW-FRAME
- ARTIFACT-RAW-PARQUET
- ARTIFACT-SAMPLE-CACHE
- ARTIFACT-SELECTED-FRAME
- ARTIFACT-STATS-PLAN
- ARTIFACT-STRUCTURAL-CLEAN
- ARTIFACT-TRAIN-PARQUET
- ARTIFACT-TRAINING-RESULT
- CFG-DATA-MODE
- CFG-DATASET-CONTRACT
- CFG-ENV-PROCESSED-SUBDIR
- CFG-ETL-SPLIT-FILES
- CFG-ETL-STEP
- CFG-STATS-FLOOR
- CFG-STEP-EVALUATE
- CFG-STEP-TRAIN
- CFG-TAXI-PROJECT
- CFG-WALKTHROUGH-THRESHOLDS
- VOCAB-DECLARED-GROUPS-ABSENT
